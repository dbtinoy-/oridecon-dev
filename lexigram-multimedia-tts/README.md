# lexigram-multimedia-tts

Text-to-speech generation for the Lexigram Framework — local and API-based
backends (`local-http`, `elevenlabs`, `openai`).

Part of the `lexigram-multimedia` package family. See the umbrella
package `lexigram-multimedia` for orchestration, or use this package
directly via `AudioTTSModule.configure(...)`.

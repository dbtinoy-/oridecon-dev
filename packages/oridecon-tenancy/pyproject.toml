[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oridecon-tenancy"
version = "0.1.000"
description = "Multi-tenant resolution, lifecycle, and isolation for the Oridecon Framework"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["oridecon", "multi-tenancy", "saas", "framework", "async", "python"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
authors = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
maintainers = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
dependencies = [
    "oridecon>=0.1.4",
    "oridecon-contracts>=0.1.4",
    "typing-extensions>=4.0.0",
    "typer>=0.9.0",
]

[project.urls]
Homepage = "https://oridecon.dev"
Repository = "https://github.com/dbtinoy-/oridecon"
Documentation = "https://docs.oridecon.dev"
Issues = "https://github.com/dbtinoy-/oridecon/issues"
Changelog = "https://github.com/dbtinoy-/oridecon/blob/main/CHANGELOG.md"

[project.optional-dependencies]
sql = ["oridecon-sql>=0.1.4"]
test = [
    "oridecon-testing>=0.1.4",
    "oridecon-workflow>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]
dev = ["ruff>=0.16.4", "mypy>=1.0.0"]
all = ["oridecon-tenancy[sql,test,dev]"]

[dependency-groups]
dev = ["ruff>=0.16.4", "mypy>=1.0.0"]
test = [
    "oridecon-testing>=0.1.4",
    "oridecon-workflow>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[project.entry-points."oridecon.modules"]
TenancyModule = "oridecon.tenancy.module:TenancyModule"

[project.entry-points."oridecon.config"]
tenancy = "oridecon.tenancy.config:TenancyConfig"

[project.entry-points."oridecon.providers"]
tenancy = "oridecon.tenancy.di.provider:TenancyProvider"

[project.entry-points."oridecon.cli.contributors"]
tenancy = "oridecon.tenancy.cli.contributor:TenancyCliContributor"

[tool.hatch.build.targets.wheel]
packages = ["src/oridecon"]

[tool.uv.sources]
oridecon = { workspace = true }
oridecon-contracts = { workspace = true }
oridecon-testing = { workspace = true }
oridecon-workflow = { workspace = true }

[tool.mypy]
python_version = "3.11"
explicit_package_bases = true
mypy_path = ["src"]
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
strict = true
show_error_codes = true
disallow_any_generics = true
overrides = [
    { module = "tests.*", ignore_errors = true },
]

[tool.ruff]
extend = "../../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint]
extend-ignore = ["F822"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "F822", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["oridecon"]
force-sort-within-sections = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = [
    "--cov=oridecon.tenancy",
    "--cov-report=term",
    "--cov-report=html",
    "--cov-fail-under=60",
]

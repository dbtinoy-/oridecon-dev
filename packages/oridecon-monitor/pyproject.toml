[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "oridecon-monitor"
version = "0.1.5009"
description = "Monitoring and observability for Oridecon Framework - Health checks, metrics, and system monitoring"
readme = "README.md"
requires-python = ">=3.11"
keywords = [
    "monitoring",
    "observability",
    "health-checks",
    "metrics",
    "prometheus",
    "grafana",
    "async",
    "framework",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: System :: Monitoring",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
dependencies = [
    "oridecon>=0.1.4",
    "oridecon-contracts>=0.1.4",
    "typing-extensions>=4.0.0",
    "starlette>=0.28.0",
    "structlog>=25.1.0",
    "typer>=0.9.0",
]
authors = [
    { name = "Oridecon Framework Team", email = "team@oridecon.dev" },
]
maintainers = [
    { name = "Oridecon Framework Team", email = "team@oridecon.dev" },
]

[project.license]
text = "MIT"

[project.optional-dependencies]
system = [
    "psutil>=5.9.0",
]
prometheus = [
    "prometheus-client>=0.19.0",
]
otel = [
    "opentelemetry-distro>=0.43b0",
    "opentelemetry-instrumentation>=0.43b0",
    "opentelemetry-exporter-otlp>=1.22.0",
]
test = [
    "oridecon-testing>=0.1.4",
    "oridecon-tasks>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]
dev = [
    "black>=23.0.0",
    "ruff>=0.16.4",
    "mypy>=1.0.0",
]
all = [
    "psutil>=5.9.0",
    "prometheus-client>=0.19.0",
    "opentelemetry-distro>=0.43b0",
    "opentelemetry-instrumentation>=0.43b0",
    "opentelemetry-exporter-otlp>=1.22.0",
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[project.urls]
Homepage = "https://oridecon.dev"
Repository = "https://github.com/dbtinoy-/oridecon"
Documentation = "https://docs.oridecon.dev"
Issues = "https://github.com/dbtinoy-/oridecon/issues"
Changelog = "https://github.com/dbtinoy-/oridecon/blob/main/CHANGELOG.md"

[project.entry-points."oridecon.modules"]
MonitorModule = "oridecon.monitor.module:MonitorModule"

[project.entry-points."oridecon.config"]
monitor = "oridecon.monitor.config:MonitorConfig"

[project.entry-points."oridecon.cli.contributors"]
monitor = "oridecon.monitor.cli.contributor:MonitorCliContributor"

[dependency-groups]
dev = [
    "black>=23.0.0",
    "ruff>=0.16.4",
    "mypy>=1.0.0",
]
test = [
    "oridecon-testing>=0.1.4",
    "oridecon-tasks>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[tool.ruff]
extend = "../../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["oridecon"]
force-sort-within-sections = true

[tool.mypy]
python_version = "3.11"
namespace_packages = true
explicit_package_bases = true
mypy_path = ["src"]
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
show_error_codes = true
overrides = [
    { module = "tests.*", ignore_errors = true },
    { module = "docs.*", ignore_errors = true },
    { module = "prometheus_client.*", ignore_missing_imports = true },
    { module = "opentelemetry.*", ignore_missing_imports = true },
    { module = "sentry_sdk.*", ignore_missing_imports = true },
    # Optional-dependency shim modules: OTel/prometheus stubs are present or
    # absent depending on which dependency groups are installed, so the
    # conditional-import fallback typing (class rebinding, dummy spans, shim
    # functions) cannot be made stable in both environments. Runtime fallback
    # behavior is covered by the monitor tests.
    { module = "oridecon.monitor.instrumentation.*", disable_error_code = ["misc", "assignment", "arg-type"] },
    { module = "oridecon.monitor.metrics.prometheus", disable_error_code = ["misc", "assignment", "arg-type"] },
]

[tool.uv.sources.oridecon]
workspace = true

[tool.uv.sources.oridecon-testing]
workspace = true

[tool.uv.sources.oridecon-contracts]
workspace = true

[tool.uv.sources.oridecon-tasks]
workspace = true

[tool.pytest.ini_options]
addopts = ["--cov=oridecon.monitor", "--cov-report=html", "--cov-report=term", "--cov-fail-under=80"]
testpaths = ["tests"]
markers = [
    "integration: mark a test as an integration test",
    "scenario: marks cross-package scenario integration tests",
    "requires_postgres: marks tests that require a live PostgreSQL connection",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/oridecon",
]

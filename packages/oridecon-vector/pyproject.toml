[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oridecon-vector"
version = "0.1.000"
description = "Vector store backends for the Oridecon Framework"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["oridecon", "vector", "embeddings", "similarity-search", "framework", "async", "python"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
authors = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
maintainers = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
dependencies = [
    "aiohttp>=3.9.0",
    "oridecon-contracts>=0.1.4",
    "oridecon>=0.1.4",
    "typer>=0.9.0",
]

[project.urls]
Homepage = "https://oridecon.dev"
Repository = "https://github.com/dbtinoy-/oridecon"
Documentation = "https://docs.oridecon.dev"
Issues = "https://github.com/dbtinoy-/oridecon/issues"
Changelog = "https://github.com/dbtinoy-/oridecon/blob/main/CHANGELOG.md"

[project.optional-dependencies]
pgvector = ["asyncpg>=0.30.0"]
pinecone = ["pinecone-client>=3.0.0"]
qdrant = ["qdrant-client>=1.9.0"]
chroma = ["chromadb>=0.4.0"]
weaviate = ["weaviate-client>=4.0.0"]
embed = ["aiohttp>=3.9.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
    "numpy>=1.24.0",
]
dev = ["ruff>=0.16.4", "mypy>=1.0.0"]
all = [
    "oridecon-vector[pgvector]",
    "oridecon-vector[pinecone]",
    "oridecon-vector[qdrant]",
    "oridecon-vector[chroma]",
    "oridecon-vector[weaviate]",
    "oridecon-vector[embed]",
]

[dependency-groups]
dev = ["ruff>=0.16.4", "mypy>=1.0.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
    "numpy>=1.24.0",
]

[project.entry-points."oridecon.modules"]
vector = "oridecon.vector.module:VectorModule"

[project.entry-points."oridecon.ai.subsystems"]
vector = "oridecon.vector.di.provider:VectorProvider"

[project.entry-points."oridecon.config"]
vector = "oridecon.vector.config:VectorConfig"

[project.entry-points."oridecon.cli.contributors"]
vector = "oridecon.vector.cli.contributor:VectorCliContributor"

[tool.hatch.build.targets.wheel]
packages = ["src/oridecon"]

[tool.uv.sources]
oridecon = { workspace = true }
oridecon-contracts = { workspace = true }
oridecon-testing = { workspace = true }

[tool.mypy]
python_version = "3.11"
namespace_packages = true
explicit_package_bases = true
mypy_path = ["src"]
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
strict = true
show_error_codes = true
ignore_missing_imports = false
overrides = [
    { module = "tests.*", ignore_errors = true },
    { module = [
        "structlog.*",
        "asyncpg.*",
        "aioredis.*",
        "aiomysql.*",
        "qdrant_client.*",
        "chromadb.*",
        "anthropic.*",
        "openai.*",
        "cohere.*",
        "groq.*",
        "mistralai.*",
        "ollama.*",
        "pinecone.*",
        "meilisearch.*",
        "boto3.*",
        "botocore.*",
        "google.cloud.*",
        "authlib.*",
        "strawberry.*",
        "mkdocs.*",
        "aiohttp.*",
        "aiofiles.*",
        "alembic.*",
        "sqlalchemy.*",
        "pydantic.*",
        "pydantic_settings.*",
        "redis.*",
        "celery.*",
        "kafka.*",
        "aiokafka.*",
        "aio_pika.*",
        "uvicorn.*",
        "starlette.*",
        "httpx.*",
        "tenacity.*",
        "cachetools.*",
        "importlib_metadata.*",
        "pkg_resources.*",
        "cryptography.*",
        "jwt.*",
        "passlib.*",
        "bcrypt.*",
        "yaml.*",
        "toml.*",
        "dotenv.*",
        "orjson.*",
        "msgpack.*",
    ], ignore_missing_imports = true },
]


[tool.ruff]
extend = "../../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["oridecon"]
force-sort-within-sections = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = [
    "--cov=oridecon.vector",
    "--cov-report=term",
    "--cov-report=html",
    "--cov-fail-under=40",
]
markers = [
    "integration: mark a test as an integration test",
]

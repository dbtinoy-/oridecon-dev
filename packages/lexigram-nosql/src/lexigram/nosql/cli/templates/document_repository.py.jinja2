"""{{ entity_name }} document repository.

Auto-generated by ``lexigram generate document-repository``.
"""

from __future__ import annotations

from typing import Any

from lexigram.contracts.data.nosql import DocumentStoreProtocol
from lexigram.nosql.repository.base import DocumentRepository


class {{ entity_name }}Repository(DocumentRepository["{{ entity_name }}", str]):
    """Repository for {{ entity_name }} documents."""

    collection_name = "{{ collection_name }}"
    id_field = "_id"

    async def _document_to_entity(self, document: dict[str, Any]) -> "{{ entity_name }}":
        """Convert a MongoDB document to a {{ entity_name }} entity."""
        return {{ entity_name }}(**document)

    async def _entity_to_document(self, entity: "{{ entity_name }}") -> dict[str, Any]:
        """Convert a {{ entity_name }} entity to a MongoDB document."""
        return entity.model_dump() if hasattr(entity, "model_dump") else vars(entity)

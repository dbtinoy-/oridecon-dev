[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oridecon-nosql"
version = "0.1.000"
description = "NoSQL document store support for the Oridecon Framework (MongoDB, DynamoDB, Firestore)"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["oridecon", "nosql", "mongodb", "document-store", "framework", "async", "python"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
authors = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
maintainers = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
dependencies = [
    "oridecon-contracts>=0.1.4",
    "oridecon>=0.1.4",
    "jinja2>=3.1.0",
]

[project.urls]
Homepage = "https://oridecon.dev"
Repository = "https://github.com/dbtinoy-/oridecon"
Documentation = "https://docs.oridecon.dev"
Issues = "https://github.com/dbtinoy-/oridecon/issues"
Changelog = "https://github.com/dbtinoy-/oridecon/blob/main/CHANGELOG.md"

[project.optional-dependencies]
mongodb = ["motor>=3.0", "pymongo>=4.6.0"]
dynamodb = ["aioboto3>=11.0.0"]
firestore = ["google-cloud-firestore>=2.19.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]
dev = ["ruff>=0.16.5", "mypy>=1.0.0"]
all = [
    "oridecon-nosql[mongodb]",
    "oridecon-nosql[dynamodb]",
    "oridecon-nosql[firestore]",
]

[dependency-groups]
dev = ["ruff>=0.16.5", "mypy>=1.0.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[project.entry-points."oridecon.modules"]
nosql = "oridecon.nosql.module:NoSQLModule"

[project.entry-points."oridecon.config"]
nosql = "oridecon.nosql.config:NoSQLConfig"

[project.entry-points."oridecon.providers"]
nosql = "oridecon.nosql.di.provider:NoSQLProvider"

[project.entry-points."oridecon.cli.contributors"]
nosql = "oridecon.nosql.cli.contributor:NoSqlCliContributor"

[tool.hatch.build.targets.wheel]
packages = ["src/oridecon"]

[tool.mypy]
ignore_missing_imports = true
python_version = "3.13"
namespace_packages = true
explicit_package_bases = true
mypy_path = ["src"]
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
strict = true
show_error_codes = true
overrides = [
    { module = "tests.*", ignore_errors = true },
]

[tool.ruff]
extend = "../../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["oridecon"]
force-sort-within-sections = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "integration: mark a test as an integration test",
    "scenario: marks cross-package scenario integration tests",
    "requires_mongodb: marks tests that require a live MongoDB connection",
]
testpaths = ["tests"]
addopts = [
    "--cov=oridecon.nosql",
    "--cov-report=term",
    "--cov-report=html",
    "--cov-fail-under=40",
]

# Runtime data (DB + render outputs + run logs) — not source
data/shorts.db
data/renders/
data/runs/
.superpowers/

# Secrets
.env
.env.*

# Runtime / generated — not source
*.db
data/assets/
data/cache/
voices/
sqlite+aiosqlite:/

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/

# DB backups
data/*.db.*

# SQLite WAL/shm sidecars (created by the running app next to data/shorts.db)
*.db-shm
*.db-wal

# Agent working files (session plans, scratch)
.opencode/
.superpowers/

# Render outputs under the src tree (mirrors data/renders/ above)
src/data/renders/

# Stray scratch file from an earlier session
/93

# Template backups
*_backup*.kdenlive

# lexigram-multimedia-upscale

Image and video super-resolution for the Lexigram Framework — local
reference-server backends (`real-esrgan`, `hat`).

Part of the `lexigram-multimedia` package family. See the umbrella
package `lexigram-multimedia` for orchestration, or use this package
directly via `UpscaleModule.configure(...)`.

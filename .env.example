# Lexigram Framework environment configuration example.
#
# Copy to .env and adjust values for your deployment:
#   cp .env.example .env
#
# Generated from docs/reference/REF_ENV_VARS.md by
# scripts/catalogs/generate_env_example.py — do not edit by hand.
#
# All variables are optional unless noted: every config value carries a
# code default, so an unset variable falls back to framework defaults.
#
# Critical secrets MUST be set in production:
#   * any value shown as <change-me-in-production>
#   * provider API keys (OpenAI, Anthropic, Google, ...) supplied through
#     your secrets manager or LEX_*_SECRET_NAME references

# ── lexigram ──
LEX_DEBUG=false  # — (str)
LEX_LEXIGRAM__ALGORITHM=  # — (str)
LEX_LEXIGRAM__ALLOWED_TAGS=  # — (set[str] | None)
LEX_LEXIGRAM__APP_NAME=  # — (str)
LEX_LEXIGRAM__AUTO_MAP=  # Fall back to auto-mapping when no explicit mapper is registered. (bool)
LEX_LEXIGRAM__BLAKE2B_DIGEST_SIZE=  # — (int)
LEX_LEXIGRAM__CIRCUIT_FAILURE_THRESHOLD=  # Consecutive failures before CircuitBreakerMiddleware opens (int)
LEX_LEXIGRAM__CIRCUIT_RECOVERY_TIMEOUT=  # Seconds before open circuit transitions to half-open (float)
LEX_LEXIGRAM__COERCE_TYPES=  # — (bool)
LEX_LEXIGRAM__CORRELATION_HEADER=  # HTTP header name used by CorrelationIdMiddleware. (str)
LEX_LEXIGRAM__CPU_POOL__MAX_WORKERS=  # — (int | None)
LEX_LEXIGRAM__CPU_POOL__THREAD_NAME_PREFIX=  # — (str | None)
LEX_LEXIGRAM__DEBUG=  # — (bool)
LEX_LEXIGRAM__DEBUG_RESOLUTION=  # — (bool)
LEX_LEXIGRAM__DEFAULT_CHANNEL_CAPACITY=  # — (int)
LEX_LEXIGRAM__DEFAULT_HASHER=  # — (str)
LEX_LEXIGRAM__DEFAULT_RETRY_COUNT=  # Default number of retries for RetryMiddleware. (int)
LEX_LEXIGRAM__DEFAULT_RETRY_DELAY=  # Default delay in seconds between retries. (float)
LEX_LEXIGRAM__DEFAULT_SANITIZE_MODE=  # — (str)
LEX_LEXIGRAM__DEFAULT_SEMAPHORE_TIMEOUT=  # — (float)
LEX_LEXIGRAM__DEFAULT_TIMEOUT=  # Default timeout in seconds for TimeoutMiddleware (float)
LEX_LEXIGRAM__DISCOVERY__AUTO_DISCOVER=  # — (bool)
LEX_LEXIGRAM__DISCOVERY__DIRECTORIES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__DISABLED_MODULES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__ENABLED_MODULES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__ENTRY_POINT_GROUP=  # — (str)
LEX_LEXIGRAM__DISPATCHER_SHUTDOWN_TIMEOUT=  # — (float)
LEX_LEXIGRAM__DKLEN=  # — (int)
LEX_LEXIGRAM__ENCODING=  # — (str)
LEX_LEXIGRAM__ENSURE_ASCII=  # — (bool)
LEX_LEXIGRAM__ENV=  # — (str)
LEX_LEXIGRAM__HASHING__ALGORITHM=  # — (str)
LEX_LEXIGRAM__HASHING__BLAKE2B_DIGEST_SIZE=  # — (int)
LEX_LEXIGRAM__HASHING__DEFAULT_HASHER=  # — (str)
LEX_LEXIGRAM__HASHING__DKLEN=  # — (int)
LEX_LEXIGRAM__HASHING__ITERATIONS=  # — (int)
LEX_LEXIGRAM__HASHING__SALT_LENGTH=  # — (int)
LEX_LEXIGRAM__HEALTH_CHECK_TIMEOUT=  # — (float)
LEX_LEXIGRAM__HEALTH__CHECK_TIMEOUT=  # — (float)
LEX_LEXIGRAM__HEALTH__INCLUDE_DETAILS=  # — (bool)
LEX_LEXIGRAM__HEALTH__STARTUP__TIMEOUT=  # — (float)
LEX_LEXIGRAM__ID_PREFIX=  # — (str)
LEX_LEXIGRAM__INDENT=  # — (int | None)
LEX_LEXIGRAM__IO_POOL=  # — (ThreadPoolConfig)
LEX_LEXIGRAM__ITERATIONS=  # — (int)
LEX_LEXIGRAM__JSON_FORMAT=  # JSON log format (bool)
LEX_LEXIGRAM__LEVEL=  # Global log level (str)
LEX_LEXIGRAM__LEVELS=  # Per-logger level overrides (dict[str, str])
LEX_LEXIGRAM__LOGGING__JSON_FORMAT=  # JSON log format (bool)
LEX_LEXIGRAM__LOGGING__LEVEL=  # Global log level (str)
LEX_LEXIGRAM__LOGGING__LEVELS=  # Per-logger level overrides (dict[str, str])
LEX_LEXIGRAM__LOGGING__REDACTION__ENABLED=  # — (bool)
LEX_LEXIGRAM__LOGGING__REDACTION__FIELD_DENYLIST=  # — (tuple[str, ...])
LEX_LEXIGRAM__LOGGING__SAMPLING__DEFAULT_RATE=  # — (float)
LEX_LEXIGRAM__LOGGING__SAMPLING__ENABLED=  # — (bool)
LEX_LEXIGRAM__LOGGING__SAMPLING__RULES=  # — (dict[str, float])
LEX_LEXIGRAM__MAX_CONCURRENT_TASKS=  # — (int)
LEX_LEXIGRAM__MAX_RESOLUTION_DEPTH=  # — (int)
LEX_LEXIGRAM__MAX_RETRIES=  # — (int)
LEX_LEXIGRAM__MODULES=  # Enabled modules (list[str])
LEX_LEXIGRAM__POOL__ENABLE_METRICS=  # — (bool)
LEX_LEXIGRAM__POOL__MAX_SIZE=  # — (int)
LEX_LEXIGRAM__POOL__MIN_SIZE=  # — (int)
LEX_LEXIGRAM__POOL__TASK_QUEUE_SIZE=  # — (int)
LEX_LEXIGRAM__POOL__WORKER_TTL=  # — (float)
LEX_LEXIGRAM__PREFERRED_BACKEND=  # — (JSONBackend)
LEX_LEXIGRAM__PREFIX_MAP=  # — (dict[str, str])
LEX_LEXIGRAM__PREFIX_SEPARATOR=  # — (str)
LEX_LEXIGRAM__QUEUE_TIMEOUT=  # — (float)
LEX_LEXIGRAM__RATE_LIMIT_MAX_REQUESTS=  # Max requests per window for RateLimiterMiddleware (int)
LEX_LEXIGRAM__RATE_LIMIT_WINDOW=  # Rate limit window in seconds (float)
LEX_LEXIGRAM__REDACTION__ENABLED=  # — (bool)
LEX_LEXIGRAM__REDACTION__FIELD_DENYLIST=  # — (tuple[str, ...])
LEX_LEXIGRAM__RETRY_FAILED_TASKS=  # — (bool)
LEX_LEXIGRAM__SALT_LENGTH=  # — (int)
LEX_LEXIGRAM__SAMPLING__DEFAULT_RATE=  # — (float)
LEX_LEXIGRAM__SAMPLING__ENABLED=  # — (bool)
LEX_LEXIGRAM__SAMPLING__RULES=  # — (dict[str, float])
LEX_LEXIGRAM__SANITIZATION__ALLOWED_TAGS=  # — (set[str] | None)
LEX_LEXIGRAM__SANITIZATION__DEFAULT_SANITIZE_MODE=  # — (str)
LEX_LEXIGRAM__SANITIZATION__STRIP_COMMENTS=  # — (bool)
LEX_LEXIGRAM__SHUTDOWN_TIMEOUT=  # — (float)
LEX_LEXIGRAM__STOP_ON_FIRST_ERROR=  # — (bool)
LEX_LEXIGRAM__STRATEGY=  # — (IdStrategy)
LEX_LEXIGRAM__STRICT=  # Raise on missing fields during mapping when True. (bool)
LEX_LEXIGRAM__STRICT_IMMUTABILITY=  # — (bool)
LEX_LEXIGRAM__STRICT_MODE=  # — (bool)
LEX_LEXIGRAM__STRIP_COMMENTS=  # — (bool)
LEX_LEXIGRAM__TYPE_HINT_CACHE_SIZE=  # — (int)
LEX_LEXIGRAM__VALIDATE_ON_REGISTER=  # — (bool)
LEX_LEXIGRAM__WORKER_THREADS=  # — (int)
LEX_PROFILE=development  # — (str)
LEX_QUIET=false  # — (str)

# ── lexigram-admin ──
LEX_ADMIN__API_PREFIX=  # — (str)
LEX_ADMIN__AUDIT__READ_AUDIT_ENABLED=  # Log read operations (off by default; compliance mode only). (bool)
LEX_ADMIN__AUTH__CSRF_TOKEN_LIFETIME=  # CSRF token expiry in seconds (int)
LEX_ADMIN__AUTH__EMAIL_OTP__ENABLED=  # Enable email OTP factor (bool)
LEX_ADMIN__AUTH__EMAIL_OTP__RESEND_COOLDOWN_SECONDS=  # Minimum seconds between email OTP sends (int)
LEX_ADMIN__AUTH__EMAIL_OTP__TTL_MINUTES=  # Code validity window in minutes (int)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__ENABLED=  # Enable email verification flow (bool)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__ENFORCEMENT=  # Block login until the email is verified (bool)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__TOKEN_TTL_HOURS=  # Verify link validity in hours (int)
LEX_ADMIN__AUTH__ENABLED=  # Enable authentication (bool)
LEX_ADMIN__AUTH__ENV=  # Deployment environment for cookie security defaults (Literal['development', 'staging', 'production'])
LEX_ADMIN__AUTH__IDLE_TIMEOUT=  # Session idle timeout in seconds (int)
LEX_ADMIN__AUTH__LOGIN_URL=  # — (str)
LEX_ADMIN__AUTH__LOGOUT_URL=  # — (str)
LEX_ADMIN__AUTH__MFA__ENABLED=  # Enable TOTP 2FA (bool)
LEX_ADMIN__AUTH__MFA__FACTOR=  # Second factor used at login: 'totp' (authenticator app) or 'email' (one-time ... (str)
LEX_ADMIN__AUTH__MFA__ISSUER=  # TOTP issuer label shown in authenticator apps (str)
LEX_ADMIN__AUTH__MFA__SKEW=  # Allowed clock skew in 30 second steps (int)
LEX_ADMIN__AUTH__OAUTH_ENABLED=  # — (bool)
LEX_ADMIN__AUTH__OAUTH_PROVIDERS=  # — (list[str])
LEX_ADMIN__AUTH__PASSWORD_POLICY__MAX_LENGTH=  # — (int)
LEX_ADMIN__AUTH__PASSWORD_POLICY__MIN_LENGTH=  # — (int)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REJECT_COMMON_PASSWORDS=  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REJECT_CONTAINING_EMAIL=  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_DIGIT=  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_LOWERCASE=  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_SPECIAL=  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_UPPERCASE=  # — (bool)
LEX_ADMIN__AUTH__PERMISSION_CACHE_TTL=  # — (int)
LEX_ADMIN__AUTH__PRINCIPAL_SOURCE=  # — (Literal['internal', 'app'])
LEX_ADMIN__AUTH__REGISTRATION__ALLOWED_EMAIL_DOMAINS=  # Restrict registration to these email domains (empty = any) (list[str])
LEX_ADMIN__AUTH__REGISTRATION__DEFAULT_ROLE=  # Role granted to new accounts (str)
LEX_ADMIN__AUTH__REGISTRATION__ENABLED=  # Allow self-service registration (bool)
LEX_ADMIN__AUTH__ROLES=  # — (dict[str, Any])
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_ENABLED=  # — (bool)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_15_MINUTES=  # — (int)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_HOUR=  # — (int)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_MINUTE=  # — (int)
LEX_ADMIN__AUTH__SECURITY__LOCKOUT_THRESHOLDS=  # — (list[tuple[int, int]])
LEX_ADMIN__AUTH__SECURITY__PERMANENT_LOCKOUT_THRESHOLD=  # — (int)
LEX_ADMIN__AUTH__SECURITY__SETUP_TOKEN=<change-me-in-production>  # Optional ADMIN_SETUP_TOKEN — when set, must be provided during first-run setup. (str | None)
LEX_ADMIN__AUTH__SECURITY__SETUP_TOKEN_OPTIN_UNSAFE=  # Explicit escape hatch: boot without a setup token. Only for local/ephemeral e... (bool)
LEX_ADMIN__AUTH__SESSION_LIFETIME=  # — (int)
LEX_ADMIN__AUTH__SESSION_SECRET=<change-me-in-production>  # Session secret for signing (SecretStr)
LEX_ADMIN__AUTH__USERS=  # — (list[Any])
LEX_ADMIN__CLUSTERS__EXTRA=  # Extra clusters beyond the built-in infrastructure cluster (list[ClusterSpec])
LEX_ADMIN__COMMANDS=  # — (list[dict[str, Any]])
LEX_ADMIN__CONTRIBUTORS=  # — (dict[str, ContributorConfig])
LEX_ADMIN__CONTRIBUTOR_COLLISION_MODE=  # How to handle name collisions when multiple contributors register widgets, pa... (Literal['warn', 'error'])
LEX_ADMIN__DASHBOARD_LAYOUT__LAYOUT=  # — (Literal['grid', 'masonry'])
LEX_ADMIN__DASHBOARD_LAYOUT__MAX_WIDGETS=  # — (int)
LEX_ADMIN__DASHBOARD_LAYOUT__WIDGET_REFRESH_DEFAULT=  # — (int)
LEX_ADMIN__DATA__QUERY_TIMEOUT_SECONDS=  # — (int)
LEX_ADMIN__DEBUG=  # — (bool)
LEX_ADMIN__ENABLED=  # — (bool)
LEX_ADMIN__EXTENSIONS=  # — (dict[str, Any])
LEX_ADMIN__FEATURES__ACTIVITY_FEED=  # — (bool)
LEX_ADMIN__FEATURES__API_DOCS=  # — (bool)
LEX_ADMIN__FEATURES__AUDIT_LOGGING=  # — (bool)
LEX_ADMIN__FEATURES__AUTOSAVE=  # — (bool)
LEX_ADMIN__FEATURES__COMMAND_PALETTE=  # — (bool)
LEX_ADMIN__FEATURES__KEYBOARD_SHORTCUTS=  # — (bool)
LEX_ADMIN__FEATURES__NOTIFICATIONS=  # — (bool)
LEX_ADMIN__FEATURES__OPTIMISTIC_UPDATES=  # — (bool)
LEX_ADMIN__FEATURES__SEARCH=  # — (bool)
LEX_ADMIN__FEATURES__THEME_TOGGLE=  # — (bool)
LEX_ADMIN__FEATURES__UNDO_REDO=  # — (bool)
LEX_ADMIN__FEATURES__WEBHOOKS=  # — (bool)
LEX_ADMIN__FORM_DEFAULTS=  # — (FormDefaults)
LEX_ADMIN__FRAMEWORK_PAGES__ENABLED=  # — (bool)
LEX_ADMIN__FRAMEWORK_PAGES__REQUIRE_PERMISSION=  # — (str)
LEX_ADMIN__HTMX_PREFIX=  # — (str)
LEX_ADMIN__INTEGRATIONS__CACHE__DEFAULT_TTL_SECONDS=  # — (int)
LEX_ADMIN__INTEGRATIONS__CACHE__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__CACHE__KEY_PREFIX=  # — (str)
LEX_ADMIN__INTEGRATIONS__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__FEATURES__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__MONITOR__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__CIRCUIT_FAILURE_THRESHOLD=  # — (int)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__RETRY_MAX_ATTEMPTS=  # — (int)
LEX_ADMIN__INTEGRATIONS__SEARCH__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__SEARCH__FALLBACK_TO_LIKE=  # — (bool)
LEX_ADMIN__INTEGRATIONS__STORAGE__ENABLED=  # — (bool)
LEX_ADMIN__INTEGRATIONS__STORAGE__PRESIGNED_URL_EXPIRY=  # — (int)
LEX_ADMIN__INTEGRATIONS__TASKS__BULK_THRESHOLD=  # — (int)
LEX_ADMIN__INTEGRATIONS__TASKS__ENABLED=  # — (bool)
LEX_ADMIN__NAME=  # — (str)
LEX_ADMIN__NAVIGATION_GROUPS=  # — (dict[str, AdminNavigationGroup])
LEX_ADMIN__OBSERVABILITY__HIGH_CARDINALITY_LABELS_ENABLED=  # — (bool)
LEX_ADMIN__OBSERVABILITY__METRICS_ENABLED=  # — (bool)
LEX_ADMIN__PREFIX=  # — (str)
LEX_ADMIN__RATE_LIMIT__BULK_PER_MINUTE=  # — (int)
LEX_ADMIN__RATE_LIMIT__BURST_SIZE=  # — (int)
LEX_ADMIN__RATE_LIMIT__CREATE_PER_MINUTE=  # — (int)
LEX_ADMIN__RATE_LIMIT__DELETE_PER_MINUTE=  # — (int)
LEX_ADMIN__RATE_LIMIT__ENABLED=  # — (bool)
LEX_ADMIN__RATE_LIMIT__REQUESTS_PER_HOUR=  # — (int)
LEX_ADMIN__RATE_LIMIT__REQUESTS_PER_MINUTE=  # — (int)
LEX_ADMIN__RATE_LIMIT__UPDATE_PER_MINUTE=  # — (int)
LEX_ADMIN__RBAC__SUPER_ADMIN_ROLE=  # already special-cased by settings/widgets/impersonation. (str)
LEX_ADMIN__REQUIRE_AUTH=  # — (bool)
LEX_ADMIN__RESOURCES=  # — (dict[str, ResourceYAMLConfig])
LEX_ADMIN__RESOURCE_DEFAULTS=  # — (ResourceDefaults)
LEX_ADMIN__STATIC_DIR=  # — (str | None)
LEX_ADMIN__STATIC_PREFIX=  # — (str)
LEX_ADMIN__STRICT_RESOURCE_RESOLUTION=  # When True (production default), resource/controller resolution failures durin... (bool)
LEX_ADMIN__TABLE_DEFAULTS=  # — (TableDefaults)
LEX_ADMIN__TEMPLATES_DIR=  # — (str | None)
LEX_ADMIN__TENANCY__COOKIE_NAME=  # — (str)
LEX_ADMIN__TENANCY__DEFAULT_TENANT_ID=  # — (str)
LEX_ADMIN__TENANCY__ENABLED=  # — (bool)
LEX_ADMIN__TENANCY__HEADER_NAME=  # — (str)
LEX_ADMIN__TENANCY__ROUTE_PREFIX_TEMPLATE=  # — (str)
LEX_ADMIN__TENANCY__TENANT_FIELD=  # — (str)
LEX_ADMIN__TITLE=  # — (str)
LEX_ADMIN__UI__CONTENT_MAX_WIDTH=  # — (int | None)
LEX_ADMIN__UI__FAVICON_URL=  # — (str | None)
LEX_ADMIN__UI__LOGO_URL=  # — (str | None)
LEX_ADMIN__UI__PRIMARY_COLOR=  # — (str)
LEX_ADMIN__UI__SIDEBAR_COLLAPSED_WIDTH=  # — (int)
LEX_ADMIN__UI__SIDEBAR_WIDTH=  # — (int)
LEX_ADMIN__UI__THEME=  # — (Literal['light', 'dark', 'system'])

# ── lexigram-ai ──
LEX_AI__ENABLED=  # Enable AI features (bool)
LEX_AI__GOVERNANCE=  # AI governance configuration (Any)
LEX_AI__LLM=  # LLM configuration (optional) (Any | None)
LEX_AI__NAME=  # Configuration name (str)
LEX_AI__OBSERVABILITY=  # AI observability configuration (tracing and metrics) (Any)
LEX_AI__RAG=  # RAG pipeline configuration (optional) (Any | None)
LEX_AI__SUBSYSTEMS=  # Dynamic configuration for third-party AI subsystems discovered via entry poin... (dict[str, dict[str, Any]])
LEX_AI__VECTOR=  # Vector store configuration (Any | None)
LEX_ENV=development  # — (str)

# ── lexigram-ai-agents ──
LEX_AI_AGENTS__DEFAULT_MAX_TOKENS=  # Default max tokens for LLM responses (int)
LEX_AI_AGENTS__DEFAULT_TEMPERATURE=  # Default temperature for LLM calls (float)
LEX_AI_AGENTS__ENABLED=  # Enable the AI agents subsystem (bool)
LEX_AI_AGENTS__ENABLE_METRICS=  # Enable Prometheus metrics (bool)
LEX_AI_AGENTS__ENABLE_TRACING=  # Enable OpenTelemetry tracing (bool)
LEX_AI_AGENTS__MAX_ITERATIONS=  # Maximum reasoning iterations per execution (int)
LEX_AI_AGENTS__TOOL_MAX_RETRIES=  # Number of retries for transient tool execution errors (ConnectionError, Timeo... (int)

# ── lexigram-ai-evaluation ──
LEX_AI_EVALUATION__DEFAULT_SEED=  # Default seed for reproducible experiment runs (int | None)
LEX_AI_EVALUATION__DEFAULT_THRESHOLD=  # Default score threshold for passing evaluations (float)
LEX_AI_EVALUATION__EMBEDDING_MODEL=  # Model to use for embedding-based evaluations (str)
LEX_AI_EVALUATION__ENABLED=  # Enable the AI evaluation subsystem (bool)
LEX_AI_EVALUATION__EXPERIMENT_DIR=  # Base directory for experiment tracking and checkpoint artifacts (str | None)
LEX_AI_EVALUATION__INCLUDE_METADATA=  # Whether to include metadata in run reports (bool)
LEX_AI_EVALUATION__MAX_RETRIES=  # Maximum retries for failed evaluations (int)
LEX_AI_EVALUATION__MAX_SAMPLES=  # Maximum number of samples per evaluation run (int | None)
LEX_AI_EVALUATION__TIMEOUT_SECONDS=  # Timeout for evaluation execution in seconds (int)

# ── lexigram-ai-feedback ──
LEX_AI_FEEDBACK__ASYNC_PROCESSING=  # Process feedback handlers asynchronously in the background (bool)
LEX_AI_FEEDBACK__ENABLED=  # Master on/off switch for all feedback collection (bool)
LEX_AI_FEEDBACK__STORE_RAW_PAYLOADS=  # Persist raw incoming feedback payloads for auditing (bool)

# ── lexigram-ai-governance ──
LEX_AI_GOVERNANCE__ENABLED=  # Enable AI governance (bool)
LEX_AI_GOVERNANCE__ENFORCE_BUDGET=  # Enforce budget limits (bool)
LEX_AI_GOVERNANCE__FAIL_OPEN_ON_PERSISTENCE_ERROR=  # Allow requests when the persistence backend is unavailable. When False (defau... (bool)
LEX_AI_GOVERNANCE__MAX_REQUEST_COST=  # Maximum cost in dollars for a single request. Requests with an estimated cost... (float | None)
LEX_AI_GOVERNANCE__MAX_TOKENS_PER_REQUEST=  # Max tokens per request (int | None)
LEX_AI_GOVERNANCE__MONTHLY_BUDGET=  # Monthly budget in dollars (float | None)
LEX_AI_GOVERNANCE__RESOURCE_UNITS=  # Resource units this governance instance tracks. Per-tenant limits are configu... (list)
LEX_AI_GOVERNANCE__RESTRICTED_MODELS=  # List of restricted models (list[str])
LEX_AI_GOVERNANCE__RPM_LIMIT=  # Requests Per Minute limit (int | None)
LEX_AI_GOVERNANCE__SOFT_LIMIT_PCT=  # Fraction of monthly_budget at which to emit a soft-limit warning (e.g. 0.8 = ... (float | None)
LEX_AI_GOVERNANCE__TPM_LIMIT=  # Tokens Per Minute limit (int | None)

# ── lexigram-ai-guard ──
LEX_AI_GUARD__ENABLED=  # — (bool)
LEX_AI_GUARD__ENABLE_LLM_GUARDS=  # — (bool)
LEX_AI_GUARD__GUARD_MODEL=  # — (str)
LEX_AI_GUARD__INJECTION_ACTION=  # — (str)
LEX_AI_GUARD__INJECTION_DETECTION=  # — (bool)
LEX_AI_GUARD__LENGTH_ACTION=  # — (str)
LEX_AI_GUARD__LLM_GUARD_FAIL_OPEN=  # — (bool)
LEX_AI_GUARD__LLM_GUARD_THRESHOLD=  # — (float)
LEX_AI_GUARD__MAX_INPUT_CHARS=  # — (int)
LEX_AI_GUARD__MAX_OUTPUT_CHARS=  # — (int)
LEX_AI_GUARD__PARALLEL_EXECUTION=  # — (bool)
LEX_AI_GUARD__PII_ACTION=  # — (str)
LEX_AI_GUARD__PII_DETECTION=  # — (bool)
LEX_AI_GUARD__PII_ENTITIES=  # — (list[str])
LEX_AI_GUARD__PII_REDACTION_OUTPUT=  # — (bool)
LEX_AI_GUARD__RESTRICTED_TOPICS=  # — (list[str])
LEX_AI_GUARD__SENSITIVITY_LEVEL=  # — (str)

# ── lexigram-ai-llm ──
LEX_AI_LLM__API_BASE=  # Custom API base URL (for Azure, local, or proxied endpoints). (str | None)
LEX_AI_LLM__API_KEY=<change-me-in-production>  # API key for the chosen provider. (SecretStr | None)
LEX_AI_LLM__BACKEND=  # Backend type: ``memory`` or ``database``. (Literal['memory', 'database'])
LEX_AI_LLM__BASE_URL=  # Base URL override (local providers, Azure, custom endpoints). (str | None)
LEX_AI_LLM__BATCH_SIZE=  # — (int)
LEX_AI_LLM__CACHE_TTL=  # Cache TTL in seconds. (int)
LEX_AI_LLM__COOLDOWN_SECONDS=  # Cooldown applied to a cascade entry after an HTTP 429. Env var: LEX_AI_LLM__Q... (int)
LEX_AI_LLM__DEFAULTS=  # Default generation parameters. (GenerationDefaults)
LEX_AI_LLM__DIMENSIONS=  # — (int | None)
LEX_AI_LLM__ENABLED=  # Enable the LLM subsystem (bool)
LEX_AI_LLM__ENABLE_CACHE=  # Enable response caching (requires CacheBackendProtocol in container). (bool)
LEX_AI_LLM__EXTRA=  # Provider-specific extra parameters passed verbatim. (dict[str, Any])
LEX_AI_LLM__EXTRAS=  # Provider-specific credential fields: azure_resource, azure_deployment, azure_... (dict[str, str])
LEX_AI_LLM__LOGGING__BACKEND=  # Backend type: ``memory`` or ``database``. (Literal['memory', 'database'])
LEX_AI_LLM__LOGGING__MAX_ENTRIES=  # Maximum in-memory log entries before FIFO eviction. (int)
LEX_AI_LLM__MAX_ENTRIES=  # Maximum in-memory log entries before FIFO eviction. (int)
LEX_AI_LLM__MAX_TOKENS=  # Maximum tokens in response. (int | None)
LEX_AI_LLM__MODEL=  # Model name or identifier. (str)
LEX_AI_LLM__NAME=  # Provider name (must match ProviderRegistry key). (str)
LEX_AI_LLM__PIN_POLICY=  # Policy for enforcing the model revision pin. (ModelPinPolicy)
LEX_AI_LLM__PRICING__CACHE_TTL=  # Pricing cache TTL in seconds (default: 24 hours). (int)
LEX_AI_LLM__PRICING__ENABLED=  # Register pricing manager and cost estimator. (bool)
LEX_AI_LLM__PRICING__ENABLE_FUZZY_MATCH=  # Allow substring matching of model names to prices. (bool)
LEX_AI_LLM__PRICING__SOURCES=  # Pricing sources in priority order. (list[PricingSourceConfig])
LEX_AI_LLM__PROVIDER=  # LLM provider name. (ModelProvider)
LEX_AI_LLM__PROVIDERS=  # Ordered list of providers (cascade order). First = highest priority. (list[ProviderConfig])
LEX_AI_LLM__QUOTA__BACKEND=  # Backend type: ``memory`` or ``database``. (Literal['memory', 'database'])
LEX_AI_LLM__QUOTA__COOLDOWN_SECONDS=  # Cooldown applied to a cascade entry after an HTTP 429. Env var: LEX_AI_LLM__Q... (int)
LEX_AI_LLM__STRATEGY=  # Routing strategy. ``sequential``: try providers in cascade order. ``parallel_... (Literal['sequential', 'parallel_race', 'cost_optimized', 'latency_optimized'])
LEX_AI_LLM__SUPPRESS_THINKING=  # When True, the LLM client actively disables thinking/chain-of-thought token g... (bool)
LEX_AI_LLM__TEMPERATURE=  # Sampling temperature. (float)
LEX_AI_LLM__THINKING=  # Thinking/reasoning configuration.  ``None`` disables thinking. Set to a ``Thi... (ThinkingConfig | None)
LEX_AI_LLM__TIMEOUT=  # Request timeout in seconds. (float)

# ── lexigram-ai-mcp ──
LEX_AI_MCP__ALLOW_UNAUTHENTICATED=  # — (bool)
LEX_AI_MCP__CLIENT_STDIO_COMMAND=  # — (list[str])
LEX_AI_MCP__CLIENT_URL=  # — (str | None)
LEX_AI_MCP__CONNECTORS__FILESYSTEM__READ_ONLY=  # — (bool)
LEX_AI_MCP__CONNECTORS__FILESYSTEM__ROOT_DIR=  # — (str)
LEX_AI_MCP__CONNECTORS__GITHUB__API_URL=  # — (str)
LEX_AI_MCP__CONNECTORS__GITHUB__TOKEN=<change-me-in-production>  # — (str)
LEX_AI_MCP__CONNECTORS__GOOGLE_DRIVE__IMPERSONATED_EMAIL=  # — (str)
LEX_AI_MCP__CONNECTORS__GOOGLE_DRIVE__SERVICE_ACCOUNT_JSON=  # — (str)
LEX_AI_MCP__CONNECTORS__SLACK__BOT_TOKEN=<change-me-in-production>  # — (str)
LEX_AI_MCP__CONNECTORS__SLACK__MAX_MESSAGES=  # — (int)
LEX_AI_MCP__CONNECTORS__SQL__ALLOWED_TABLES=  # — (list[str])
LEX_AI_MCP__CONNECTORS__SQL__DSN=  # — (str)
LEX_AI_MCP__CONNECTORS__SQL__READ_ONLY=  # — (bool)
LEX_AI_MCP__CONNECTORS__WEB_FETCH__ENABLED=  # — (bool)
LEX_AI_MCP__CONNECTORS__WEB_FETCH__MAX_CONTENT_BYTES=  # — (int)
LEX_AI_MCP__CONNECTORS__WEB_FETCH__USER_AGENT=  # — (str)
LEX_AI_MCP__CONNECTORS__WEB_SEARCH__API_KEY=<change-me-in-production>  # — (str)
LEX_AI_MCP__CONNECTORS__WEB_SEARCH__MAX_RESULTS=  # — (int)
LEX_AI_MCP__CONNECTORS__WEB_SEARCH__PROVIDER=  # — (str)
LEX_AI_MCP__CORS_ORIGINS=  # — (list[str])
LEX_AI_MCP__ENABLED=  # Enable the MCP server subsystem (bool)
LEX_AI_MCP__ENABLE_SSE=  # — (bool)
LEX_AI_MCP__HOST=  # — (str)
LEX_AI_MCP__MAX_REQUEST_SIZE=  # — (int)
LEX_AI_MCP__PATH=  # — (str)
LEX_AI_MCP__PORT=  # — (int)
LEX_AI_MCP__REQUEST_TIMEOUT=  # — (float)
LEX_AI_MCP__SERVER_NAME=  # — (str)
LEX_AI_MCP__SERVER_VERSION=  # — (str)
LEX_AI_MCP__STDIO_MODE=  # — (bool)

# ── lexigram-ai-memory ──
LEX_AI_MEMORY__AGE_THRESHOLD_HOURS=  # Minimum entry age (hours) before it can be consolidated (float)
LEX_AI_MEMORY__BATCH_SIZE=  # Maximum entries processed per consolidation pass (int)
LEX_AI_MEMORY__CONSOLIDATION__AGE_THRESHOLD_HOURS=  # Minimum entry age (hours) before it can be consolidated (float)
LEX_AI_MEMORY__CONSOLIDATION__BATCH_SIZE=  # Maximum entries processed per consolidation pass (int)
LEX_AI_MEMORY__CONSOLIDATION__ENABLED=  # Whether automatic background consolidation is active (bool)
LEX_AI_MEMORY__CONSOLIDATION__IMPORTANCE_PRUNE_THRESHOLD=  # Entries below this importance score are eligible for pruning (float)
LEX_AI_MEMORY__CONSOLIDATION__INTERVAL_SECONDS=  # How often to run a consolidation pass (seconds) (float)
LEX_AI_MEMORY__DEFAULT_BACKEND=  # Backend type to use ('in_memory', 'cache', 'database', 'vector') (str)
LEX_AI_MEMORY__DEFAULT_TOP_K=  # Default number of episodes to retrieve (int)
LEX_AI_MEMORY__ENABLED=  # Whether automatic background consolidation is active (bool)
LEX_AI_MEMORY__EPISODIC_FRACTION=  # Fraction of remaining budget for episodic recall (float)
LEX_AI_MEMORY__EPISODIC__DEFAULT_TOP_K=  # Default number of episodes to retrieve (int)
LEX_AI_MEMORY__EPISODIC__IMPORTANCE_WEIGHT=  # Weight applied to entry importance during scoring (float)
LEX_AI_MEMORY__EPISODIC__RECENCY_WEIGHT=  # Weight applied to temporal recency during scoring (float)
LEX_AI_MEMORY__EPISODIC__RELEVANCE_WEIGHT=  # Weight applied to semantic similarity during scoring (float)
LEX_AI_MEMORY__EPISODIC__TTL_SECONDS=  # Time-to-live for entries in seconds (0 = never expire) (int)
LEX_AI_MEMORY__IMPORTANCE_PRUNE_THRESHOLD=  # Entries below this importance score are eligible for pruning (float)
LEX_AI_MEMORY__IMPORTANCE_WEIGHT=  # Weight applied to entry importance during scoring (float)
LEX_AI_MEMORY__INTERVAL_SECONDS=  # How often to run a consolidation pass (seconds) (float)
LEX_AI_MEMORY__MAX_FACTS_PER_ENTITY=  # Hard cap on stored facts per entity (int)
LEX_AI_MEMORY__MAX_RECENT_TURNS=  # Hard cap on recent turns regardless of budget (int)
LEX_AI_MEMORY__MIN_CONFIDENCE=  # Minimum confidence score required to store a fact (float)
LEX_AI_MEMORY__RECENCY_WEIGHT=  # Weight applied to temporal recency during scoring (float)
LEX_AI_MEMORY__RECENT_TURNS_FRACTION=  # Fraction of remaining budget for recent turns (float)
LEX_AI_MEMORY__RELEVANCE_WEIGHT=  # Weight applied to semantic similarity during scoring (float)
LEX_AI_MEMORY__SEMANTIC_FRACTION=  # Fraction of remaining budget for semantic facts (float)
LEX_AI_MEMORY__SEMANTIC__MAX_FACTS_PER_ENTITY=  # Hard cap on stored facts per entity (int)
LEX_AI_MEMORY__SEMANTIC__MIN_CONFIDENCE=  # Minimum confidence score required to store a fact (float)
LEX_AI_MEMORY__SYSTEM_PROMPT_TOKENS=  # Fixed token allocation for system prompt (int)
LEX_AI_MEMORY__TOOL_DESCRIPTIONS_FRACTION=  # Fraction of remaining budget for tool descriptions (float)
LEX_AI_MEMORY__TTL_SECONDS=  # Time-to-live for entries in seconds (0 = never expire) (int)
LEX_AI_MEMORY__WORKING__EPISODIC_FRACTION=  # Fraction of remaining budget for episodic recall (float)
LEX_AI_MEMORY__WORKING__MAX_RECENT_TURNS=  # Hard cap on recent turns regardless of budget (int)
LEX_AI_MEMORY__WORKING__RECENT_TURNS_FRACTION=  # Fraction of remaining budget for recent turns (float)
LEX_AI_MEMORY__WORKING__SEMANTIC_FRACTION=  # Fraction of remaining budget for semantic facts (float)
LEX_AI_MEMORY__WORKING__SYSTEM_PROMPT_TOKENS=  # Fixed token allocation for system prompt (int)
LEX_AI_MEMORY__WORKING__TOOL_DESCRIPTIONS_FRACTION=  # Fraction of remaining budget for tool descriptions (float)

# ── lexigram-ai-observability ──
LEX_AI_OBSERVABILITY__ENABLED=  # Master on/off switch for all observability (bool)
LEX_AI_OBSERVABILITY__HEALTH_CHECKS_ENABLED=  # Enable background health checking for AI components (bool)
LEX_AI_OBSERVABILITY__METRICS_ENABLED=  # Enable metrics collection (bool)
LEX_AI_OBSERVABILITY__TRACE_MAX_ATTRIBUTE_LENGTH=  # Cap on string attribute values written to trace spans, in characters. 0 disab... (int)
LEX_AI_OBSERVABILITY__TRACE_REDACTION_ENABLED=  # Redact secret-shaped keys (e.g. token, password, api_key) from trace span att... (bool)
LEX_AI_OBSERVABILITY__TRACING_ENABLED=  # Enable distributed tracing (bool)

# ── lexigram-ai-prompt ──
LEX_AI_PROMPT__DEFAULT_FORMAT=  # — (RenderFormat)
LEX_AI_PROMPT__ENABLED=  # Enable the AI prompt subsystem (bool)
LEX_AI_PROMPT__MAX_VARIABLE_LENGTH=  # — (int)
LEX_AI_PROMPT__SANITIZE_INPUTS=  # — (bool)
LEX_AI_PROMPT__STRICT_SANITIZER=  # — (bool)

# ── lexigram-ai-rag ──
LEX_AI_RAG__AUTO_EVALUATE_EVERY_N=  # Run automatic evaluation every N pipeline requests. None disables auto-evalua... (int | None)
LEX_AI_RAG__CACHE_ENABLED=  # — (bool)
LEX_AI_RAG__CACHE_RESULTS=  # — (bool)
LEX_AI_RAG__CACHE_TTL=  # Cache TTL in seconds (default: 1 hour) (int)
LEX_AI_RAG__CHUNKING_STRATEGY=  # Chunking strategy (recursive, semantic, token) (str)
LEX_AI_RAG__CHUNK_OVERLAP=  # Overlap between consecutive chunks (int)
LEX_AI_RAG__CHUNK_SIZE=  # Text chunk size in tokens (int)
LEX_AI_RAG__CITATIONS_ENABLED=  # — (bool)
LEX_AI_RAG__CITATION_STYLE=  # Citation style (inline, footnote, numbered) (str)
LEX_AI_RAG__COLLECTION_NAME=  # Collection/index name for vector store (str)
LEX_AI_RAG__COLLECT_METRICS=  # — (bool)
LEX_AI_RAG__COMPRESSION_ENABLED=  # — (bool)
LEX_AI_RAG__COMPRESSION_STRATEGY=  # — (str)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__CITATIONS_ENABLED=  # — (bool)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__COMPRESSION_ENABLED=  # — (bool)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__COMPRESSION_STRATEGY=  # — (str)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__DEDUPLICATION_ENABLED=  # — (bool)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__DEDUPLICATION_THRESHOLD=  # — (float)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__ENABLED=  # — (bool)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__MAX_CONTEXT_TOKENS=  # — (int)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__RANKING_ENABLED=  # — (bool)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__STRATEGY=  # — (str)
LEX_AI_RAG__CONTEXT_OPTIMIZATION__TOP_K=  # — (int)
LEX_AI_RAG__DEDUPLICATION_ENABLED=  # — (bool)
LEX_AI_RAG__DEDUPLICATION_THRESHOLD=  # — (float)
LEX_AI_RAG__DEFAULT_ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__DESCRIPTION=  # — (str)
LEX_AI_RAG__DETAILED_LOGGING=  # — (bool)
LEX_AI_RAG__DOCUMENT_FORMATS=  # — (list[DocumentFormat])
LEX_AI_RAG__EMBEDDING_MODEL=  # Embedding model identifier. Must be set explicitly — no vendor-specific default. (str | None)
LEX_AI_RAG__EMBEDDING_PROVIDER=  # Embedding provider (openai, cohere, etc.) (str)
LEX_AI_RAG__ENABLED=  # Enable the RAG pipeline (bool)
LEX_AI_RAG__ENABLE_CACHING=  # Enable caching for RAG queries (bool)
LEX_AI_RAG__ENABLE_CITATIONS=  # Include source citations in responses (bool)
LEX_AI_RAG__ENABLE_HALLUCINATION_DETECTION=  # Enable hallucination detection for AI responses (bool)
LEX_AI_RAG__ENABLE_HYDE=  # Enable HyDE (Hypothetical Document Embeddings) (bool)
LEX_AI_RAG__ENABLE_QUERY_EXPANSION=  # Enable query expansion techniques (bool)
LEX_AI_RAG__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__FAIL_FAST=  # — (bool)
LEX_AI_RAG__HALLUCINATION_DETECTION_ENABLED=  # — (bool)
LEX_AI_RAG__HALLUCINATION_STRICT_MODE=  # — (bool)
LEX_AI_RAG__HYDE_ENABLED=  # — (bool)
LEX_AI_RAG__HYDE_NUM_DOCUMENTS=  # — (int)
LEX_AI_RAG__INGESTION__CHUNKING_STRATEGY=  # — (str)
LEX_AI_RAG__INGESTION__CHUNK_OVERLAP=  # — (int)
LEX_AI_RAG__INGESTION__CHUNK_SIZE=  # — (int)
LEX_AI_RAG__INGESTION__DOCUMENT_FORMATS=  # — (list[DocumentFormat])
LEX_AI_RAG__INGESTION__ENABLED=  # — (bool)
LEX_AI_RAG__INGESTION__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__INGESTION__FAIL_FAST=  # — (bool)
LEX_AI_RAG__INGESTION__METADATA_ENRICHMENT_ENABLED=  # — (bool)
LEX_AI_RAG__INGESTION__MIN_CHUNK_SIZE=  # — (int)
LEX_AI_RAG__INGESTION__OCR_ENABLED=  # — (bool)
LEX_AI_RAG__INGESTION__PREPROCESSING_ENABLED=  # — (bool)
LEX_AI_RAG__INGESTION__TABLE_EXTRACTION_ENABLED=  # — (bool)
LEX_AI_RAG__KNOWLEDGE_GRAPH_ENABLED=  # — (bool)
LEX_AI_RAG__MAX_CONTEXT_TOKENS=  # — (int)
LEX_AI_RAG__MAX_GRAPH_DEPTH=  # — (int)
LEX_AI_RAG__MAX_HOPS=  # — (int)
LEX_AI_RAG__MAX_RETRIES=  # — (int)
LEX_AI_RAG__METADATA=  # — (dict[str, Any])
LEX_AI_RAG__METADATA_ENRICHMENT_ENABLED=  # — (bool)
LEX_AI_RAG__MIN_CHUNK_SIZE=  # — (int)
LEX_AI_RAG__MIN_CITATION_CONFIDENCE=  # Minimum confidence for citation inclusion (float)
LEX_AI_RAG__MIN_CONFIDENCE=  # — (float)
LEX_AI_RAG__MIN_FAITHFULNESS=  # — (float)
LEX_AI_RAG__MIN_RELEVANCE=  # — (float)
LEX_AI_RAG__MULTI_HOP_ENABLED=  # — (bool)
LEX_AI_RAG__NAME=  # — (str)
LEX_AI_RAG__OCR_ENABLED=  # — (bool)
LEX_AI_RAG__PERSIST_DIRECTORY=  # Local directory path for vector store persistence (e.g. Chroma) (str | None)
LEX_AI_RAG__POST_PROCESSING__CACHE_ENABLED=  # — (bool)
LEX_AI_RAG__POST_PROCESSING__CACHE_RESULTS=  # — (bool)
LEX_AI_RAG__POST_PROCESSING__COLLECT_METRICS=  # — (bool)
LEX_AI_RAG__POST_PROCESSING__DETAILED_LOGGING=  # — (bool)
LEX_AI_RAG__POST_PROCESSING__ENABLED=  # — (bool)
LEX_AI_RAG__POST_PROCESSING__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__PREPROCESSING_ENABLED=  # — (bool)
LEX_AI_RAG__QUALITY_ASSURANCE__ENABLED=  # — (bool)
LEX_AI_RAG__QUALITY_ASSURANCE__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__QUALITY_ASSURANCE__HALLUCINATION_DETECTION_ENABLED=  # — (bool)
LEX_AI_RAG__QUALITY_ASSURANCE__HALLUCINATION_STRICT_MODE=  # — (bool)
LEX_AI_RAG__QUALITY_ASSURANCE__MIN_CONFIDENCE=  # — (float)
LEX_AI_RAG__QUALITY_ASSURANCE__MIN_FAITHFULNESS=  # — (float)
LEX_AI_RAG__QUALITY_ASSURANCE__MIN_RELEVANCE=  # — (float)
LEX_AI_RAG__QUALITY_ASSURANCE__REJECT_LOW_QUALITY=  # — (bool)
LEX_AI_RAG__QUALITY_ASSURANCE__WARN_LOW_QUALITY=  # — (bool)
LEX_AI_RAG__QUERY_PROCESSING__ENABLED=  # — (bool)
LEX_AI_RAG__QUERY_PROCESSING__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__QUERY_PROCESSING__HYDE_ENABLED=  # — (bool)
LEX_AI_RAG__QUERY_PROCESSING__HYDE_NUM_DOCUMENTS=  # — (int)
LEX_AI_RAG__QUERY_PROCESSING__ROUTING_ENABLED=  # — (bool)
LEX_AI_RAG__QUERY_PROCESSING__ROUTING_STRATEGY=  # — (RoutingStrategyType)
LEX_AI_RAG__QUERY_PROCESSING__TRANSFORMATION_ENABLED=  # — (bool)
LEX_AI_RAG__QUERY_PROCESSING__TRANSFORMATION_STRATEGIES=  # — (list[str])
LEX_AI_RAG__RANKING_ENABLED=  # — (bool)
LEX_AI_RAG__REJECT_LOW_QUALITY=  # — (bool)
LEX_AI_RAG__REQUIRE_CITATIONS=  # Raise MissingCitationsError when the synthesis result contains no citations. (bool)
LEX_AI_RAG__RETRIEVAL__ENABLED=  # — (bool)
LEX_AI_RAG__RETRIEVAL__ERROR_STRATEGY=  # — (str)
LEX_AI_RAG__RETRIEVAL__KNOWLEDGE_GRAPH_ENABLED=  # — (bool)
LEX_AI_RAG__RETRIEVAL__MAX_GRAPH_DEPTH=  # — (int)
LEX_AI_RAG__RETRIEVAL__MAX_HOPS=  # — (int)
LEX_AI_RAG__RETRIEVAL__MULTI_HOP_ENABLED=  # — (bool)
LEX_AI_RAG__RETRIEVAL__SIMILARITY_THRESHOLD=  # — (float)
LEX_AI_RAG__RETRIEVAL__STRATEGY=  # — (str)
LEX_AI_RAG__RETRIEVAL__TOP_K=  # — (int)
LEX_AI_RAG__RETRIEVAL__VECTOR_SEARCH_ENABLED=  # — (bool)
LEX_AI_RAG__RETRY_DELAY=  # — (float)
LEX_AI_RAG__ROUTING_ENABLED=  # — (bool)
LEX_AI_RAG__ROUTING_STRATEGY=  # — (RoutingStrategyType)
LEX_AI_RAG__SIMILARITY_THRESHOLD=  # Minimum similarity score threshold (float)
LEX_AI_RAG__STAGES=  # Ordered list of pipeline stages to execute (list[PipelineStageType])
LEX_AI_RAG__STRATEGY=  # — (str)
LEX_AI_RAG__SYNTHESIS=  # — (SynthesisConfig)
LEX_AI_RAG__SYNTHESIS_STRATEGY=  # Synthesis strategy (direct, extractive, abstractive, hybrid) (str)
LEX_AI_RAG__TABLE_EXTRACTION_ENABLED=  # — (bool)
LEX_AI_RAG__TENANCY__ENABLED=  # Enable tenant-aware collection resolution in RAG pipeline (bool)
LEX_AI_RAG__TOP_K=  # Number of documents to retrieve (int)
LEX_AI_RAG__TRANSFORMATION_ENABLED=  # — (bool)
LEX_AI_RAG__TRANSFORMATION_STRATEGIES=  # — (list[str])
LEX_AI_RAG__USE_HYBRID_SEARCH=  # Enable hybrid search (semantic + keyword) (bool)
LEX_AI_RAG__VECTOR_DIMENSION=  # Embedding vector dimension (1536 for OpenAI ada-002) (int)
LEX_AI_RAG__VECTOR_SEARCH_ENABLED=  # — (bool)
LEX_AI_RAG__VECTOR_STORE_TYPE=  # Vector store backend (pgvector, chroma, qdrant, mock) (str)
LEX_AI_RAG__WARN_LOW_QUALITY=  # — (bool)

# ── lexigram-ai-relay-gateway ──
LEX_AI_RELAY_GATEWAY__AUTO_DISABLE_ON_FAILURES=  # — (bool)
LEX_AI_RELAY_GATEWAY__AUTO_TEST_CHANNELS=  # — (bool)
LEX_AI_RELAY_GATEWAY__AUTO_TEST_INTERVAL_SECONDS=  # — (int)
LEX_AI_RELAY_GATEWAY__CHANNELS=  # — (tuple[RelayChannel, ...])
LEX_AI_RELAY_GATEWAY__FAILOVER_FAILURE_THRESHOLD=  # — (int)
LEX_AI_RELAY_GATEWAY__JOB_TTL_SECONDS=  # — (int)
LEX_AI_RELAY_GATEWAY__LOAD_BALANCING=  # — (Literal['deterministic', 'weighted'])
LEX_AI_RELAY_GATEWAY__MAX_UPSTREAM_RETRIES=  # — (int)
LEX_AI_RELAY_GATEWAY__PROVIDER_OPTIONS=  # — (Mapping[str, Mapping[str, JsonValue]])
LEX_AI_RELAY_GATEWAY__RATE_LIMITS=  # — (Mapping[str, Mapping[str, int]])
LEX_AI_RELAY_GATEWAY__REQUIRE_AUTH=  # — (bool)

# ── lexigram-ai-session ──
LEX_AI_SESSION__AUTO_CHECKPOINT_INTERVAL=  # Checkpoint every N turns; None to disable (int | None)
LEX_AI_SESSION__BACKEND=  # Persistence backend (in_memory, cache, database) (str)
LEX_AI_SESSION__CLEANUP_INTERVAL_S=  # How often the cleanup scheduler sweeps for expired sessions (int)
LEX_AI_SESSION__CONSOLIDATE_ON_CLOSE=  # Whether to trigger memory consolidation on session close (bool)
LEX_AI_SESSION__COOKIE_NAME=  # Cookie name for web session ID; None disables cookies (str | None)
LEX_AI_SESSION__DEFAULT_SYSTEM_PROMPT=  # System prompt injected into every new session (str | None)
LEX_AI_SESSION__DEFAULT_TURN_STRATEGY=  # Default turn-selection strategy (round_robin, priority, llm_directed) (str)
LEX_AI_SESSION__ENABLED=  # Enable the AI session subsystem (bool)
LEX_AI_SESSION__HEADER_NAME=  # HTTP header name for session ID pass-through (str)
LEX_AI_SESSION__MAX_AGENTS_PER_GROUP=  # Maximum agents in a multi-agent group session (int)
LEX_AI_SESSION__MAX_BRANCHES_PER_SESSION=  # Maximum forked branches per session (int)
LEX_AI_SESSION__MAX_CHECKPOINTS_PER_SESSION=  # Maximum retained checkpoints per session (int)
LEX_AI_SESSION__MAX_SESSIONS_PER_USER=  # Maximum concurrent sessions per user (int)
LEX_AI_SESSION__MAX_TURNS_PER_SESSION=  # Hard cap on turns before the session is closed (int)
LEX_AI_SESSION__NAME=  # Logical name used for DI registration keys (str)
LEX_AI_SESSION__SESSION_TTL=  # Maximum age of a session in seconds (0 to disable) (int)

# ── lexigram-ai-skills ──
LEX_AI_SKILLS__ALLOWED_SCRIPT_TYPES=  # Allowed script types (py, sh, js) (list[str])
LEX_AI_SKILLS__AUTO_DISCOVER=  # Whether to auto-scan packages for skills on boot (bool)
LEX_AI_SKILLS__BUILTIN_SKILLS=  # Names of built-in skills to register (list[str])
LEX_AI_SKILLS__CACHE_BACKEND=  # Which cache backend to use (in_memory, cache) (str)
LEX_AI_SKILLS__CACHE_ENABLED=  # Whether result caching is globally enabled (bool)
LEX_AI_SKILLS__CACHE_TTL_SECONDS=  # Default TTL for cached skill results (seconds) (int)
LEX_AI_SKILLS__DEFAULT_TIMEOUT_SECONDS=  # Default execution timeout per skill (seconds) (float)
LEX_AI_SKILLS__ENABLED_DIRECTORIES=  # Which skill directories to enable (claude_code, opencode, cursor, etc.) (list[str])
LEX_AI_SKILLS__ENABLE_BUILTIN=  # Whether built-in skills are registered on boot (bool)
LEX_AI_SKILLS__ENABLE_SKILL_SOURCES=  # Whether to scan for external skill sources on boot (bool)
LEX_AI_SKILLS__ENFORCE_PERMISSIONS=  # Whether permission checks are enforced (bool)
LEX_AI_SKILLS__LAZY_LOAD_CONTEXT=  # Whether to lazily load skill context files (bool)
LEX_AI_SKILLS__MAX_CONCURRENT_EXECUTIONS=  # Semaphore cap on concurrent skill executions (int)
LEX_AI_SKILLS__MAX_RETRIES=  # Default maximum retry attempts for skill execution (int)
LEX_AI_SKILLS__NAME=  # Logical name used for DI registration keys (str)
LEX_AI_SKILLS__SCAN_PACKAGES=  # Fully-qualified package names to scan for skills (list[str])
LEX_AI_SKILLS__SCRIPT_TIMEOUT_SECONDS=  # Timeout for skill script execution (seconds) (int)
LEX_AI_SKILLS__SKILL_PATHS=  # Paths to scan for skills (SKILL.md folders) (list[str])

# ── lexigram-ai-workers ──
LEX_AI_WORKERS__BATCH_EMBEDDING_CONCURRENCY=  # Concurrency level for batch embedding execution (int)
LEX_AI_WORKERS__DLQ_CHECK_INTERVAL=  # Interval in seconds for DLQ recovery sweeps (int)
LEX_AI_WORKERS__DOCUMENT_INGESTION_CONCURRENCY=  # Concurrency level for document parsing and chunking (int)
LEX_AI_WORKERS__ENABLED=  # Master on/off switch for all background workers (bool)
LEX_AI_WORKERS__ENABLE_MAINTENANCE=  # Enable vector store and cache maintenance tasks (bool)

# ── lexigram-audit ──
LEX_AUDIT__ENABLE_ADMIN=  # Whether to register the AuditAdminContributor (bool)
LEX_AUDIT__HMAC_KEY=<change-me-in-production>  # HMAC key for checksum computation (bytes | None)
LEX_AUDIT__RETENTION_POLICY=  # Retention rules (RetentionPolicy)
LEX_AUDIT__STORE_BACKEND=  # Backend type — 'sql' or 'memory' (str)
LEX_AUDIT__TABLE_NAME=  # SQL table name for the unified audit store (str)
LEX_AUDIT__VERIFICATION_BATCH_SIZE=  # Entries to verify per verification run (int)
LEX_AUDIT__VERIFICATION_SCHEDULE=  # Cron expression for scheduled verification (str)

# ── lexigram-auth ──
LEX_AUTH__ACCESS_TOKEN_EXPIRE=  # Access token expiry duration (Duration)
LEX_AUTH__ADMIN_EMAIL=  # Initial admin email (str | None)
LEX_AUTH__ADMIN_PASSWORD=<change-me-in-production>  # Initial admin password (str | None)
LEX_AUTH__ALGORITHM=  # Algorithm (str)
LEX_AUTH__ARGON2_MEMORY_COST=  # Argon2id memory cost in KiB (OWASP floor is 19456) (int)
LEX_AUTH__ARGON2_PARALLELISM=  # Argon2id parallelism (int)
LEX_AUTH__ARGON2_TIME_COST=  # Argon2id time cost (int)
LEX_AUTH__BACKEND=  # Auth backend type (str)
LEX_AUTH__BANNED_PATTERNS=  # Substrings that must not appear in the password (case-insensitive). Use to re... (list[str])
LEX_AUTH__BCRYPT_ROUNDS=  # bcrypt cost factor for new hashes (minimum 12 in production) (int)
LEX_AUTH__CACHE_PERMISSIONS=  # Cache resolved permissions (bool)
LEX_AUTH__DEFAULT_ROLE=  # Default role for new users (str)
LEX_AUTH__DESCRIPTION=  # Role description (str)
LEX_AUTH__EMAIL=  # Email address (str)
LEX_AUTH__ENABLED=  # Enable RBAC enforcement (bool)
LEX_AUTH__EXCLUDE_PATHS=  # Paths excluded from auth (list[str])
LEX_AUTH__EXCLUDE_PREFIXES=  # Path prefixes excluded (list[str])
LEX_AUTH__HEADER_NAME=  # Header name for token (str)
LEX_AUTH__ID_TOKEN_EXPIRE=  # ID token expiry duration (Duration)
LEX_AUTH__INHERITS=  # Parent roles to inherit from (list[str])
LEX_AUTH__IS_ACTIVE=  # Whether user is active (bool)
LEX_AUTH__KEY_ROTATION_GRACE_PERIOD=  # Duration during which tokens signed by a rotated-out key remain accepted. Pre... (Duration)
LEX_AUTH__LOGIN_RATE_LIMIT=  # Rate limit for auth endpoints (str)
LEX_AUTH__LOGIN_URL=  # URL to redirect for login (str | None)
LEX_AUTH__MAX_LENGTH=  # Maximum password length (int)
LEX_AUTH__MAX_SESSIONS_PER_USER=  # Maximum number of concurrent sessions allowed per user. ``None`` (the default... (int | None)
LEX_AUTH__MIDDLEWARE__BACKEND=  # Auth backend type (str)
LEX_AUTH__MIDDLEWARE__EXCLUDE_PATHS=  # Paths excluded from auth (list[str])
LEX_AUTH__MIDDLEWARE__EXCLUDE_PREFIXES=  # Path prefixes excluded (list[str])
LEX_AUTH__MIDDLEWARE__HEADER_NAME=  # Header name for token (str)
LEX_AUTH__MIDDLEWARE__LOGIN_RATE_LIMIT=  # Rate limit for auth endpoints (str)
LEX_AUTH__MIDDLEWARE__LOGIN_URL=  # URL to redirect for login (str | None)
LEX_AUTH__MIDDLEWARE__OPTIONAL_AUTH=  # Whether authentication is optional (bool)
LEX_AUTH__MIDDLEWARE__PERMISSIONS_REQUIRED=  # Permissions required (list[str])
LEX_AUTH__MIDDLEWARE__ROLES_REQUIRED=  # Roles required (list[str])
LEX_AUTH__MIDDLEWARE__SCHEME=  # Token scheme (str)
LEX_AUTH__MIN_LENGTH=  # Minimum password length (int)
LEX_AUTH__NAME=  # User name (preferred over username) (str)
LEX_AUTH__OAUTH2_PROVIDERS=  # OAuth2 configs (dict[str, dict[str, str]])
LEX_AUTH__OPTIONAL_AUTH=  # Whether authentication is optional (bool)
LEX_AUTH__PASSWORD=<change-me-in-production>  # Plain password (str | None)
LEX_AUTH__PASSWORD_HASH=  # Pre-hashed password (str | None)
LEX_AUTH__PASSWORD__ARGON2_MEMORY_COST=  # Argon2id memory cost in KiB (OWASP floor is 19456) (int)
LEX_AUTH__PASSWORD__ARGON2_PARALLELISM=  # Argon2id parallelism (int)
LEX_AUTH__PASSWORD__ARGON2_TIME_COST=  # Argon2id time cost (int)
LEX_AUTH__PASSWORD__BANNED_PATTERNS=  # Substrings that must not appear in the password (case-insensitive). Use to re... (list[str])
LEX_AUTH__PASSWORD__BCRYPT_ROUNDS=  # bcrypt cost factor for new hashes (minimum 12 in production) (int)
LEX_AUTH__PASSWORD__MAX_LENGTH=  # Maximum password length (int)
LEX_AUTH__PASSWORD__MIN_LENGTH=  # Minimum password length (int)
LEX_AUTH__PASSWORD__REQUIRE_DIGITS=  # Require at least one digit (bool)
LEX_AUTH__PASSWORD__REQUIRE_LOWERCASE=  # Require at least one lowercase letter (bool)
LEX_AUTH__PASSWORD__REQUIRE_SPECIAL=  # Require at least one special character (non-alphanumeric) (bool)
LEX_AUTH__PASSWORD__REQUIRE_UPPERCASE=  # Require at least one uppercase letter (bool)
LEX_AUTH__PERMISSIONS=  # Permission patterns (list[str])
LEX_AUTH__PERMISSIONS_REQUIRED=  # Permissions required (list[str])
LEX_AUTH__PERMISSION_CACHE_TTL=  # Permission cache TTL in seconds (int)
LEX_AUTH__RBAC__CACHE_PERMISSIONS=  # Cache resolved permissions (bool)
LEX_AUTH__RBAC__DEFAULT_ROLE=  # Default role for new users (str)
LEX_AUTH__RBAC__ENABLED=  # Enable RBAC enforcement (bool)
LEX_AUTH__RBAC__PERMISSION_CACHE_TTL=  # Permission cache TTL in seconds (int)
LEX_AUTH__RBAC__SUPERUSER_BYPASS=  # Allow superuser role to bypass all checks (bool)
LEX_AUTH__REFRESH_TOKEN_EXPIRE=  # Refresh token expiry duration (Duration)
LEX_AUTH__RELAY_VERIFICATION=  # Enable binding ``RelayAuthVerifierProtocol`` for the relay gateway's inbound ... (bool)
LEX_AUTH__REQUIRED_AUDIENCE=  # Expected ``aud`` claim for every token verified by this service. When set, to... (str | None)
LEX_AUTH__REQUIRE_DIGITS=  # Require at least one digit (bool)
LEX_AUTH__REQUIRE_LOWERCASE=  # Require at least one lowercase letter (bool)
LEX_AUTH__REQUIRE_SPECIAL=  # Require at least one special character (non-alphanumeric) (bool)
LEX_AUTH__REQUIRE_UPPERCASE=  # Require at least one uppercase letter (bool)
LEX_AUTH__ROLES=  # List of role names (list[str])
LEX_AUTH__ROLES_REQUIRED=  # Roles required (list[str])
LEX_AUTH__SCHEME=  # Token scheme (str)
LEX_AUTH__SECRET_KEY=<change-me-in-production>  # Secret key for signing tokens (SecretStr)
LEX_AUTH__SUPERUSER_BYPASS=  # Allow superuser role to bypass all checks (bool)
LEX_AUTH__TOKEN__ACCESS_TOKEN_EXPIRE=  # Access token expiry duration (Duration)
LEX_AUTH__TOKEN__ALGORITHM=  # Algorithm (str)
LEX_AUTH__TOKEN__ID_TOKEN_EXPIRE=  # ID token expiry duration (Duration)
LEX_AUTH__TOKEN__KEY_ROTATION_GRACE_PERIOD=  # Duration during which tokens signed by a rotated-out key remain accepted. Pre... (Duration)
LEX_AUTH__TOKEN__REFRESH_TOKEN_EXPIRE=  # Refresh token expiry duration (Duration)
LEX_AUTH__TOKEN__REQUIRED_AUDIENCE=  # Expected ``aud`` claim for every token verified by this service. When set, to... (str | None)
LEX_AUTH__TOKEN__SECRET_KEY=<change-me-in-production>  # Secret key for signing tokens (SecretStr)
LEX_AUTH__USERNAME=  # Legacy username (str | None)
LEX_AUTH__USERS=  # Initial users (list[AuthUserConfig])

# ── lexigram-cache ──
LEX_CACHE__BACKENDS=  # Backend configs (list[CacheBackendConfig])
LEX_CACHE__DEBUG=  # Debug mode (bool)
LEX_CACHE__ENABLED=  # Whether cache is enabled (bool)
LEX_CACHE__ENV=  # Environment (development/staging/production) (str | None)
LEX_CACHE__ENVIRONMENT=  # Environment (str)
LEX_CACHE__NAME=  # Provider name (str)
LEX_CACHE__SERVICE__CIRCUIT_BREAKER_ENABLED=  # Enable circuit breaker (bool)
LEX_CACHE__SERVICE__CIRCUIT_BREAKER_THRESHOLD=  # Circuit breaker threshold (int)
LEX_CACHE__SERVICE__DEFAULT_BACKEND=  # Default backend name (str | None)
LEX_CACHE__SERVICE__DEFAULT_SERIALIZER=  # Default serializer (str)
LEX_CACHE__SERVICE__ENABLE_HEALTH_CHECKS=  # Enable health checks (bool)
LEX_CACHE__SERVICE__ENABLE_METRICS=  # Enable metrics (bool)
LEX_CACHE__SERVICE__ENABLE_PROTECTION=  # Enable stampede protection (bool)
LEX_CACHE__SERVICE__PROTECTION_LOCK_TTL=  # Protection lock TTL (int)
LEX_CACHE__SERVICE__PROTECTION_MAX_WAIT=  # Max wait for locks (float)
LEX_CACHE__SERVICE__PROTECTION_RETRY_INTERVAL=  # Lock retry interval (float)
LEX_CACHE__VERSION=  # Config version (str)

# ── lexigram-cli ──
LEX_CLI__COLOR=  # — (bool)
LEX_CLI__DEFAULT_DATABASE=  # — (str)
LEX_CLI__DEFAULT_TEMPLATE=  # — (str)
LEX_CLI__VERBOSE=  # — (bool)
LEX_CONFIG=  # — (str)

# ── lexigram-events ──
LEX_EVENTS__ALLOW_NO_HANDLERS=  # — (bool)
LEX_EVENTS__AUTO_CREATE_INDEXES=  # — (bool)
LEX_EVENTS__AUTO_CREATE_TABLES=  # — (bool)
LEX_EVENTS__BATCH_SIZE=  # — (int)
LEX_EVENTS__BUFFER_SIZE=  # — (int)
LEX_EVENTS__CHECKPOINT_INTERVAL=  # — (int)
LEX_EVENTS__CLEANUP_COMPLETED_AFTER_HOURS=  # — (int)
LEX_EVENTS__COMMAND_BUS__ENABLE_LOGGING=  # — (bool)
LEX_EVENTS__COMMAND_BUS__ENABLE_METRICS=  # — (bool)
LEX_EVENTS__COMMAND_BUS__ENABLE_VALIDATION=  # — (bool)
LEX_EVENTS__COMMAND_BUS__MAX_RETRIES=  # — (int)
LEX_EVENTS__COMMAND_BUS__RETRY_DELAY_SECONDS=  # — (float)
LEX_EVENTS__COMMAND_BUS__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__COMMAND_TIMEOUT=  # — (float)
LEX_EVENTS__CONNECTION_STRING=  # MongoDB connection string (SecretStr)
LEX_EVENTS__CONTINUE_ON_ERROR=  # — (bool)
LEX_EVENTS__COUNTERS_COLLECTION=  # — (str)
LEX_EVENTS__DATABASE=  # — (str)
LEX_EVENTS__DATABASE_NAME=  # — (str)
LEX_EVENTS__DEBUG=  # — (bool)
LEX_EVENTS__DEFAULT_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__DSN=  # PostgreSQL connection string. Optional when a DatabaseProviderProtocol is inj... (SecretStr | None)
LEX_EVENTS__ENABLED=  # — (bool)
LEX_EVENTS__ENABLE_COMPENSATION=  # — (bool)
LEX_EVENTS__ENABLE_DEAD_LETTER=  # — (bool)
LEX_EVENTS__ENABLE_LOGGING=  # — (bool)
LEX_EVENTS__ENABLE_METRICS=  # — (bool)
LEX_EVENTS__ENABLE_PARALLEL_PROJECTIONS=  # — (bool)
LEX_EVENTS__ENABLE_SNAPSHOTS=  # — (bool)
LEX_EVENTS__ENABLE_VALIDATION=  # — (bool)
LEX_EVENTS__ENABLE_WEBSOCKET=  # — (bool)
LEX_EVENTS__ENV=  # Environment (development/staging/production) (str | None)
LEX_EVENTS__EVENTS_COLLECTION=  # — (str)
LEX_EVENTS__EVENTS_TABLE=  # — (str)
LEX_EVENTS__EVENT_BUS__ALLOW_NO_HANDLERS=  # — (bool)
LEX_EVENTS__EVENT_BUS__CONTINUE_ON_ERROR=  # — (bool)
LEX_EVENTS__EVENT_BUS__ENABLE_DEAD_LETTER=  # — (bool)
LEX_EVENTS__EVENT_BUS__HANDLER_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__EVENT_BUS__MAX_CONCURRENT_HANDLERS=  # — (int)
LEX_EVENTS__EVENT_BUS__MAX_HANDLER_RETRIES=  # — (int)
LEX_EVENTS__EVENT_BUS__MAX_QUEUE_PER_SUBSCRIBER=  # Maximum number of events queued per event type before backpressure is applied... (int)
LEX_EVENTS__EVENT_BUS__PARALLEL_DISPATCH=  # — (bool)
LEX_EVENTS__EVENT_BUS__RETRY_FAILED_HANDLERS=  # — (bool)
LEX_EVENTS__EVENT_COUNT_THRESHOLD=  # — (int)
LEX_EVENTS__EVENT_STORE_BACKEND=  # — (EventStoreBackend)
LEX_EVENTS__EXPONENTIAL_BASE=  # — (float)
LEX_EVENTS__HANDLER_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_EVENTS__INCLUDE_HISTOGRAMS=  # — (bool)
LEX_EVENTS__INCLUDE_PAYLOAD=  # — (bool)
LEX_EVENTS__INITIAL_DELAY_SECONDS=  # — (float)
LEX_EVENTS__ISOLATION_LEVEL=  # — (str)
LEX_EVENTS__JOURNAL_MODE=  # — (str)
LEX_EVENTS__KAFKA__AUTO_OFFSET_RESET=  # — (str)
LEX_EVENTS__KAFKA__BOOTSTRAP_SERVERS=  # Kafka bootstrap servers (str)
LEX_EVENTS__KAFKA__CONSUMER_GROUP=  # — (str)
LEX_EVENTS__KAFKA__ENABLE_AUTO_COMMIT=  # — (bool)
LEX_EVENTS__KAFKA__TOPIC_PREFIX=  # — (str)
LEX_EVENTS__LOGGING_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__LOGGING_MIDDLEWARE__INCLUDE_PAYLOAD=  # — (bool)
LEX_EVENTS__LOGGING_MIDDLEWARE__LOG_LEVEL=  # — (str)
LEX_EVENTS__LOGGING_MIDDLEWARE__MAX_PAYLOAD_LENGTH=  # — (int)
LEX_EVENTS__LOG_LEVEL=  # — (str)
LEX_EVENTS__MAX_CATCH_UP_EVENTS=  # — (int)
LEX_EVENTS__MAX_CONCURRENT_HANDLERS=  # — (int)
LEX_EVENTS__MAX_DELAY_SECONDS=  # — (float)
LEX_EVENTS__MAX_EVENTS_PER_STREAM=  # — (int)
LEX_EVENTS__MAX_HANDLER_RETRIES=  # — (int)
LEX_EVENTS__MAX_PAYLOAD_LENGTH=  # — (int)
LEX_EVENTS__MAX_POOL_SIZE=  # — (int)
LEX_EVENTS__MAX_QUEUE_PER_SUBSCRIBER=  # Maximum number of events queued per event type before backpressure is applied... (int)
LEX_EVENTS__MAX_RETRIES=  # — (int)
LEX_EVENTS__MAX_RETRIES_PER_STEP=  # — (int)
LEX_EVENTS__MAX_SNAPSHOTS_PER_AGGREGATE=  # — (int)
LEX_EVENTS__MAX_SUBSCRIBERS=  # — (int)
LEX_EVENTS__MEMORY__ENABLE_SNAPSHOTS=  # — (bool)
LEX_EVENTS__MEMORY__MAX_EVENTS_PER_STREAM=  # — (int)
LEX_EVENTS__METRICS_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__METRICS_MIDDLEWARE__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_EVENTS__METRICS_MIDDLEWARE__INCLUDE_HISTOGRAMS=  # — (bool)
LEX_EVENTS__METRICS_MIDDLEWARE__PREFIX=  # — (str)
LEX_EVENTS__MONGODB__CONNECTION_STRING=  # MongoDB connection string (SecretStr)
LEX_EVENTS__MONGODB__DATABASE_NAME=  # — (str)
LEX_EVENTS__MONGODB__EVENTS_COLLECTION=  # — (str)
LEX_EVENTS__MONGODB__MAX_POOL_SIZE=  # — (int)
LEX_EVENTS__MONGODB__SERVER_SELECTION_TIMEOUT=  # — (int)
LEX_EVENTS__MONGODB__SNAPSHOTS_COLLECTION=  # — (str)
LEX_EVENTS__NAME=  # — (str)
LEX_EVENTS__PARALLEL_DISPATCH=  # — (bool)
LEX_EVENTS__PERSIST_STATE=  # — (bool)
LEX_EVENTS__POLL_INTERVAL_MS=  # — (int)
LEX_EVENTS__POOL_MAX_SIZE=  # — (int)
LEX_EVENTS__POOL_MIN_SIZE=  # — (int)
LEX_EVENTS__POSTGRES__AUTO_CREATE_TABLES=  # — (bool)
LEX_EVENTS__POSTGRES__COMMAND_TIMEOUT=  # — (float)
LEX_EVENTS__POSTGRES__DSN=  # PostgreSQL connection string. Optional when a DatabaseProviderProtocol is inj... (SecretStr | None)
LEX_EVENTS__POSTGRES__EVENTS_TABLE=  # — (str)
LEX_EVENTS__POSTGRES__POOL_MAX_SIZE=  # — (int)
LEX_EVENTS__POSTGRES__POOL_MIN_SIZE=  # — (int)
LEX_EVENTS__POSTGRES__SNAPSHOTS_TABLE=  # — (str)
LEX_EVENTS__PRAGMAS=  # — (dict[str, str])
LEX_EVENTS__PREFIX=  # — (str)
LEX_EVENTS__PROJECTION__BATCH_SIZE=  # — (int)
LEX_EVENTS__PROJECTION__CHECKPOINT_INTERVAL=  # — (int)
LEX_EVENTS__PROJECTION__ENABLE_PARALLEL_PROJECTIONS=  # — (bool)
LEX_EVENTS__PROJECTION__MAX_CATCH_UP_EVENTS=  # — (int)
LEX_EVENTS__PROJECTION__REBUILD_BATCH_SIZE=  # — (int)
LEX_EVENTS__QUERY_BUS__ENABLE_LOGGING=  # — (bool)
LEX_EVENTS__QUERY_BUS__ENABLE_METRICS=  # — (bool)
LEX_EVENTS__QUERY_BUS__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__RABBITMQ__DURABLE=  # — (bool)
LEX_EVENTS__RABBITMQ__EXCHANGE_NAME=  # — (str)
LEX_EVENTS__RABBITMQ__PREFETCH_COUNT=  # — (int)
LEX_EVENTS__RABBITMQ__QUEUE_PREFIX=  # — (str)
LEX_EVENTS__RABBITMQ__URL=  # AMQP connection URL (SecretStr)
LEX_EVENTS__REBUILD_BATCH_SIZE=  # — (int)
LEX_EVENTS__RECONNECT_ATTEMPTS=  # Number of reconnection attempts (int)
LEX_EVENTS__RECONNECT_DELAY=  # Delay between reconnection attempts (float)
LEX_EVENTS__RETRY_DELAY_SECONDS=  # — (float)
LEX_EVENTS__RETRY_FAILED_HANDLERS=  # — (bool)
LEX_EVENTS__RETRY_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__RETRY_MIDDLEWARE__EXPONENTIAL_BASE=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__INITIAL_DELAY_SECONDS=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__MAX_DELAY_SECONDS=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__MAX_RETRIES=  # — (int)
LEX_EVENTS__SAGA__CLEANUP_COMPLETED_AFTER_HOURS=  # — (int)
LEX_EVENTS__SAGA__DEFAULT_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__SAGA__ENABLE_COMPENSATION=  # — (bool)
LEX_EVENTS__SAGA__MAX_RETRIES_PER_STEP=  # — (int)
LEX_EVENTS__SAGA__PERSIST_STATE=  # — (bool)
LEX_EVENTS__SAGA__RETRY_DELAY_SECONDS=  # — (float)
LEX_EVENTS__SERVER_SELECTION_TIMEOUT=  # — (int)
LEX_EVENTS__SNAPSHOTS_COLLECTION=  # — (str)
LEX_EVENTS__SNAPSHOTS_TABLE=  # — (str)
LEX_EVENTS__SNAPSHOTS__ENABLED=  # — (bool)
LEX_EVENTS__SNAPSHOTS__EVENT_COUNT_THRESHOLD=  # — (int)
LEX_EVENTS__SNAPSHOTS__MAX_SNAPSHOTS_PER_AGGREGATE=  # — (int)
LEX_EVENTS__SNAPSHOTS__STRATEGY=  # — (SnapshotStrategy)
LEX_EVENTS__SNAPSHOTS__TIME_THRESHOLD_SECONDS=  # — (int)
LEX_EVENTS__SQLITE__DATABASE=  # — (str)
LEX_EVENTS__SQLITE__JOURNAL_MODE=  # — (str)
LEX_EVENTS__SQLITE__PRAGMAS=  # — (dict[str, str])
LEX_EVENTS__SQLITE__WAL_MODE=  # — (bool)
LEX_EVENTS__STRATEGY=  # — (SnapshotStrategy)
LEX_EVENTS__STREAMING__BATCH_SIZE=  # — (int)
LEX_EVENTS__STREAMING__BUFFER_SIZE=  # — (int)
LEX_EVENTS__STREAMING__ENABLE_WEBSOCKET=  # — (bool)
LEX_EVENTS__STREAMING__MAX_SUBSCRIBERS=  # — (int)
LEX_EVENTS__STREAMING__POLL_INTERVAL_MS=  # — (int)
LEX_EVENTS__STREAMING__WEBSOCKET_PING_INTERVAL=  # — (int)
LEX_EVENTS__STRICT_MODE=  # — (bool)
LEX_EVENTS__TIMEOUT=  # Connection timeout in seconds (float)
LEX_EVENTS__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__TIME_THRESHOLD_SECONDS=  # — (int)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__ISOLATION_LEVEL=  # — (str)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__URI=  # MongoDB connection URI (SecretStr)
LEX_EVENTS__VALIDATION_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__VALIDATION_MIDDLEWARE__STRICT_MODE=  # — (bool)
LEX_EVENTS__VERSION_SKEW_ALERTS_ENABLED=  # — (bool)
LEX_EVENTS__WAL_MODE=  # — (bool)
LEX_EVENTS__WEBSOCKET_PING_INTERVAL=  # — (int)

# ── lexigram-features ──
LEX_FEATURES__CACHE_TTL=  # Seconds to cache flag evaluations (0 = disabled). (int)
LEX_FEATURES__DEFAULT_ENABLED=  # Default value when a flag is not found in the provider. (bool)
LEX_FEATURES__ENABLED=  # Enable the feature flags subsystem (bool)
LEX_FEATURES__FLAG_ENV_PREFIX=  # Env var prefix used by EnvProvider when reading flag values. (str)
LEX_FEATURES__INITIAL_FLAGS=  # Seed flags for the in-memory provider (name -> enabled). (dict[str, bool])

# ── lexigram-graph ──
LEX_GRAPH__BACKEND=  # Graph store backend to use (str)
LEX_GRAPH__BULK_BATCH_SIZE=  # Batch size for bulk operations (int)
LEX_GRAPH__DEFAULT_QUERY_LIMIT=  # Default limit for query results (int)
LEX_GRAPH__DEFAULT_TRAVERSAL_MAX_DEPTH=  # Default maximum depth for traversals (int)
LEX_GRAPH__ENABLED=  # Enable the graph store subsystem (bool)
LEX_GRAPH__MAX_RETRIES=  # Maximum number of retries for operations (int)
LEX_GRAPH__MEMORY__MAX_EDGES=  # Maximum number of edges in memory (int)
LEX_GRAPH__MEMORY__MAX_NODES=  # Maximum number of nodes in memory (int)
LEX_GRAPH__NEO4J__CONNECTION_TIMEOUT=  # Connection timeout in seconds (float)
LEX_GRAPH__NEO4J__DATABASE=  # Target database name (str)
LEX_GRAPH__NEO4J__ENCRYPTED=  # Whether to use SSL/TLS encryption (bool)
LEX_GRAPH__NEO4J__FETCH_SIZE=  # Default fetch size for results (int)
LEX_GRAPH__NEO4J__MAX_CONNECTION_POOL_SIZE=  # Maximum number of connections in the pool (int)
LEX_GRAPH__NEO4J__MAX_TRANSACTION_RETRY_TIME=  # Maximum time for transaction retries (float)
LEX_GRAPH__NEO4J__PASSWORD=<change-me-in-production>  # Neo4j password (SecretStr)
LEX_GRAPH__NEO4J__TRUST=  # Trust strategy for SSL (str)
LEX_GRAPH__NEO4J__URI=  # Neo4j BOLT URI (str)
LEX_GRAPH__NEO4J__USERNAME=  # Neo4j username (str)
LEX_GRAPH__RETRY_DELAY=  # Delay between retries in seconds (float)
LEX_GRAPH__TENANCY__ENABLED=  # Enable tenant-aware graph resolution (bool)
LEX_GRAPH__TENANCY__STRATEGY=  # Which tenancy strategy to use. One of ``"node_property"`` or ``"graph_per_ten... (str)
LEX_GRAPH__TENANCY__TEMPLATE=  # Collection name template for ``GRAPH_PER_TENANT`` strategy. Supports ``{logic... (str)

# ── lexigram-graphql ──
LEX_GRAPHQL__ALIAS_LIMIT__ENABLED=  # — (bool)
LEX_GRAPHQL__ALIAS_LIMIT__MAX_ALIASES=  # — (int)
LEX_GRAPHQL__ALLOWED_ENVIRONMENTS=  # — (set[str])
LEX_GRAPHQL__BATCH_DELAY_MS=  # Delay in milliseconds before executing a DataLoaderProtocol batch. A small no... (float)
LEX_GRAPHQL__BATCH_ENABLED=  # — (bool)
LEX_GRAPHQL__BATCH__ENABLED=  # — (bool)
LEX_GRAPHQL__BATCH__MAX_BATCH_SIZE=  # — (int)
LEX_GRAPHQL__CACHE_ENABLED=  # — (bool)
LEX_GRAPHQL__CACHE__DEFAULT_MAX_AGE=  # — (Duration | int)
LEX_GRAPHQL__CACHE__DEFAULT_SCOPE=  # — (CacheScope)
LEX_GRAPHQL__CACHE__ENABLED=  # — (bool)
LEX_GRAPHQL__CACHE__VARY_HEADERS=  # — (list[str])
LEX_GRAPHQL__COMPLEXITY__DEFAULT_FIELD_COST=  # — (float)
LEX_GRAPHQL__COMPLEXITY__DEFAULT_LIST_COST=  # — (float)
LEX_GRAPHQL__COMPLEXITY__ENABLED=  # — (bool)
LEX_GRAPHQL__COMPLEXITY__MAX_COMPLEXITY=  # — (int)
LEX_GRAPHQL__CONNECTION_TIMEOUT=  # — (Duration | int)
LEX_GRAPHQL__DATALOADER__BATCH_DELAY_MS=  # Delay in milliseconds before executing a DataLoaderProtocol batch. A small no... (float)
LEX_GRAPHQL__DATALOADER__BATCH_ENABLED=  # — (bool)
LEX_GRAPHQL__DATALOADER__CACHE_ENABLED=  # — (bool)
LEX_GRAPHQL__DATALOADER__ENABLED=  # — (bool)
LEX_GRAPHQL__DATALOADER__MAX_BATCH_SIZE=  # — (int)
LEX_GRAPHQL__DEBUG=  # — (bool)
LEX_GRAPHQL__DEBUG_MODE=  # — (bool)
LEX_GRAPHQL__DEFAULT_FIELD_COST=  # — (float)
LEX_GRAPHQL__DEFAULT_LIST_COST=  # — (float)
LEX_GRAPHQL__DEFAULT_MAX_AGE=  # — (Duration | int)
LEX_GRAPHQL__DEFAULT_SCOPE=  # — (CacheScope)
LEX_GRAPHQL__DEPTH_LIMIT__ENABLED=  # — (bool)
LEX_GRAPHQL__DEPTH_LIMIT__IGNORE_INTROSPECTION=  # — (bool)
LEX_GRAPHQL__DEPTH_LIMIT__MAX_DEPTH=  # — (int)
LEX_GRAPHQL__ENABLED=  # — (bool)
LEX_GRAPHQL__ENABLE_IDENTITY_RESOLUTION=  # — (bool)
LEX_GRAPHQL__ENV=  # Environment (development/staging/production) (str | None)
LEX_GRAPHQL__ERRORS__DEBUG_MODE=  # — (bool)
LEX_GRAPHQL__ERRORS__INCLUDE_STACKTRACE=  # — (bool)
LEX_GRAPHQL__ERRORS__LOG_ERRORS=  # — (bool)
LEX_GRAPHQL__ERRORS__MASK_ERRORS=  # — (bool)
LEX_GRAPHQL__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_GRAPHQL__IGNORE_INTROSPECTION=  # — (bool)
LEX_GRAPHQL__INCLUDE_LABELS=  # — (list[str])
LEX_GRAPHQL__INCLUDE_STACKTRACE=  # — (bool)
LEX_GRAPHQL__INTROSPECTION__ALLOWED_ENVIRONMENTS=  # — (set[str])
LEX_GRAPHQL__INTROSPECTION__ENABLED=  # — (bool)
LEX_GRAPHQL__KEEPALIVE_INTERVAL=  # — (Duration | int)
LEX_GRAPHQL__LOG_ERRORS=  # — (bool)
LEX_GRAPHQL__MASK_ERRORS=  # — (bool)
LEX_GRAPHQL__MAX_ALIASES=  # — (int)
LEX_GRAPHQL__MAX_BATCH_SIZE=  # — (int)
LEX_GRAPHQL__MAX_COMPLEXITY=  # — (int)
LEX_GRAPHQL__MAX_DEPTH=  # — (int)
LEX_GRAPHQL__METRICS__ENABLED=  # — (bool)
LEX_GRAPHQL__METRICS__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_GRAPHQL__METRICS__INCLUDE_LABELS=  # — (list[str])
LEX_GRAPHQL__METRICS__NAMESPACE=  # — (str)
LEX_GRAPHQL__NAME=  # — (str)
LEX_GRAPHQL__NAMESPACE=  # — (str)
LEX_GRAPHQL__PATH=  # — (str)
LEX_GRAPHQL__PERSISTED_QUERIES__ENABLED=  # — (bool)
LEX_GRAPHQL__PERSISTED_QUERIES__STORE_TYPE=  # — (str)
LEX_GRAPHQL__PERSISTED_QUERIES__TTL_SECONDS=  # — (Duration | int)
LEX_GRAPHQL__PLAYGROUND__ENABLED=  # — (bool)
LEX_GRAPHQL__PLAYGROUND__PATH=  # — (str)
LEX_GRAPHQL__PLAYGROUND__TITLE=  # — (str)
LEX_GRAPHQL__PROTOCOL=  # — (SubscriptionProtocol)
LEX_GRAPHQL__RATE_LIMIT=  # — (RateLimitConfig)
LEX_GRAPHQL__SAMPLE_RATE=  # — (float)
LEX_GRAPHQL__SCHEMA_BASELINE_PATH=  # Path to a GraphQL SDL (.graphql) file containing the baseline schema. When se... (str | None)
LEX_GRAPHQL__SERVICE_NAME=  # — (str)
LEX_GRAPHQL__STORE_TYPE=  # — (str)
LEX_GRAPHQL__SUBSCRIPTIONS__CONNECTION_TIMEOUT=  # — (Duration | int)
LEX_GRAPHQL__SUBSCRIPTIONS__ENABLED=  # — (bool)
LEX_GRAPHQL__SUBSCRIPTIONS__KEEPALIVE_INTERVAL=  # — (Duration | int)
LEX_GRAPHQL__SUBSCRIPTIONS__PATH=  # — (str)
LEX_GRAPHQL__SUBSCRIPTIONS__PROTOCOL=  # — (SubscriptionProtocol)
LEX_GRAPHQL__TITLE=  # — (str)
LEX_GRAPHQL__TRACE_DATALOADERS=  # — (bool)
LEX_GRAPHQL__TRACE_RESOLVERS=  # — (bool)
LEX_GRAPHQL__TRACING__ENABLED=  # — (bool)
LEX_GRAPHQL__TRACING__SAMPLE_RATE=  # — (float)
LEX_GRAPHQL__TRACING__SERVICE_NAME=  # — (str)
LEX_GRAPHQL__TRACING__TRACE_DATALOADERS=  # — (bool)
LEX_GRAPHQL__TRACING__TRACE_RESOLVERS=  # — (bool)
LEX_GRAPHQL__TTL_SECONDS=  # — (Duration | int)
LEX_GRAPHQL__VARY_HEADERS=  # — (list[str])

# ── lexigram-http ──
LEX_HTTP__COOKIE_JAR=  # — (bool)
LEX_HTTP__ENFORCE_URL_SAFETY=  # — (bool)
LEX_HTTP__FORCE_CLOSE=  # — (bool)
LEX_HTTP__MAX_CONNECTIONS=  # — (int)
LEX_HTTP__MAX_CONNECTIONS_PER_HOST=  # — (int)
LEX_HTTP__MAX_KEEPALIVE_CONNECTIONS=  # — (int)
LEX_HTTP__POOL__FORCE_CLOSE=  # — (bool)
LEX_HTTP__POOL__MAX_CONNECTIONS=  # — (int)
LEX_HTTP__POOL__MAX_CONNECTIONS_PER_HOST=  # — (int)
LEX_HTTP__POOL__MAX_KEEPALIVE_CONNECTIONS=  # — (int)
LEX_HTTP__POOL__TIMEOUT=  # — (float)
LEX_HTTP__POOL__TTL_DNS_CACHE=  # — (int)
LEX_HTTP__PROXY=  # — (str | None)
LEX_HTTP__TIMEOUT=  # — (float)
LEX_HTTP__TRUST_ENV=  # — (bool)
LEX_HTTP__TTL_DNS_CACHE=  # — (int)

# ── lexigram-monitor ──
LEX_MONITOR__ALERT_CHANNELS=  # Alert channel names for SLO violation dispatch (list[str])
LEX_MONITOR__BACKEND_TYPE=  # Monitoring backend type (BackendType)
LEX_MONITOR__BATCH_SIZE=  # Export batch size (int)
LEX_MONITOR__CHECKS=  # List of health check names to run (list[str])
LEX_MONITOR__COLLECTION_INTERVAL=  # Metrics collection interval in seconds (float)
LEX_MONITOR__COMPRESSION=  # Compression type (none, gzip) (str)
LEX_MONITOR__DEBUG=  # Enable debug mode (bool)
LEX_MONITOR__DEFAULT_LABELS=  # Default labels for all metrics (dict[str, str])
LEX_MONITOR__DSN=  # Sentry DSN; error tracking is a no-op when unset (str | None)
LEX_MONITOR__ENABLED=  # Enable metrics collection (bool)
LEX_MONITOR__ENABLE_DEFAULT_METRICS=  # Enable default process metrics (bool)
LEX_MONITOR__ENDPOINT=  # Collector endpoint URL (str | None)
LEX_MONITOR__ENV=  # Environment (development/staging/production) (str | None)
LEX_MONITOR__ENVIRONMENT=  # Deployment environment (Environment)
LEX_MONITOR__ERROR_TRACKING__DSN=  # Sentry DSN; error tracking is a no-op when unset (str | None)
LEX_MONITOR__ERROR_TRACKING__ENVIRONMENT=  # Environment tag for captured events (str | None)
LEX_MONITOR__ERROR_TRACKING__SEND_DEFAULT_PII=  # Send default PII fields to the error tracker (bool)
LEX_MONITOR__ERROR_TRACKING__TRACES_SAMPLE_RATE=  # Traces sample rate (0.0 to 1.0) (float)
LEX_MONITOR__EVALUATION_INTERVAL=  # SLO evaluation interval in seconds (float)
LEX_MONITOR__EXPORT_INTERVAL=  # Export interval seconds (float)
LEX_MONITOR__FORMAT=  # Log format (json, text) (str)
LEX_MONITOR__HEADERS=  # Custom headers for OTLP (dict[str, str])
LEX_MONITOR__HEALTH__CHECKS=  # List of health check names to run (list[str])
LEX_MONITOR__HEALTH__ENABLED=  # Enable health checks (bool)
LEX_MONITOR__HEALTH__INCLUDE_DETAILS=  # Include detailed health info in response (bool)
LEX_MONITOR__HEALTH__INTERVAL=  # Health check interval in seconds (int)
LEX_MONITOR__HEALTH__PATH=  # Health endpoint path (str)
LEX_MONITOR__HEALTH__TIMEOUT=  # Health check timeout in seconds (float)
LEX_MONITOR__HISTOGRAM_BUCKETS=  # Default histogram bucket boundaries (list[float])
LEX_MONITOR__INCLUDE_DETAILS=  # Include detailed health info in response (bool)
LEX_MONITOR__INCLUDE_TRACE_CONTEXT=  # Include trace context in logs (bool)
LEX_MONITOR__INSECURE=  # Use insecure connection (bool)
LEX_MONITOR__INTERVAL=  # Health check interval in seconds (int)
LEX_MONITOR__LEVEL=  # Default log level (str)
LEX_MONITOR__LOGGING__ENABLED=  # Enable structured logging (bool)
LEX_MONITOR__LOGGING__FORMAT=  # Log format (json, text) (str)
LEX_MONITOR__LOGGING__INCLUDE_TRACE_CONTEXT=  # Include trace context in logs (bool)
LEX_MONITOR__LOGGING__LEVEL=  # Default log level (str)
LEX_MONITOR__LOGGING__REDACT_FIELDS=  # Fields to redact from logs (list[str])
LEX_MONITOR__MAX_ATTRIBUTES=  # Max attributes per span (int)
LEX_MONITOR__MAX_EVENTS=  # Max events per span (int)
LEX_MONITOR__MAX_LINKS=  # Max links per span (int)
LEX_MONITOR__MAX_SPANS=  # Max number of spans to keep in memory (int)
LEX_MONITOR__MAX_TRACES_PER_SECOND=  # Max traces to sample per second (int)
LEX_MONITOR__METRICS_EXPORTERS=  # List of metrics exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__METRICS_TABLE=  # Table name for metrics samples (str)
LEX_MONITOR__METRICS__COLLECTION_INTERVAL=  # Metrics collection interval in seconds (float)
LEX_MONITOR__METRICS__DEFAULT_LABELS=  # Default labels for all metrics (dict[str, str])
LEX_MONITOR__METRICS__ENABLED=  # Enable metrics collection (bool)
LEX_MONITOR__METRICS__HISTOGRAM_BUCKETS=  # Default histogram bucket boundaries (list[float])
LEX_MONITOR__METRICS__PREFIX=  # MetricProtocol name prefix (str)
LEX_MONITOR__NAME=  # Provider name (str)
LEX_MONITOR__OPENTELEMETRY__BATCH_SIZE=  # Export batch size (int)
LEX_MONITOR__OPENTELEMETRY__COMPRESSION=  # Compression type (none, gzip) (str)
LEX_MONITOR__OPENTELEMETRY__ENDPOINT=  # OTLP endpoint URL (str | None)
LEX_MONITOR__OPENTELEMETRY__EXPORT_INTERVAL=  # Export interval seconds (float)
LEX_MONITOR__OPENTELEMETRY__HEADERS=  # OTLP request headers (dict[str, str])
LEX_MONITOR__OPENTELEMETRY__INSECURE=  # Use insecure connection (bool)
LEX_MONITOR__OPENTELEMETRY__METRICS_EXPORTERS=  # List of metrics exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__OPENTELEMETRY__TIMEOUT=  # Export timeout seconds (float)
LEX_MONITOR__OPENTELEMETRY__TRACING_EXPORTERS=  # List of tracing exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__PATH=  # Health endpoint path (str)
LEX_MONITOR__PORT=  # Metrics server port (int)
LEX_MONITOR__PREFIX=  # MetricProtocol name prefix (str)
LEX_MONITOR__PROMETHEUS__ENABLE_DEFAULT_METRICS=  # Enable default process metrics (bool)
LEX_MONITOR__PROMETHEUS__METRICS_TABLE=  # Table name for metrics samples (str)
LEX_MONITOR__PROMETHEUS__PATH=  # Metrics endpoint path (str)
LEX_MONITOR__PROMETHEUS__PORT=  # Metrics server port (int)
LEX_MONITOR__PROMETHEUS__PUSHGATEWAY_URL=  # Pushgateway URL for push-based metrics (str | None)
LEX_MONITOR__PROMETHEUS__PUSH_INTERVAL=  # Push interval for Pushgateway (float)
LEX_MONITOR__PROMETHEUS__STORE_IN_DB=  # Persist metrics observations to DB (bool)
LEX_MONITOR__PROPAGATION_FORMATS=  # Propagation format list (list[str])
LEX_MONITOR__PUSHGATEWAY_URL=  # Pushgateway URL for push-based metrics (str | None)
LEX_MONITOR__PUSH_INTERVAL=  # Push interval for Pushgateway (float)
LEX_MONITOR__REDACT_FIELDS=  # Fields to redact from logs (list[str])
LEX_MONITOR__SAMPLER_TYPE=  # Tracing sampler type (SamplerType)
LEX_MONITOR__SAMPLE_RATE=  # Sample rate (0.0 to 1.0) (float)
LEX_MONITOR__SEND_DEFAULT_PII=  # Send default PII fields to the error tracker (bool)
LEX_MONITOR__SERVICE_NAME=  # Service name for traces (str)
LEX_MONITOR__SLO__ALERT_CHANNELS=  # Alert channel names for SLO violation dispatch (list[str])
LEX_MONITOR__SLO__ENABLED=  # Enable periodic SLO evaluation worker (bool)
LEX_MONITOR__SLO__EVALUATION_INTERVAL=  # SLO evaluation interval in seconds (float)
LEX_MONITOR__SLO__SUPPRESSION_WINDOW_SECONDS=  # Alert suppression window in seconds (int)
LEX_MONITOR__STORE_IN_DB=  # Persist metrics observations to DB (bool)
LEX_MONITOR__SUPPRESSION_WINDOW_SECONDS=  # Alert suppression window in seconds (int)
LEX_MONITOR__TIMEOUT=  # Health check timeout in seconds (float)
LEX_MONITOR__TRACES_SAMPLE_RATE=  # Traces sample rate (0.0 to 1.0) (float)
LEX_MONITOR__TRACING_EXPORTERS=  # List of tracing exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__TRACING__ENABLED=  # Enable tracing (bool)
LEX_MONITOR__TRACING__MAX_ATTRIBUTES=  # Max attributes per span (int)
LEX_MONITOR__TRACING__MAX_EVENTS=  # Max events per span (int)
LEX_MONITOR__TRACING__MAX_LINKS=  # Max links per span (int)
LEX_MONITOR__TRACING__MAX_SPANS=  # Max number of spans to keep in memory (int)
LEX_MONITOR__TRACING__MAX_TRACES_PER_SECOND=  # Max traces to sample per second (int)
LEX_MONITOR__TRACING__PROPAGATION_FORMATS=  # Propagation format list (list[str])
LEX_MONITOR__TRACING__SAMPLER_TYPE=  # Tracing sampler type (SamplerType)
LEX_MONITOR__TRACING__SAMPLE_RATE=  # Sample rate (0.0 to 1.0) (float)
LEX_MONITOR__TRACING__SERVICE_NAME=  # Service name for traces (str)
LEX_MONITOR__TYPE=  # Exporter type (console, jaeger, otlp) (str)

# ── lexigram-multimedia ──
LEX_MULTIMEDIA__BEAT=  # — (BeatAnalysisConfig)
LEX_MULTIMEDIA__CACHE_RESULTS=  # — (bool)
LEX_MULTIMEDIA__IMAGE=  # — (ImageConfig)
LEX_MULTIMEDIA__INTERPOLATE=  # — (InterpolationConfig)
LEX_MULTIMEDIA__MUSIC=  # — (MusicConfig)
LEX_MULTIMEDIA__STORAGE_PATH_PREFIX=  # — (str)
LEX_MULTIMEDIA__TTS=  # — (TTSConfig)
LEX_MULTIMEDIA__UPSCALE=  # — (UpscaleConfig)
LEX_MULTIMEDIA__VIDEO=  # — (VideoConfig)

# ── lexigram-multimedia-beat ──
LEX_MULTIMEDIA_BEAT__BACKEND=  # — (Literal['librosa', 'madmom'])
LEX_MULTIMEDIA_BEAT__LIBROSA_SAMPLE_RATE=  # — (int)
LEX_MULTIMEDIA_BEAT__MADMOM_BASE_URL=  # — (str)
LEX_MULTIMEDIA_BEAT__MAX_ANALYZE_SAMPLES=  # — (int)
LEX_MULTIMEDIA_BEAT__MAX_ASSET_BYTES=  # — (int)
LEX_MULTIMEDIA_BEAT__TIMEOUT=  # — (float)

# ── lexigram-multimedia-image ──
LEX_MULTIMEDIA_IMAGE__BACKEND=  # — (Literal['local-http', 'stability', 'openai', 'comfyui'])
LEX_MULTIMEDIA_IMAGE__COMFYUI_BASE_URL=  # — (str)
LEX_MULTIMEDIA_IMAGE__COMFYUI_CFG_SCALE=  # — (float)
LEX_MULTIMEDIA_IMAGE__COMFYUI_CHECKPOINT=  # — (str)
LEX_MULTIMEDIA_IMAGE__COMFYUI_POLL_INTERVAL=  # — (float)
LEX_MULTIMEDIA_IMAGE__COMFYUI_STEPS=  # — (int)
LEX_MULTIMEDIA_IMAGE__COMFYUI_WORKFLOW_PATH=  # — (str | None)
LEX_MULTIMEDIA_IMAGE__LOCAL_HTTP_BASE_URL=  # — (str)
LEX_MULTIMEDIA_IMAGE__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_IMAGE__OPENAI_BASE_URL=  # — (str)
LEX_MULTIMEDIA_IMAGE__OPENAI_MODEL=  # — (str)
LEX_MULTIMEDIA_IMAGE__STABILITY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_IMAGE__TIMEOUT=  # — (float)

# ── lexigram-multimedia-interpolate ──
LEX_MULTIMEDIA_INTERPOLATE__BACKEND=  # — (Literal['rife'])
LEX_MULTIMEDIA_INTERPOLATE__RIFE_BASE_URL=  # — (str)
LEX_MULTIMEDIA_INTERPOLATE__TIMEOUT=  # — (float)

# ── lexigram-multimedia-music ──
LEX_MULTIMEDIA_MUSIC__ACE_STEP_BASE_URL=  # — (str)
LEX_MULTIMEDIA_MUSIC__BACKEND=  # — (Literal['local-http', 'stability-audio', 'ace-step', 'stable-audio-open'])
LEX_MULTIMEDIA_MUSIC__LOCAL_HTTP_BASE_URL=  # — (str)
LEX_MULTIMEDIA_MUSIC__STABILITY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_MUSIC__STABLE_AUDIO_OPEN_BASE_URL=  # — (str)
LEX_MULTIMEDIA_MUSIC__TIMEOUT=  # — (float)

# ── lexigram-multimedia-tts ──
LEX_MULTIMEDIA_TTS__BACKEND=  # — (Literal['local-http', 'elevenlabs', 'openai', 'chatterbox', 'kokoro', 'f5-tts', 'piper'])
LEX_MULTIMEDIA_TTS__CHATTERBOX_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__CHATTERBOX_CFG_WEIGHT=  # — (float)
LEX_MULTIMEDIA_TTS__CHATTERBOX_EXAGGERATION=  # — (float)
LEX_MULTIMEDIA_TTS__CHATTERBOX_TEMPERATURE=  # — (float)
LEX_MULTIMEDIA_TTS__ELEVENLABS_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_TTS__ELEVENLABS_VOICE_ID=  # — (str | None)
LEX_MULTIMEDIA_TTS__F5_TTS_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__KOKORO_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__KOKORO_DEFAULT_VOICE=  # — (str)
LEX_MULTIMEDIA_TTS__LOCAL_HTTP_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_TTS__OPENAI_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__OPENAI_MODEL=  # — (str)
LEX_MULTIMEDIA_TTS__OPENAI_VOICE=  # — (str)
LEX_MULTIMEDIA_TTS__PIPER_BASE_URL=  # — (str)
LEX_MULTIMEDIA_TTS__PIPER_DEFAULT_VOICE=  # — (str)
LEX_MULTIMEDIA_TTS__TIMEOUT=  # — (float)

# ── lexigram-multimedia-upscale ──
LEX_MULTIMEDIA_UPSCALE__BACKEND=  # — (Literal['real-esrgan', 'hat'])
LEX_MULTIMEDIA_UPSCALE__HAT_BASE_URL=  # — (str)
LEX_MULTIMEDIA_UPSCALE__REAL_ESRGAN_BASE_URL=  # — (str)
LEX_MULTIMEDIA_UPSCALE__TIMEOUT=  # — (float)

# ── lexigram-multimedia-video ──
LEX_MULTIMEDIA_VIDEO__BACKEND=  # — (Literal['local-http', 'runway', 'openai', 'wan22', 'cogvideox', 'svd', 'comfyui'])
LEX_MULTIMEDIA_VIDEO__COGVIDEOX_BASE_URL=  # — (str)
LEX_MULTIMEDIA_VIDEO__COMFYUI_BASE_URL=  # — (str)
LEX_MULTIMEDIA_VIDEO__COMFYUI_CHECKPOINT=  # — (str)
LEX_MULTIMEDIA_VIDEO__COMFYUI_FPS=  # — (int)
LEX_MULTIMEDIA_VIDEO__COMFYUI_MOTION_BUCKET_ID=  # — (int)
LEX_MULTIMEDIA_VIDEO__COMFYUI_POLL_INTERVAL=  # — (float)
LEX_MULTIMEDIA_VIDEO__COMFYUI_WORKFLOW_PATH=  # — (str | None)
LEX_MULTIMEDIA_VIDEO__FFMPEG_BINARY=  # — (str)
LEX_MULTIMEDIA_VIDEO__LOCAL_HTTP_BASE_URL=  # — (str)
LEX_MULTIMEDIA_VIDEO__MAX_ASSET_BYTES=  # — (int)
LEX_MULTIMEDIA_VIDEO__MAX_CONCURRENT_JOBS=  # — (int)
LEX_MULTIMEDIA_VIDEO__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_VIDEO__OPENAI_BASE_URL=  # — (str)
LEX_MULTIMEDIA_VIDEO__OPENAI_MODEL=  # — (str)
LEX_MULTIMEDIA_VIDEO__PROCESSING__FFMPEG_BINARY=  # — (str)
LEX_MULTIMEDIA_VIDEO__PROCESSING__MAX_ASSET_BYTES=  # — (int)
LEX_MULTIMEDIA_VIDEO__PROCESSING__MAX_CONCURRENT_JOBS=  # — (int)
LEX_MULTIMEDIA_VIDEO__PROCESSING__TEMP_DIR=  # — (str | None)
LEX_MULTIMEDIA_VIDEO__PROCESSING__TIMEOUT=  # — (float)
LEX_MULTIMEDIA_VIDEO__RUNWAY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA_VIDEO__SVD_BASE_URL=  # — (str)
LEX_MULTIMEDIA_VIDEO__TEMP_DIR=  # — (str | None)
LEX_MULTIMEDIA_VIDEO__TIMEOUT=  # — (float)
LEX_MULTIMEDIA_VIDEO__WAN22_BASE_URL=  # — (str)

# ── lexigram-nosql ──
LEX_NOSQL__BACKENDS=  # Named NoSQL backends for multi-store support. When non-empty, the provider re... (list[NamedNoSQLConfig])
LEX_NOSQL__DRIVER=  # NoSQL driver name (str)
LEX_NOSQL__ENABLED=  # Enable NoSQL support (bool)
LEX_NOSQL__FIRESTORE__CREDENTIALS_JSON=  # Path to a service account JSON key file, or the raw JSON string. When ``None`... (str | None)
LEX_NOSQL__FIRESTORE__DATABASE_ID=  # Firestore database ID (use '(default)' for the default database) (str)
LEX_NOSQL__FIRESTORE__PROJECT_ID=  # Google Cloud project ID (str)
LEX_NOSQL__MONGODB__AUTH_SOURCE=  # Authentication database (str)
LEX_NOSQL__MONGODB__CONNECT_TIMEOUT_MS=  # Connection timeout (ms) (int)
LEX_NOSQL__MONGODB__DATABASE=  # Database name (str)
LEX_NOSQL__MONGODB__MAX_POOL_SIZE=  # Maximum connection pool size (int)
LEX_NOSQL__MONGODB__MIN_POOL_SIZE=  # Minimum connection pool size (int)
LEX_NOSQL__MONGODB__READ_PREFERENCE=  # Read preference mode (str)
LEX_NOSQL__MONGODB__RETRY_READS=  # Enable read retries (bool)
LEX_NOSQL__MONGODB__RETRY_WRITES=  # Enable write retries (bool)
LEX_NOSQL__MONGODB__SERVER_SELECTION_TIMEOUT_MS=  # Server selection timeout (ms) (int)
LEX_NOSQL__MONGODB__SOCKET_TIMEOUT_MS=  # Socket timeout (ms) (int)
LEX_NOSQL__MONGODB__URI=  # MongoDB connection URI (str)
LEX_NOSQL__MONGODB__WRITE_CONCERN_W=  # Write concern level (str | int)

# ── lexigram-notification ──
LEX_NOTIFICATION__BACKENDS=  # Named mailer backends for multi-backend support. When non-empty, the provider... (list[NamedMailerConfig])
LEX_NOTIFICATION__CONSOLE_FALLBACK=  # When no backends are configured, bind a ConsoleMailer as the default MailerPr... (bool)
LEX_NOTIFICATION__MARK_READ_ON_FETCH=  # Automatically mark messages as read when fetched. (bool)
LEX_NOTIFICATION__MAX_PAGE_SIZE=  # Maximum messages returned per page. (int)
LEX_NOTIFICATION__PUSH_BACKENDS=  # Named push notification backends for multi-backend support. When non-empty, t... (list[NamedPushConfig])
LEX_NOTIFICATION__RETENTION_DAYS=  # Days to retain inbox messages before pruning. (int)
LEX_NOTIFICATION__SMS_BACKENDS=  # Named SMS backends for multi-backend support. When non-empty, the provider re... (list[NamedSMSConfig])
LEX_NOTIFICATION__STORE_BACKEND=  # Storage backend. One of 'database' or 'memory'. (str)

# ── lexigram-queue ──
LEX_QUEUE__BACKENDS=  # Named queue backends for multi-backend support. When non-empty, the provider ... (list[NamedQueueConfig])

# ── lexigram-resilience ──
LEX_RESILIENCE__AUTO_CLEANUP=  # Start background cleanup task on init. (bool)
LEX_RESILIENCE__BULKHEAD__MAX_CONCURRENT=  # Max concurrent requests (int)
LEX_RESILIENCE__BULKHEAD__NAME=  # Bulkhead name (str)
LEX_RESILIENCE__BULKHEAD__QUEUE_SIZE=  # Max queue size (int)
LEX_RESILIENCE__BULKHEAD__TIMEOUT=  # Execution timeout (float)
LEX_RESILIENCE__CIRCUIT_BREAKER=  # — (CircuitBreakerConfig)
LEX_RESILIENCE__CLEANUP_INTERVAL=  # Seconds between background cleanup sweeps. (float)
LEX_RESILIENCE__KEY_PREFIX=  # Prefix for all keys in backing stores. (str)
LEX_RESILIENCE__MAX_ENTRIES=  # Maximum in-memory entries before FIFO eviction. (int)
LEX_RESILIENCE__MAX_KEY_LENGTH=  # Maximum allowed idempotency key length. (int)
LEX_RESILIENCE__RETRY=  # — (RetryConfig)
LEX_RESILIENCE__TIMEOUT=  # — (TimeoutConfig)
LEX_RESILIENCE__TTL=  # TTL for cached results in seconds. (int)

# ── lexigram-search ──
LEX_SEARCH__API_KEY=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__API_URL=  # — (str | None)
LEX_SEARCH__AUTO_CREATE_TABLES=  # — (bool)
LEX_SEARCH__BACKENDS=  # Named search backends for multi-backend support. When non-empty, the provider... (list[NamedSearchConfig])
LEX_SEARCH__BACKEND_TYPE=  # Search backend type (BackendType)
LEX_SEARCH__BULK_CHUNK_SIZE=  # Bulk request chunk size (int)
LEX_SEARCH__CONNECTION_STRING=  # PostgreSQL connection string (SecretStr)
LEX_SEARCH__CONNECTION_TIMEOUT=  # Connection timeout (int)
LEX_SEARCH__DATABASE=  # Named database to use for DB-backed backends (postgres/mysql). References a n... (str | None)
LEX_SEARCH__DATABASE_NAME=  # — (str)
LEX_SEARCH__DB_PATH=  # — (str)
LEX_SEARCH__DEFAULT_LIMIT=  # — (int)
LEX_SEARCH__DISPLAYED_ATTRIBUTES=  # Fields to return in results (list[str])
LEX_SEARCH__ELASTICSEARCH__API_KEY=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__ELASTICSEARCH__HOSTS=  # Elasticsearch hosts (list[str])
LEX_SEARCH__ELASTICSEARCH__INDEX_PREFIX=  # — (str)
LEX_SEARCH__ELASTICSEARCH__NUMBER_OF_REPLICAS=  # — (int)
LEX_SEARCH__ELASTICSEARCH__NUMBER_OF_SHARDS=  # — (int)
LEX_SEARCH__ELASTICSEARCH__PASSWORD=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__ELASTICSEARCH__USERNAME=  # — (str | None)
LEX_SEARCH__ELASTICSEARCH__USE_SSL=  # — (bool)
LEX_SEARCH__ELASTICSEARCH__VERIFY_CERTS=  # — (bool)
LEX_SEARCH__ENABLED=  # Enable the search subsystem (bool)
LEX_SEARCH__ENABLE_AGGREGATIONS=  # — (bool)
LEX_SEARCH__ENABLE_FACETING=  # — (bool)
LEX_SEARCH__ENABLE_HIGHLIGHTING=  # — (bool)
LEX_SEARCH__ENABLE_TRIGRAM=  # Enable pg_trgm fuzzy matching (bool)
LEX_SEARCH__FILTERABLE_ATTRIBUTES=  # Attributes that can be filtered (list[str])
LEX_SEARCH__FULLTEXT_MODE=  # — (str)
LEX_SEARCH__FUZZY_THRESHOLD=  # — (float)
LEX_SEARCH__HEALTH_CHECK_INTERVAL=  # Health check interval (int)
LEX_SEARCH__HOSTS=  # Elasticsearch hosts (list[str])
LEX_SEARCH__INDEX_PREFIX=  # — (str)
LEX_SEARCH__MAX_CONNECTIONS=  # Maximum number of connections (int)
LEX_SEARCH__MAX_LIMIT=  # — (int)
LEX_SEARCH__MAX_RETRIES=  # Max retry attempts (int)
LEX_SEARCH__MEILISEARCH__API_KEY=<change-me-in-production>  # MeiliSearch API key (SecretStr | None)
LEX_SEARCH__MEILISEARCH__DISPLAYED_ATTRIBUTES=  # Fields to return in results (list[str])
LEX_SEARCH__MEILISEARCH__FILTERABLE_ATTRIBUTES=  # Attributes that can be filtered (list[str])
LEX_SEARCH__MEILISEARCH__MAX_CONNECTIONS=  # Maximum number of connections (int)
LEX_SEARCH__MEILISEARCH__MIN_WORD_SIZE_FOR_TYPOS=  # Minimum word size for typo tolerance (dict[str, int])
LEX_SEARCH__MEILISEARCH__RANKING_RULES=  # Ranking rules in order (list[str])
LEX_SEARCH__MEILISEARCH__SEARCHABLE_ATTRIBUTES=  # Fields to search in (list[str])
LEX_SEARCH__MEILISEARCH__SORTABLE_ATTRIBUTES=  # Attributes that can be sorted (list[str])
LEX_SEARCH__MEILISEARCH__TIMEOUT=  # Request timeout in seconds (int)
LEX_SEARCH__MEILISEARCH__TYPO_TOLERANCE_ENABLED=  # Enable typo tolerance (bool)
LEX_SEARCH__MEILISEARCH__URL=  # MeiliSearch server URL (str)
LEX_SEARCH__MIN_WORD_LENGTH=  # — (int)
LEX_SEARCH__MIN_WORD_SIZE_FOR_TYPOS=  # Minimum word size for typo tolerance (dict[str, int])
LEX_SEARCH__MONGO__CONNECTION_STRING=  # — (SecretStr)
LEX_SEARCH__MONGO__DATABASE_NAME=  # — (str)
LEX_SEARCH__MONGO__USE_ATLAS_SEARCH=  # — (bool)
LEX_SEARCH__MYSQL__CONNECTION_STRING=  # — (SecretStr)
LEX_SEARCH__MYSQL__FULLTEXT_MODE=  # — (str)
LEX_SEARCH__MYSQL__MIN_WORD_LENGTH=  # — (int)
LEX_SEARCH__NODES=  # Typesense node connections (list[dict[str, str]])
LEX_SEARCH__NUMBER_OF_REPLICAS=  # — (int)
LEX_SEARCH__NUMBER_OF_SHARDS=  # — (int)
LEX_SEARCH__OPENSEARCH__HOSTS=  # OpenSearch hosts (list[str])
LEX_SEARCH__OPENSEARCH__INDEX_PREFIX=  # — (str)
LEX_SEARCH__OPENSEARCH__PASSWORD=<change-me-in-production>  # — (str | None)
LEX_SEARCH__OPENSEARCH__TIMEOUT=  # — (int)
LEX_SEARCH__OPENSEARCH__USERNAME=  # — (str | None)
LEX_SEARCH__OPENSEARCH__USE_SSL=  # — (bool)
LEX_SEARCH__OPENSEARCH__VERIFY_SSL=  # — (bool)
LEX_SEARCH__OPERATIONS__BULK_CHUNK_SIZE=  # Bulk request chunk size (int)
LEX_SEARCH__OPERATIONS__MAX_RETRIES=  # Max retry attempts (int)
LEX_SEARCH__OPERATIONS__REQUEST_TIMEOUT=  # Request timeout seconds (float)
LEX_SEARCH__OPERATIONS__RETRY_BACKOFF=  # Retry backoff multiplier (float)
LEX_SEARCH__PASSWORD=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__POSTGRES__AUTO_CREATE_TABLES=  # — (bool)
LEX_SEARCH__POSTGRES__CONNECTION_STRING=  # PostgreSQL connection string (SecretStr)
LEX_SEARCH__POSTGRES__ENABLE_TRIGRAM=  # Enable pg_trgm fuzzy matching (bool)
LEX_SEARCH__POSTGRES__TEXT_SEARCH_CONFIG=  # PostgreSQL text search config (str)
LEX_SEARCH__QUERY__DEFAULT_LIMIT=  # — (int)
LEX_SEARCH__QUERY__ENABLE_AGGREGATIONS=  # — (bool)
LEX_SEARCH__QUERY__ENABLE_FACETING=  # — (bool)
LEX_SEARCH__QUERY__ENABLE_HIGHLIGHTING=  # — (bool)
LEX_SEARCH__QUERY__FUZZY_THRESHOLD=  # — (float)
LEX_SEARCH__QUERY__MAX_LIMIT=  # — (int)
LEX_SEARCH__QUERY__STRATEGY=  # — (str)
LEX_SEARCH__RANKING_RULES=  # Ranking rules in order (list[str])
LEX_SEARCH__REQUEST_TIMEOUT=  # Request timeout seconds (float)
LEX_SEARCH__RETRY_BACKOFF=  # Retry backoff multiplier (float)
LEX_SEARCH__SEARCHABLE_ATTRIBUTES=  # Fields to search in (list[str])
LEX_SEARCH__SORTABLE_ATTRIBUTES=  # Attributes that can be sorted (list[str])
LEX_SEARCH__SQLITE__AUTO_CREATE_TABLES=  # — (bool)
LEX_SEARCH__SQLITE__DB_PATH=  # — (str)
LEX_SEARCH__SQLITE__TOKENIZER=  # — (str)
LEX_SEARCH__STRATEGY=  # — (str)
LEX_SEARCH__TEXT_SEARCH_CONFIG=  # PostgreSQL text search config (str)
LEX_SEARCH__TIMEOUT=  # — (int)
LEX_SEARCH__TOKENIZER=  # — (str)
LEX_SEARCH__TYPESENSE__API_KEY=<change-me-in-production>  # Typesense API key (SecretStr | None)
LEX_SEARCH__TYPESENSE__CONNECTION_TIMEOUT=  # Connection timeout (int)
LEX_SEARCH__TYPESENSE__HEALTH_CHECK_INTERVAL=  # Health check interval (int)
LEX_SEARCH__TYPESENSE__NODES=  # Typesense node connections (list[dict[str, str]])
LEX_SEARCH__TYPO_TOLERANCE_ENABLED=  # Enable typo tolerance (bool)
LEX_SEARCH__URL=  # MeiliSearch server URL (str)
LEX_SEARCH__USERNAME=  # — (str | None)
LEX_SEARCH__USE_ATLAS_SEARCH=  # — (bool)
LEX_SEARCH__USE_SSL=  # — (bool)
LEX_SEARCH__VERIFY_CERTS=  # — (bool)
LEX_SEARCH__VERIFY_SSL=  # — (bool)

# ── lexigram-secrets ──
LEX_SECRETS__AUDIT_ACTOR_ID=<change-me-in-production>  # Actor identifier for audit log entries (str)
LEX_SECRETS__BACKEND_OPTIONS=<change-me-in-production>  # Keyword arguments for backend constructor (dict)
LEX_SECRETS__BACKEND_TYPE=<change-me-in-production>  # Backend store type (memory, vault, ...) (str)
LEX_SECRETS__ENABLED=<change-me-in-production>  # Whether secrets subsystem is enabled (bool)
LEX_SECRETS__MAX_AGE_SECONDS=<change-me-in-production>  # Seconds before automatic rotation (float)
LEX_SECRETS__NAME=<change-me-in-production>  # Configuration name (str)
LEX_SECRETS__TENANT_ID=<change-me-in-production>  # Optional tenant namespace (str | None)
LEX_SECRETS__WARNING_BEFORE_SECONDS=<change-me-in-production>  # Seconds before expiry to warn (float)

# ── lexigram-sql ──
LEX_SQL__AUDIT_HMAC_KEY=change-me-in-production  # HMAC key for audit checksum signing. Plain text or base64. (str | None)
LEX_SQL__BACKENDS=  # Multi-database backends list. When non-empty, drives multi-DB mode. The entry... (list[NamedDatabaseConfig])
LEX_SQL__BACKEND__URL=postgresql://lexigram:lexigram@localhost:5432/lexigram  # Database connection URL (may contain credentials) (SecretStr)
LEX_SQL__DEFAULT_CURSOR_SIZE=  # — (int)
LEX_SQL__DEFAULT_PAGE_SIZE=  # — (int)
LEX_SQL__ENABLED=  # — (bool)
LEX_SQL__MAX_PAGE_SIZE=  # — (int)
LEX_SQL__MIGRATIONS__LOCK_TIMEOUT=  # — (Duration)
LEX_SQL__NAME=  # — (str)
LEX_SQL__OPERATIONS__ECHO=  # — (bool)
LEX_SQL__OPERATIONS__STATEMENT_TIMEOUT=  # — (Duration)
LEX_SQL__OUTBOX__BATCH_MAX_AGE=  # — (Duration)
LEX_SQL__OUTBOX__ENABLED=  # — (bool)
LEX_SQL__OUTBOX__POLL_INTERVAL=  # — (Duration)
LEX_SQL__POOL__ACQUIRE_TIMEOUT=  # — (Duration)
LEX_SQL__POOL__IDLE_TIMEOUT=  # — (Duration)
LEX_SQL__POOL__MAX_LIFETIME=  # — (Duration)
LEX_SQL__POOL__MAX_OVERFLOW=  # — (int)
LEX_SQL__POOL__MAX_SIZE=  # — (int)
LEX_SQL__POOL__MIN_SIZE=  # — (int)
LEX_SQL__POOL__RECYCLE=  # — (int)
LEX_SQL__POOL__TIMEOUT=  # — (float)

# ── lexigram-storage ──
LEX_STORAGE__ACCESS_KEY=  # AWS access key (SecretStr | None)
LEX_STORAGE__ACCOUNT_KEY=  # Azure storage account key (SecretStr)
LEX_STORAGE__ACCOUNT_NAME=  # Azure storage account name (str)
LEX_STORAGE__ALLOWED_MIME_TYPES=  # Allowed MIME types for upload validation. Defaults to a safe set of common im... (list[str])
LEX_STORAGE__BACKENDS=  # Named storage backends for multi-store support. When non-empty, the provider ... (list[NamedStorageConfig])
LEX_STORAGE__BASE_URL=  # Base URL for file access (str)
LEX_STORAGE__BUCKET=  # S3 bucket name (str)
LEX_STORAGE__CONTAINER=  # Blob container name (str)
LEX_STORAGE__CREDENTIALS_PATH=  # Path to service account credentials (str | None)
LEX_STORAGE__DEFAULT_DRIVER=  # Default storage driver to use (Literal['local', 's3', 'gcs', 'azure', 'memory', 'r2'])
LEX_STORAGE__DRIVERS=  # Driver-specific configurations (dict[str, StorageLocalConfig | StorageS3Config | StorageGCSConfig | StorageAzureConfig | StorageMemoryConfig | StorageR2Config])
LEX_STORAGE__ENABLED=  # Enable server-side encryption (bool)
LEX_STORAGE__ENCRYPTION__ENABLED=  # Enable server-side encryption (bool)
LEX_STORAGE__ENCRYPTION__KMS_KEY_ID=  # KMS/CMEK key ARN or ID (required for aws:kms and gcs:cmek types) (str | None)
LEX_STORAGE__ENCRYPTION__TYPE=  # Encryption type (Literal['AES256', 'aws:kms', 'gcs:cmek'])
LEX_STORAGE__ENDPOINT_URL=  # Custom endpoint URL (for MinIO, etc.) (str | None)
LEX_STORAGE__ENV=  # Environment (development/staging/production) (str | None)
LEX_STORAGE__HEALTH_CHECK_TIMEOUT=  # Timeout in seconds for the startup health check in StorageProvider.boot() (float)
LEX_STORAGE__KMS_KEY_ID=  # KMS/CMEK key ARN or ID (required for aws:kms and gcs:cmek types) (str | None)
LEX_STORAGE__MAX_FILE_SIZE_MB=  # Maximum file size in MB (int)
LEX_STORAGE__NAME=  # — (str)
LEX_STORAGE__PROJECT_ID=  # Google Cloud project ID (str)
LEX_STORAGE__PUBLIC_URL=  # Custom public URL for serving files (e.g., Cloudflare R2 custom domain). (str | None)
LEX_STORAGE__REGION=  # AWS region (str)
LEX_STORAGE__ROOT_DIR=  # Root directory for file storage (str)
LEX_STORAGE__SECRET_KEY=<change-me-in-production>  # AWS secret key (SecretStr | None)
LEX_STORAGE__SERVICE__ALLOWED_MIME_TYPES=  # Allowed MIME types for upload validation. Defaults to a safe set of common im... (list[str])
LEX_STORAGE__SERVICE__MAX_FILE_SIZE_MB=  # Maximum file size in MB (int)
LEX_STORAGE__TYPE=  # Encryption type (Literal['AES256', 'aws:kms', 'gcs:cmek'])

# ── lexigram-tasks ──
LEX_TASKS__AMQP_URL=  # AMQP connection URL (may contain credentials). (SecretStr)
LEX_TASKS__BACKENDS=  # Named task queue backends for multi-queue support. When non-empty, the provid... (list[NamedTaskConfig])
LEX_TASKS__BACKEND__AMQP_URL=  # AMQP connection URL (may contain credentials). (SecretStr)
LEX_TASKS__BACKEND__POSTGRES_DSN=  # Postgres DSN (required when type="postgres"; may contain credentials). (SecretStr | None)
LEX_TASKS__BACKEND__QUEUE_NAME=  # Name of the task queue (str)
LEX_TASKS__BACKEND__REDIS_URL=redis://localhost:6379/0  # Redis connection URL (may contain credentials). (SecretStr)
LEX_TASKS__BACKEND__TYPE=  # Queue backend type (str)
LEX_TASKS__BURST=  # Maximum burst size (int | None)
LEX_TASKS__CHECK_INTERVAL=  # Interval between schedule checks (seconds) (float)
LEX_TASKS__DEFAULT_TIMEOUT=  # Default timeout for tasks without an explicit timeout (seconds) (float)
LEX_TASKS__ENABLED=  # Whether scheduling is enabled (bool)
LEX_TASKS__ENFORCE_TIMEOUT=  # Whether to enforce timeouts on all tasks (bool)
LEX_TASKS__ENV=  # Environment (development/staging/production) (str | None)
LEX_TASKS__EXTRA=  # — (dict[str, Any])
LEX_TASKS__MAX_CONCURRENT_TASKS=  # Maximum concurrent tasks per worker (int)
LEX_TASKS__MAX_TIMEOUT=  # Maximum allowed timeout for any task (seconds) (float)
LEX_TASKS__NAME=  # Configuration name (str)
LEX_TASKS__PER=  # Time period in seconds (float)
LEX_TASKS__POLL_INTERVAL=  # Interval between queue polls (seconds) (float)
LEX_TASKS__POSTGRES_DSN=  # Postgres DSN (required when type="postgres"; may contain credentials). (SecretStr | None)
LEX_TASKS__QUEUE_NAME=  # Name of the task queue (str)
LEX_TASKS__RATE=  # Number of tasks allowed per time period (int)
LEX_TASKS__RATE_LIMIT__BURST=  # Maximum burst size (int | None)
LEX_TASKS__RATE_LIMIT__ENABLED=  # Whether rate limiting is enabled (bool)
LEX_TASKS__RATE_LIMIT__PER=  # Time period in seconds (float)
LEX_TASKS__RATE_LIMIT__RATE=  # Number of tasks allowed per time period (int)
LEX_TASKS__REDIS_URL=redis://localhost:6379/0  # Redis connection URL (may contain credentials). (SecretStr)
LEX_TASKS__RETRY=  # — (RetryConfig)
LEX_TASKS__SCHEDULER__CHECK_INTERVAL=  # Interval between schedule checks (seconds) (float)
LEX_TASKS__SCHEDULER__ENABLED=  # Whether scheduling is enabled (bool)
LEX_TASKS__SCHEDULER__TIMEZONE=  # Timezone for cron expressions (str)
LEX_TASKS__SHUTDOWN_TIMEOUT=  # Timeout for graceful shutdown (seconds) (float)
LEX_TASKS__TIMEOUT__DEFAULT_TIMEOUT=  # Default timeout (float)
LEX_TASKS__TIMEOUT__ENFORCE_TIMEOUT=  # Enforce timeouts (bool)
LEX_TASKS__TIMEOUT__MAX_TIMEOUT=  # Maximum allowed timeout (float)
LEX_TASKS__TIMEZONE=  # Timezone for cron expressions (str)
LEX_TASKS__TYPE=  # Queue backend type (str)
LEX_TASKS__WORKER_COUNT=  # Number of worker instances (int)
LEX_TASKS__WORKER__DEFAULT_TIMEOUT=  # Default timeout for tasks without an explicit timeout (seconds) (float)
LEX_TASKS__WORKER__ENFORCE_TIMEOUT=  # Whether to enforce timeouts on all tasks (bool)
LEX_TASKS__WORKER__MAX_CONCURRENT_TASKS=  # Maximum concurrent tasks per worker (int)
LEX_TASKS__WORKER__MAX_TIMEOUT=  # Maximum allowed timeout for any task (seconds) (float)
LEX_TASKS__WORKER__POLL_INTERVAL=  # Interval between queue polls (seconds) (float)
LEX_TASKS__WORKER__SHUTDOWN_TIMEOUT=  # Timeout for graceful shutdown (seconds) (float)
LEX_TASKS__WORKER__WORKER_COUNT=  # Number of worker instances (int)

# ── lexigram-tenancy ──
LEX_TENANCY__INTEGRATION__CACHE_KEY_PREFIX=  # — (bool)
LEX_TENANCY__INTEGRATION__SQL_CONTEXT_BRIDGE=  # — (bool)
LEX_TENANCY__LIFECYCLE__AUTO_PROVISION_ISOLATION=  # — (bool)
LEX_TENANCY__LIFECYCLE__ISOLATION_STRATEGY=  # — (str)
LEX_TENANCY__OVERRIDES__CACHE_TTL=  # — (int)
LEX_TENANCY__RESOLUTION__HEADER_NAME=  # — (str)
LEX_TENANCY__RESOLUTION__JWT_CLAIM_KEY=  # — (str)
LEX_TENANCY__RESOLUTION__PATH_PATTERN=  # — (str | None)
LEX_TENANCY__RESOLUTION__RESOLVERS=  # — (list[str])
LEX_TENANCY__RESOLUTION__STRICT_MEMBERSHIP=  # — (bool)
LEX_TENANCY__RESOLUTION__SUBDOMAIN_PATTERN=  # — (str | None)
LEX_TENANCY__RESOLUTION__TRUSTED_RESOLVERS=  # — (list[str])
LEX_TENANCY__RESOLUTION__VALIDATOR_CACHE_TTL=  # — (int)

# ── lexigram-testing ──
LEX_TESTING__AUDIT_CAPACITY=  # — (int)
LEX_TESTING__CLEANUP_TEMP_FILES=  # Clean up temporary files after tests (bool)
LEX_TESTING__DB_REUSE=  # Reuse test databases between tests (bool)
LEX_TESTING__ELASTICSEARCH_URL=  # — (str)
LEX_TESTING__ENABLED=  # — (bool)
LEX_TESTING__EVENT_BUS_CAPACITY=  # — (int)
LEX_TESTING__KAFKA_BOOTSTRAP=  # — (str)
LEX_TESTING__MINIO_ACCESS_KEY=  # — (str)
LEX_TESTING__MINIO_ENDPOINT=  # — (str)
LEX_TESTING__MINIO_SECRET_KEY=<change-me-in-production>  # — (str)
LEX_TESTING__MOCK_EXTERNAL_SERVICES=  # Mock external service calls (bool)
LEX_TESTING__MONGODB_DSN=  # — (str)
LEX_TESTING__NEO4J_AUTH=  # — (str)
LEX_TESTING__NEO4J_URL=  # — (str)
LEX_TESTING__OUTBOX_CAPACITY=  # — (int)
LEX_TESTING__POSTGRES_DSN=  # — (str)
LEX_TESTING__POSTGRES_DSN_RAW=  # — (str)
LEX_TESTING__QDRANT_URL=  # — (str)
LEX_TESTING__REDIS_URL=redis://localhost:6379/0  # — (str)
LEX_TESTING__REPOSITORY_CAPACITY=  # — (int)
LEX_TEST_ELASTICSEARCH_URL=  # — (str)
LEX_TEST_KAFKA_BOOTSTRAP=  # — (str)
LEX_TEST_MINIO_ACCESS_KEY=  # — (str)
LEX_TEST_MINIO_ENDPOINT=  # — (str)
LEX_TEST_MINIO_SECRET_KEY=<change-me-in-production>  # — (str)
LEX_TEST_MONGODB_DSN=  # — (str)
LEX_TEST_NEO4J_AUTH=  # — (str)
LEX_TEST_NEO4J_URL=  # — (str)
LEX_TEST_POSTGRES_DSN=  # — (str)
LEX_TEST_POSTGRES_DSN_RAW=  # — (str)
LEX_TEST_QDRANT_URL=  # — (str)
LEX_TEST_REDIS_URL=redis://localhost:6379/0  # — (str)

# ── lexigram-ui ──
LEX_UI__ANIMATION_IN=  # — (str)
LEX_UI__ANIMATION_OUT=  # — (str)
LEX_UI__AUTHOR=  # — (str)
LEX_UI__AUTO_ESCAPE=  # HTML-escape user strings by default. (bool)
LEX_UI__CHANGED=  # — (bool)
LEX_UI__CHARSET=  # — (str)
LEX_UI__COPYRIGHT_HOLDER=  # — (str)
LEX_UI__COPYRIGHT_START_YEAR=  # — (int | None)
LEX_UI__CSS_FILES=  # — (list[str])
LEX_UI__CSS_FRAMEWORK=  # — (str)
LEX_UI__CSS_FRAMEWORK_URL=  # — (str)
LEX_UI__CUSTOM_LEFT=  # — (str)
LEX_UI__CUSTOM_RIGHT=  # — (str)
LEX_UI__DEBUG_COMPONENTS=  # Render data-component debug attributes. (bool)
LEX_UI__DEFAULT_DURATION_MS=  # — (int)
LEX_UI__DEFAULT_THEME=  # Default CSS theme name. (str)
LEX_UI__DELAY_MS=  # — (int)
LEX_UI__DESCRIPTION=  # — (str)
LEX_UI__ENABLE_REALTIME=  # Enable realtime update features. (bool)
LEX_UI__ENABLE_SSE=  # Enable Server-Sent Events support. (bool)
LEX_UI__EVENT_NAME=  # — (str)
LEX_UI__EXTRA_HEAD=  # — (str)
LEX_UI__FAVICON=  # — (str | None)
LEX_UI__FAVICON_TYPE=  # — (str)
LEX_UI__FONT_URL=  # — (str)
LEX_UI__HTMX_URL=  # — (str)
LEX_UI__HTMX_VERSION=  # HTMX CDN version. (str)
LEX_UI__HYPERSCRIPT_URL=  # — (str)
LEX_UI__ICON_LIBRARY=  # — (str)
LEX_UI__ICON_LIBRARY_URL=  # — (str)
LEX_UI__INCLUDE_HYPERSCRIPT=  # — (bool)
LEX_UI__INLINE_CSS=  # — (str)
LEX_UI__KEYWORDS=  # — (list[str])
LEX_UI__LANG=  # — (str)
LEX_UI__LINKS=  # — (list[Any])
LEX_UI__LISTEN_FOR_EVENTS=  # — (bool)
LEX_UI__MAX_TOASTS=  # — (int)
LEX_UI__OG_DESCRIPTION=  # — (str)
LEX_UI__OG_IMAGE=  # — (str)
LEX_UI__OG_TITLE=  # — (str)
LEX_UI__OG_TYPE=  # — (str)
LEX_UI__OG_URL=  # — (str)
LEX_UI__POSITION=  # — (str)
LEX_UI__ROBOTS=  # — (str)
LEX_UI__SHOW_COPYRIGHT=  # — (bool)
LEX_UI__SHOW_DIVIDER=  # — (bool)
LEX_UI__SHOW_VERSION=  # — (bool)
LEX_UI__STACK_DIRECTION=  # — (str)
LEX_UI__STICKY=  # — (bool)
LEX_UI__THEME=  # Active UI theme. (str)
LEX_UI__THEME_COLOR=  # — (str)
LEX_UI__VERSION=  # — (str)
LEX_UI__VIEWPORT=  # — (str)

# ── lexigram-vector ──
LEX_VECTOR__API_BASE=  # Base URL of the embedding API. The client appends '/embeddings' to this URL. (str)
LEX_VECTOR__API_KEY=<change-me-in-production>  # API key sent as Bearer token (required for OpenAI and most cloud providers). (str | None)
LEX_VECTOR__BACKEND=  # Vector store backend to use (str)
LEX_VECTOR__BACKENDS=  # Named vector store backends for multi-store support. When non-empty, the prov... (list[NamedVectorConfig])
LEX_VECTOR__BATCH_SIZE=  # Maximum number of texts per embedding API request. (int)
LEX_VECTOR__CACHE_TTL=  # Cache TTL in seconds (default: 24 hours) (int)
LEX_VECTOR__COLLECTION_NAME=  # Default collection name for AI-layer operations (str)
LEX_VECTOR__DEFAULT_DIMENSION=  # Default vector dimension (int)
LEX_VECTOR__DEFAULT_DISTANCE_METRIC=  # Default distance metric for new collections (DistanceMetric)
LEX_VECTOR__DEFAULT_INDEX_TYPE=  # Default index type for new collections (IndexType)
LEX_VECTOR__DIMENSION=  # Expected output vector dimension. Must match the model (768 for nomic-embed-t... (int)
LEX_VECTOR__EMBEDDING_MODEL=  # Embedding model name for AI-layer embedding generation (str)
LEX_VECTOR__ENABLED=  # Enable the vector store subsystem (bool)
LEX_VECTOR__ENABLE_CACHE=  # Enable embedding caching (requires a CacheBackend binding) (bool)
LEX_VECTOR__FORMAT=  # API payload format. 'openai' uses {'input': [...]}, 'fastembed' uses {'texts'... (Literal['openai', 'fastembed', 'cohere'])
LEX_VECTOR__MAX_RETRIES=  # Maximum number of retries for operations (int)
LEX_VECTOR__MEMORY__MAX_COLLECTIONS=  # Maximum number of collections in memory (int)
LEX_VECTOR__MEMORY__MAX_VECTORS_PER_COLLECTION=  # Maximum number of vectors per collection (int)
LEX_VECTOR__MODEL=  # Embedding model identifier passed to the API. (str)
LEX_VECTOR__PGVECTOR__CREATE_EXTENSION=  # Whether to create pgvector extension if missing (bool)
LEX_VECTOR__PGVECTOR__DATABASE=  # Name of the database backend from db.backends to use for pgvector. Matches a ... (str)
LEX_VECTOR__PGVECTOR__DEFAULT_EF_SEARCH=  # Default ef_search for HNSW index (int)
LEX_VECTOR__PGVECTOR__DEFAULT_LISTS=  # Default number of lists for IVFFlat index (int)
LEX_VECTOR__PGVECTOR__DEFAULT_PROBES=  # Default number of probes for IVFFlat index (int)
LEX_VECTOR__PGVECTOR__SCHEMA=  # Database schema for vector tables (str)
LEX_VECTOR__PGVECTOR__TABLE_PREFIX=  # Prefix for vector storage tables (str)
LEX_VECTOR__PINECONE__API_KEY=<change-me-in-production>  # Pinecone API key (SecretStr)
LEX_VECTOR__PINECONE__ENVIRONMENT=  # Pinecone environment (e.g. 'us-west1-gcp') (str)
LEX_VECTOR__PINECONE__INDEX_NAME=  # Name of the Pinecone index (str)
LEX_VECTOR__PINECONE__NAMESPACE=  # Default namespace for the index (str)
LEX_VECTOR__PINECONE__POOL_THREADS=  # Number of threads for the connection pool (int)
LEX_VECTOR__PINECONE__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__QDRANT__API_KEY=<change-me-in-production>  # Qdrant API key (SecretStr | None)
LEX_VECTOR__QDRANT__GRPC_PORT=  # gRPC port for Qdrant (int)
LEX_VECTOR__QDRANT__PREFER_GRPC=  # Whether to prefer gRPC over HTTP (bool)
LEX_VECTOR__QDRANT__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__QDRANT__URL=  # Qdrant server URL (str)
LEX_VECTOR__RETRY_DELAY=  # Delay between retries in seconds (float)
LEX_VECTOR__TENANCY__ENABLED=  # Enable tenant-aware collection name resolution (bool)
LEX_VECTOR__TENANCY__RESOLVER_KIND=  # Which ``TenantCollectionResolver`` to use. One of ``"templated"`` or ``"pinec... (str)
LEX_VECTOR__TIMEOUT=  # HTTP request timeout in seconds. (float)
LEX_VECTOR__UPSERT_BATCH_SIZE=  # Number of vectors per upsert batch (int)
LEX_VECTOR__WEAVIATE__API_KEY=<change-me-in-production>  # Weaviate API key for authenticated clusters (SecretStr | None)
LEX_VECTOR__WEAVIATE__GRPC_PORT=  # gRPC port for the Weaviate cluster (int)
LEX_VECTOR__WEAVIATE__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__WEAVIATE__URL=  # Weaviate cluster URL (HTTP) (str)

# ── lexigram-web ──
LEX_WEB__ALLOWED_HOSTS=  # Hostnames permitted to reach the application. Empty by default; must be confi... (list[str])
LEX_WEB__ALLOWED_ORIGINS=  # Allowed origins (use ['*'] to allow all) (list[str])
LEX_WEB__ALLOW_CREDENTIALS=<change-me-in-production>  # — (bool)
LEX_WEB__ALLOW_HEADERS=  # — (list[str])
LEX_WEB__ALLOW_METHODS=  # — (list[str])
LEX_WEB__ALLOW_ORIGIN_REGEX=  # Regex pattern for allowed origins (matched when not in allowed_origins) (str | None)
LEX_WEB__API_DOCS__ENABLED=  # Enable API documentation endpoints (/docs, /redoc) and auto-configure CSP for... (bool)
LEX_WEB__API_DOCS__PROVIDER=  # Documentation provider: 'swagger', 'redoc', or 'both' (str)
LEX_WEB__AUTH_EXCLUDE_PATHS=  # Paths to exclude from authentication (list[str])
LEX_WEB__BURST=  # Burst capacity (defaults to requests) (int | None)
LEX_WEB__COMPRESSION_ENABLED=  # — (bool)
LEX_WEB__CONTENT_TYPE_NOSNIFF=  # — (bool)
LEX_WEB__COOKIE_DOMAIN=  # — (str | None)
LEX_WEB__COOKIE_HTTPONLY=  # — (bool)
LEX_WEB__COOKIE_NAME=  # — (str)
LEX_WEB__COOKIE_PATH=  # — (str)
LEX_WEB__COOKIE_SAMESITE=  # — (str)
LEX_WEB__COOKIE_SECURE=  # — (bool)
LEX_WEB__CORS=  # — (CORSConfig)
LEX_WEB__CORS__ALLOWED_ORIGINS=  # Allowed origins (use ['*'] to allow all) (list[str])
LEX_WEB__CORS__ALLOW_CREDENTIALS=<change-me-in-production>  # — (bool)
LEX_WEB__CORS__ALLOW_HEADERS=  # — (list[str])
LEX_WEB__CORS__ALLOW_METHODS=  # — (list[str])
LEX_WEB__CORS__ALLOW_ORIGIN_REGEX=  # Regex pattern for allowed origins (matched when not in allowed_origins) (str | None)
LEX_WEB__CORS__DEBUG_PERMISSIVE=  # When True and debug mode is active, allow any origin via wildcard (explicit o... (bool)
LEX_WEB__CORS__ENABLED=  # Enable CORS (bool)
LEX_WEB__CORS__EXPOSE_HEADERS=  # — (list[str])
LEX_WEB__CORS__MAX_AGE=  # — (int)
LEX_WEB__CROSS_ORIGIN__EMBEDDER_POLICY=  # Cross-Origin-Embedder-Policy header value (str)
LEX_WEB__CROSS_ORIGIN__ENABLED=  # Emit cross-origin isolation headers (bool)
LEX_WEB__CROSS_ORIGIN__OPENER_POLICY=  # Cross-Origin-Opener-Policy header value (str)
LEX_WEB__CROSS_ORIGIN__RESOURCE_POLICY=  # Cross-Origin-Resource-Policy header value (str)
LEX_WEB__CSP=  # — (str | None)
LEX_WEB__CSP__DIRECTIVES=  # CSP directives mapping directive name to source expression(s) (dict[str, Any])
LEX_WEB__CSP__ENABLED=  # Emit the Content-Security-Policy header (bool)
LEX_WEB__CSRF__COOKIE_DOMAIN=  # — (str | None)
LEX_WEB__CSRF__COOKIE_HTTPONLY=  # — (bool)
LEX_WEB__CSRF__COOKIE_NAME=  # — (str)
LEX_WEB__CSRF__COOKIE_PATH=  # — (str)
LEX_WEB__CSRF__COOKIE_SAMESITE=  # — (str)
LEX_WEB__CSRF__COOKIE_SECURE=  # — (bool)
LEX_WEB__CSRF__ENABLED=  # — (bool)
LEX_WEB__CSRF__EXCLUDED_PATHS=  # URL path prefixes exempt from CSRF validation for cookie-less requests; cooki... (list[str])
LEX_WEB__CSRF__EXCLUDE_AUTH_SCHEMES=  # Authorization header schemes that bypass CSRF validation (explicit opt-in). (list[str])
LEX_WEB__CSRF__EXCLUDE_CONTENT_TYPES=  # Content-Type values that bypass CSRF validation (explicit opt-in — JSON reque... (list[str])
LEX_WEB__CSRF__HEADER_NAME=  # — (str)
LEX_WEB__CSRF__SECRET_KEY=<change-me-in-production>  # HMAC secret used to sign and verify CSRF tokens (populated via LEX_WEB__SECUR... (str | None)
LEX_WEB__CSRF__TOKEN_LENGTH=  # — (int)
LEX_WEB__CSRF__TOKEN_TTL=  # TTL in seconds for synchronizer-mode tokens stored in cache. (int)
LEX_WEB__CUSTOM_HEADERS=  # Additional HTTP response headers emitted verbatim (dict[str, str])
LEX_WEB__DEBUG=  # Enable debug mode (bool)
LEX_WEB__DEBUG_PERMISSIVE=  # When True and debug mode is active, allow any origin via wildcard (explicit o... (bool)
LEX_WEB__DEBUG_ROUTES=  # Enable debug routes (bool)
LEX_WEB__DEBUG_ROUTES_RATE_LIMIT=  # Rate limit for debug routes (int)
LEX_WEB__DEBUG_ROUTES_RATE_WINDOW_SECONDS=  # Rate limit window for debug routes (int)
LEX_WEB__DEBUG_ROUTES_REQUIRE_MIDDLEWARE=  # Required middleware for debug routes (str | None)
LEX_WEB__DEBUG_ROUTES_TOKEN=<change-me-in-production>  # Token required to access debug routes (sent as X-Debug-Token header). (SecretStr | None)
LEX_WEB__DEFAULT_LIMIT=  # Max requests per window (int)
LEX_WEB__DEFAULT_WINDOW=  # Window size in seconds (int)
LEX_WEB__DIRECTIVES=  # CSP directives mapping directive name to source expression(s) (dict[str, Any])
LEX_WEB__DIRECTORY=  # Directory to serve (str)
LEX_WEB__EMBEDDER_POLICY=  # Cross-Origin-Embedder-Policy header value (str)
LEX_WEB__ENABLED=  # Enable API documentation endpoints (/docs, /redoc) and auto-configure CSP for... (bool)
LEX_WEB__ENABLE_AUTH=  # Enable built-in authentication middleware. Requires authenticators to be regi... (bool)
LEX_WEB__ENABLE_CORS=  # — (bool)
LEX_WEB__ENABLE_CSRF=  # — (bool)
LEX_WEB__ENABLE_DEBUG_ROUTES_ENV_GATE=  # Require explicit opt-in for debug route registration. (bool)
LEX_WEB__ENABLE_IDENTITY_RESOLUTION=  # Automatically resolve OAuth external IDs to internal UUIDs in authenticated r... (bool)
LEX_WEB__ENV=  # Environment (development/staging/production) (str | None)
LEX_WEB__EXCEPTION_FILTERS=  # Exception filter class names (list[str])
LEX_WEB__EXCLUDED_PATHS=  # URL path prefixes exempt from CSRF validation for cookie-less requests; cooki... (list[str])
LEX_WEB__EXCLUDE_AUTH_SCHEMES=  # Authorization header schemes that bypass CSRF validation (explicit opt-in). (list[str])
LEX_WEB__EXCLUDE_CONTENT_TYPES=  # Content-Type values that bypass CSRF validation (explicit opt-in — JSON reque... (list[str])
LEX_WEB__EXPOSE_HEADERS=  # — (list[str])
LEX_WEB__FAIL_ON_ROUTE_CONFLICT=  # If true, duplicate route registrations will raise during startup; otherwise a... (bool)
LEX_WEB__FRAME_OPTIONS=  # — (str)
LEX_WEB__HEADERS__CONTENT_TYPE_NOSNIFF=  # — (bool)
LEX_WEB__HEADERS__CSP=  # — (str | None)
LEX_WEB__HEADERS__FRAME_OPTIONS=  # — (str)
LEX_WEB__HEADERS__HSTS_INCLUDE_SUBDOMAINS=  # — (bool)
LEX_WEB__HEADERS__HSTS_MAX_AGE=  # — (int)
LEX_WEB__HEADERS__PERMISSIONS_POLICY=  # — (str | None)
LEX_WEB__HEADERS__REFERRER_POLICY=  # — (str)
LEX_WEB__HEADERS__XSS_PROTECTION=  # — (str)
LEX_WEB__HEADER_NAME=  # — (str)
LEX_WEB__HOST=  # Bind host (str)
LEX_WEB__HSTS_INCLUDE_SUBDOMAINS=  # — (bool)
LEX_WEB__HSTS_MAX_AGE=  # — (int)
LEX_WEB__HSTS__ENABLED=  # Emit the Strict-Transport-Security header (bool)
LEX_WEB__HSTS__INCLUDE_SUBDOMAINS=  # Apply HSTS to all subdomains (bool)
LEX_WEB__HSTS__MAX_AGE=  # HSTS max-age in seconds (default 1 year) (int)
LEX_WEB__HSTS__PRELOAD=  # Include site in HSTS preload list (bool)
LEX_WEB__HTML=  # Serve HTML files (SPA mode) (bool)
LEX_WEB__INCLUDE_SUBDOMAINS=  # Apply HSTS to all subdomains (bool)
LEX_WEB__MAX_AGE=  # — (int)
LEX_WEB__MAX_BODY_SIZE=  # Maximum allowed request body size in bytes. Requests with a Content-Length he... (int | None)
LEX_WEB__MIDDLEWARE=  # Middleware class names (list[str])
LEX_WEB__NAME=  # — (str)
LEX_WEB__OPENAPI_TITLE=  # OpenAPI Title (str)
LEX_WEB__OPENAPI_URL=  # — (str | None)
LEX_WEB__OPENAPI_VERSION=  # OpenAPI Version (str)
LEX_WEB__OPENER_POLICY=  # Cross-Origin-Opener-Policy header value (str)
LEX_WEB__PATH=  # Path to guard ('/**' suffix matches the prefix) (str)
LEX_WEB__PERMISSIONS_POLICY=  # — (str | None)
LEX_WEB__PORT=  # Bind port (int)
LEX_WEB__PREFIX=  # URL prefix for static files (str)
LEX_WEB__PRELOAD=  # Include site in HSTS preload list (bool)
LEX_WEB__PROVIDER=  # Documentation provider: 'swagger', 'redoc', or 'both' (str)
LEX_WEB__RATE_LIMIT__DEFAULT_LIMIT=  # Max requests per window (int)
LEX_WEB__RATE_LIMIT__DEFAULT_WINDOW=  # Window size in seconds (int)
LEX_WEB__RATE_LIMIT__ENABLED=  # Enable rate limiting. When true, RateLimitMiddleware enforces the matched per... (bool)
LEX_WEB__RATE_LIMIT__RULES=  # Per-path rate limit rules; longest-prefix match wins (dict[str, RateLimitRuleConfig])
LEX_WEB__RATE_LIMIT__STORAGE_BACKEND=  # Storage backend (memory/redis) (str)
LEX_WEB__RATE_LIMIT__WHITELIST_IPS=  # Exempt IP addresses (list[str])
LEX_WEB__REDOC_JS_URL=  # — (str | None)
LEX_WEB__REDOC_URL=  # — (str | None)
LEX_WEB__REFERRER_POLICY=  # — (str)
LEX_WEB__RELOAD=  # Enable auto-reload (bool)
LEX_WEB__REQUESTS=  # Max requests per window (int)
LEX_WEB__RESOURCE_POLICY=  # Cross-Origin-Resource-Policy header value (str)
LEX_WEB__ROLES=  # Role identifiers allowed to pass (list[str])
LEX_WEB__ROLE_GUARD__RULES=  # Role guard rules in declaration order (list[RoleGuardRuleConfig])
LEX_WEB__RULES=  # Per-path rate limit rules; longest-prefix match wins (dict[str, RateLimitRuleConfig])
LEX_WEB__SECRET_KEY=<change-me-in-production>  # HMAC secret used to sign and verify CSRF tokens (populated via LEX_WEB__SECUR... (str | None)
LEX_WEB__SECURITY__ALLOWED_HOSTS=  # Hostnames permitted to reach the application. Empty by default; must be confi... (list[str])
LEX_WEB__SECURITY__CORS__ALLOWED_ORIGINS=  # Allowed origins (use ['*'] to allow all) (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_CREDENTIALS=<change-me-in-production>  # — (bool)
LEX_WEB__SECURITY__CORS__ALLOW_HEADERS=  # — (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_METHODS=  # — (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_ORIGIN_REGEX=  # Regex pattern for allowed origins (matched when not in allowed_origins) (str | None)
LEX_WEB__SECURITY__CORS__DEBUG_PERMISSIVE=  # When True and debug mode is active, allow any origin via wildcard (explicit o... (bool)
LEX_WEB__SECURITY__CORS__ENABLED=  # Enable CORS (bool)
LEX_WEB__SECURITY__CORS__EXPOSE_HEADERS=  # — (list[str])
LEX_WEB__SECURITY__CORS__MAX_AGE=  # — (int)
LEX_WEB__SECURITY__CROSS_ORIGIN__EMBEDDER_POLICY=  # Cross-Origin-Embedder-Policy header value (str)
LEX_WEB__SECURITY__CROSS_ORIGIN__ENABLED=  # Emit cross-origin isolation headers (bool)
LEX_WEB__SECURITY__CROSS_ORIGIN__OPENER_POLICY=  # Cross-Origin-Opener-Policy header value (str)
LEX_WEB__SECURITY__CROSS_ORIGIN__RESOURCE_POLICY=  # Cross-Origin-Resource-Policy header value (str)
LEX_WEB__SECURITY__CSP__DIRECTIVES=  # CSP directives mapping directive name to source expression(s) (dict[str, Any])
LEX_WEB__SECURITY__CSP__ENABLED=  # Emit the Content-Security-Policy header (bool)
LEX_WEB__SECURITY__CSRF__COOKIE_DOMAIN=  # — (str | None)
LEX_WEB__SECURITY__CSRF__COOKIE_HTTPONLY=  # — (bool)
LEX_WEB__SECURITY__CSRF__COOKIE_NAME=  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_PATH=  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_SAMESITE=  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_SECURE=  # — (bool)
LEX_WEB__SECURITY__CSRF__ENABLED=  # — (bool)
LEX_WEB__SECURITY__CSRF__EXCLUDED_PATHS=  # URL path prefixes exempt from CSRF validation for cookie-less requests; cooki... (list[str])
LEX_WEB__SECURITY__CSRF__EXCLUDE_AUTH_SCHEMES=  # Authorization header schemes that bypass CSRF validation (explicit opt-in). (list[str])
LEX_WEB__SECURITY__CSRF__EXCLUDE_CONTENT_TYPES=  # Content-Type values that bypass CSRF validation (explicit opt-in — JSON reque... (list[str])
LEX_WEB__SECURITY__CSRF__HEADER_NAME=  # — (str)
LEX_WEB__SECURITY__CSRF__SECRET_KEY=change-me-in-production  # HMAC secret used to sign and verify CSRF tokens (populated via LEX_WEB__SECUR... (str | None)
LEX_WEB__SECURITY__CSRF__TOKEN_LENGTH=  # — (int)
LEX_WEB__SECURITY__CSRF__TOKEN_TTL=  # TTL in seconds for synchronizer-mode tokens stored in cache. (int)
LEX_WEB__SECURITY__CUSTOM_HEADERS=  # Additional HTTP response headers emitted verbatim (dict[str, str])
LEX_WEB__SECURITY__ENABLED=  # Enable the security subsystem (bool)
LEX_WEB__SECURITY__ENABLE_CORS=  # — (bool)
LEX_WEB__SECURITY__ENABLE_CSRF=  # — (bool)
LEX_WEB__SECURITY__HEADERS__CONTENT_TYPE_NOSNIFF=  # — (bool)
LEX_WEB__SECURITY__HEADERS__CSP=  # — (str | None)
LEX_WEB__SECURITY__HEADERS__FRAME_OPTIONS=  # — (str)
LEX_WEB__SECURITY__HEADERS__HSTS_INCLUDE_SUBDOMAINS=  # — (bool)
LEX_WEB__SECURITY__HEADERS__HSTS_MAX_AGE=  # — (int)
LEX_WEB__SECURITY__HEADERS__PERMISSIONS_POLICY=  # — (str | None)
LEX_WEB__SECURITY__HEADERS__REFERRER_POLICY=  # — (str)
LEX_WEB__SECURITY__HEADERS__XSS_PROTECTION=  # — (str)
LEX_WEB__SECURITY__HSTS__ENABLED=  # Emit the Strict-Transport-Security header (bool)
LEX_WEB__SECURITY__HSTS__INCLUDE_SUBDOMAINS=  # Apply HSTS to all subdomains (bool)
LEX_WEB__SECURITY__HSTS__MAX_AGE=  # HSTS max-age in seconds (default 1 year) (int)
LEX_WEB__SECURITY__HSTS__PRELOAD=  # Include site in HSTS preload list (bool)
LEX_WEB__SECURITY__PERMISSIONS_POLICY=  # Permissions-Policy directive map (dict[str, str])
LEX_WEB__SECURITY__REFERRER_POLICY=  # Referrer-Policy header value (str)
LEX_WEB__SERVER__DEBUG=  # Enable debug mode (bool)
LEX_WEB__SERVER__HOST=  # Bind host (str)
LEX_WEB__SERVER__PORT=  # Bind port (int)
LEX_WEB__SERVER__RELOAD=  # Enable auto-reload (bool)
LEX_WEB__SERVER__WORKERS=  # Number of workers (int)
LEX_WEB__STATIC__DIRECTORY=  # Directory to serve (str)
LEX_WEB__STATIC__ENABLED=  # Enable static file serving (bool)
LEX_WEB__STATIC__HTML=  # Serve HTML files (SPA mode) (bool)
LEX_WEB__STATIC__PREFIX=  # URL prefix for static files (str)
LEX_WEB__STORAGE_BACKEND=  # Storage backend (memory/redis) (str)
LEX_WEB__SWAGGER_CSS_URL=  # — (str | None)
LEX_WEB__SWAGGER_JS_URL=  # — (str | None)
LEX_WEB__SWAGGER_UI_URL=  # — (str | None)
LEX_WEB__TEMPLATE_DIRECTORY=  # Directory for Jinja2 templates (str)
LEX_WEB__TOKEN_LENGTH=  # — (int)
LEX_WEB__TOKEN_TTL=  # TTL in seconds for synchronizer-mode tokens stored in cache. (int)
LEX_WEB__WHITELIST_IPS=  # Exempt IP addresses (list[str])
LEX_WEB__WINDOW=  # Window size in seconds (int)
LEX_WEB__WORKERS=  # Number of workers (int)
LEX_WEB__XSS_PROTECTION=  # — (str)

# ── lexigram-webhook ──
LEX_WEBHOOK__ALLOW_PRIVATE_URLS=  # Allow registering and delivering webhooks to private, loopback, or link-local... (bool)
LEX_WEBHOOK__DELIVERY_LOG_RETENTION_DAYS=  # Days to retain delivery log (0 = indefinite) (int)
LEX_WEBHOOK__DELIVERY_TIMEOUT_SECONDS=  # HTTP request timeout per delivery attempt (float)
LEX_WEBHOOK__DISABLE_AFTER_CONSECUTIVE_FAILURES=  # Auto-disable threshold (int)
LEX_WEBHOOK__ENABLE_ADMIN=  # Whether to register the admin contributor (bool)
LEX_WEBHOOK__EVENT_ID_HEADER=  # HTTP header for the event ID (str)
LEX_WEBHOOK__EVENT_TYPE_HEADER=  # HTTP header for the event type (str)
LEX_WEBHOOK__FAILURE_WINDOW_HOURS=  # Window for counting consecutive failures (int)
LEX_WEBHOOK__RETRY_BACKOFF_FACTOR=  # Exponential backoff multiplier (float)
LEX_WEBHOOK__RETRY_BASE_DELAY=  # Initial retry delay in seconds (float)
LEX_WEBHOOK__RETRY_MAX_ATTEMPTS=  # Maximum delivery attempts before dead-letter (int)
LEX_WEBHOOK__RETRY_MAX_DELAY=  # Maximum retry delay ceiling in seconds (float)
LEX_WEBHOOK__SECRET_LENGTH=<change-me-in-production>  # Secret length in bytes (hex-encoded output is 2x) (int)
LEX_WEBHOOK__SECRET_ROTATION_GRACE_HOURS=<change-me-in-production>  # Hours both old and new secrets are accepted (int)
LEX_WEBHOOK__SIGNATURE_ALGORITHM=  # HMAC algorithm name (str)
LEX_WEBHOOK__SIGNATURE_HEADER=  # HTTP header for the HMAC signature (str)
LEX_WEBHOOK__STORE_BACKEND=  # Persistence backend ('sql' requires lexigram-webhook[sql]) (str)
LEX_WEBHOOK__TIMESTAMP_HEADER=  # HTTP header for the delivery timestamp (str)

# ── lexigram-workflow ──
LEX_WORKFLOW__BATCH_SIZE=  # Number of items per batch. (int)
LEX_WORKFLOW__CHECKPOINT_ENABLED=  # Persist state checkpoints for resumption after failures (bool)
LEX_WORKFLOW__CIRCUIT_BREAKER_CONFIG=  # Optional circuit breaker configuration for bulk operations. (CircuitBreakerConfig | None)
LEX_WORKFLOW__DEFAULT_TTL_SECONDS=  # Default TTL for cache-backed checkpoint stores. (int)
LEX_WORKFLOW__ENABLED=  # Enable the graph workflow subsystem (bool)
LEX_WORKFLOW__ENABLE_PROGRESS_TRACKING=  # Track operation progress. (bool)
LEX_WORKFLOW__INLINE_THRESHOLD_BYTES=  # Max inline bytes before blob offload. (int)
LEX_WORKFLOW__MAX_CONCURRENCY=  # Maximum concurrent operations. (int)
LEX_WORKFLOW__MAX_ITERATIONS=  # Maximum graph traversal steps (anti-infinite-loop guard) (int)
LEX_WORKFLOW__MAX_PARALLEL_BRANCHES=  # Max parallel branches at once. 0 = unlimited (int)
LEX_WORKFLOW__NODE_TIMEOUT=  # Per-node execution timeout in seconds. 0 = no limit (float)
LEX_WORKFLOW__PARALLEL_BRANCHES=  # Execute independent parallel branches via asyncio.gather (bool)
LEX_WORKFLOW__PIPELINE_TIMEOUT=  # Default pipeline execution timeout in seconds. (float)
LEX_WORKFLOW__RETRY_ATTEMPTS=  # Number of retry attempts on failure. (int)
LEX_WORKFLOW__RETRY_DELAY=  # Delay in seconds between retries. (float)
LEX_WORKFLOW__TIMEOUT=  # Operation timeout in seconds. (float)
LEX_WORKFLOW__TOTAL_TIMEOUT=  # Total workflow timeout in seconds. 0 = no limit (float)

# ---------------------------------------------------------------------------
# Variables referenced directly in source (os.getenv / os.environ) — optional;
# unset values fall back to code defaults. Keep in sync with source usage.
#
# Required by other demos:
#   LEXIGRAM_EXPERIMENT_SEED (llm-experiment), REALTIME_PORT (realtime-monitor)
# Referenced by core framework source / doctor CLIs:
#   ANTHROPIC_API_KEY, APNS_KEY_PATH, APP_ENV, AUDIT_HMAC_KEY, AUTH_JWT_SECRET,
#   BROKER_URL, DATABASE_URL, F5_TTS_REFERENCE_ROOT, FCM_SERVER_KEY, JWT_SECRET,
#   OAUTH_CLIENT_SECRET, OPENAI_API_KEY, OTEL_EXPORTER_OTLP_ENDPOINT,
#   RABBITMQ_URL, REDIS_URL, SMTP_HOST, VECTOR_BACKEND, VECTOR_STORE_BACKEND
# Test/tooling only:
#   ADMIN_BASE, ADMIN_SETUP_TOKEN, AUTH_SECRET, ENVIRONMENT, LEXI_SECRET,
#   LOG_LEVEL, PLAYWRIGHT_SNAPSHOT, TEST_POSTGRES_DSN, UV, UV_PUBLISH_TOKEN
# ---------------------------------------------------------------------------
ADMIN_BASE=http://127.0.0.1:9003  # lexigram-admin e2e test base URL
ADMIN_SETUP_TOKEN=changeme  # lexigram-admin boot token (integration/CI)
ANTHROPIC_API_KEY=sk-ant-changeme  # Anthropic provider key (AI doctor)
APNS_KEY_PATH=  # Apple push notification key path
APP_ENV=development  # generic app environment read
AUDIT_HMAC_KEY=changeme  # audit doctor / signing key
AUTH_JWT_SECRET=changeme  # lexigram-auth JWT secret
AUTH_SECRET=changeme  # lexigram-cli environment validation
OAUTH_CLIENT_SECRET=change-me-oauth-client-secret  # app startup secret hook
BROKER_URL=amqp://guest:guest@localhost:5672//  # queue doctor broker URL
DATABASE_URL=postgresql://lexigram:lexigram@localhost:5432/lexigram  # SQL doctor / DB URL
ENVIRONMENT=development  # legacy app-environment compatibility reads
F5_TTS_REFERENCE_ROOT=  # multimedia-tts reference audio root
FCM_SERVER_KEY=changeme  # Firebase Cloud Messaging key
JWT_SECRET=changeme  # app startup secret hook (JWT signing)
LEXIGRAM_EXPERIMENT_SEED=  # demos/llm-experiment seed
LEXI_SECRET=changeme  # secrets store test fixture
LOG_LEVEL=info  # SQL logging level read
OPENAI_API_KEY=sk-changeme  # OpenAI provider key (AI doctor)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318  # OpenTelemetry collector endpoint
PLAYWRIGHT_SNAPSHOT=  # set to 1 to update admin e2e snapshots
RABBITMQ_URL=amqp://guest:guest@localhost:5672//  # queue doctor RabbitMQ URL
REALTIME_PORT=7071  # demos/realtime-monitor port
REDIS_URL=redis://localhost:6379/0  # queue doctor Redis URL
SENTRY_DSN=  # Sentry DSN; error tracking falls back to this when LEX_MONITOR__ERROR_TRACKING__DSN is unset
SMTP_HOST=localhost:25  # notification doctor SMTP host
TEST_POSTGRES_DSN=postgresql://lexigram:lexigram@localhost:5432/lexigram_test  # events/tasks postgres integration tests
UV=uv  # uv binary used by publish tooling
UV_PUBLISH_TOKEN=changeme  # PyPI publish token
VECTOR_BACKEND=memory  # vector doctor backend name
VECTOR_STORE_BACKEND=memory  # vector doctor backend name

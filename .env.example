# Lexigram Framework core environment configuration example.
#
# Copy to .env and adjust values for your deployment:
#   cp .env.example .env
#
# All variables are optional unless noted: every config value carries a
# code default, so an unset variable falls back to framework defaults.
#
# Critical secrets MUST be set in production:
#   * any value shown as <change-me-in-production>
#   * provider API keys (OpenAI, Anthropic, Google, ...) supplied through
#     your secrets manager or *_SECRET_NAME references

# ── lexigram ──
DEBUG=false  # — (str)
LEXIGRAM__APP_NAME=lexigram-app  # Application name (str)
LEXIGRAM__DEBUG=false  # Enable debug mode. Also toggled by the DEBUG env var. (bool)
LEXIGRAM__DISCOVERY__AUTO_DISCOVER=false  # — (bool)
LEXIGRAM__DISCOVERY__DIRECTORIES=  # — (list[str])
LEXIGRAM__DISCOVERY__DISABLED_MODULES=  # — (list[str])
LEXIGRAM__DISCOVERY__ENABLED_MODULES=  # — (list[str])
LEXIGRAM__DISCOVERY__ENTRY_POINT_GROUP=lexigram.modules  # — (str)
LEXIGRAM__ENV=Environment.DEVELOPMENT  # Deployment environment (development, staging, production, test). (Environment)
LEXIGRAM__HEALTH__CHECK_TIMEOUT=DEFAULT_HEALTH_CHECK_TIMEOUT  # — (float)
LEXIGRAM__HEALTH__INCLUDE_DETAILS=true  # — (bool)
LEXIGRAM__LOGGING__JSON_FORMAT=false  # JSON log format (bool)
LEXIGRAM__LOGGING__LEVEL=INFO  # Global log level (str)
LEXIGRAM__LOGGING__LEVELS=  # Per-logger level overrides (dict[str, str])
LEXIGRAM__LOGGING__REDACTION__ENABLED=true  # — (bool)
LEXIGRAM__LOGGING__REDACTION__FIELD_DENYLIST=  # — (tuple[str, ...])
LEXIGRAM__LOGGING__SAMPLING__DEFAULT_RATE=1.0  # — (float)
LEXIGRAM__LOGGING__SAMPLING__ENABLED=false  # — (bool)
LEXIGRAM__LOGGING__SAMPLING__RULES=  # — (dict[str, float])
LEXIGRAM__MODULES=  # Enabled modules (list[str])
PROFILE=development  # — (str)
QUIET=false  # — (str)
SECURITY__HASHING__ALGORITHM=pbkdf2_sha256  # — (str)
SECURITY__HASHING__BLAKE2B_DIGEST_SIZE=64  # — (int)
SECURITY__HASHING__DEFAULT_HASHER=sha256  # — (str)
SECURITY__HASHING__DKLEN=32  # — (int)
SECURITY__HASHING__ITERATIONS=100000  # — (int)
SECURITY__HASHING__SALT_LENGTH=16  # — (int)
SECURITY__SANITIZATION__ALLOWED_TAGS=  # — (set[str] | None)
SECURITY__SANITIZATION__DEFAULT_SANITIZE_MODE=allow  # — (str)
SECURITY__SANITIZATION__STRIP_COMMENTS=true  # — (bool)

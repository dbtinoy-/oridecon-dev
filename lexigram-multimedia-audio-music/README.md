# lexigram-multimedia-audio-music

Music generation for the Lexigram Framework — local and API-based
backends (`local-http`, `stable-audio`).

Part of the `lexigram-multimedia` package family. See the umbrella
package `lexigram-multimedia` for orchestration, or use this package
directly via `AudioMusicModule.configure(...)`.

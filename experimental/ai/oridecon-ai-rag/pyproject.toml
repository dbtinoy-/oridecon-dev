[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "oridecon-ai-rag"
version = "0.1.000"
description = "Retrieval-Augmented Generation (RAG) pipeline for the Oridecon Framework"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["oridecon", "rag", "retrieval-augmented-generation", "ai", "framework", "async", "python"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
authors = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
maintainers = [{ name = "Oridecon Framework Team", email = "team@oridecon.dev" }]
dependencies = [
    "oridecon-contracts>=0.1.4",
    "oridecon>=0.1.4",
    "typing-extensions>=4.0.0",
    "aiofiles>=23.0.0",
    "numpy>=1.24.0",
    "pyyaml>=6.0.0",
]
[project.urls]
Homepage = "https://oridecon.dev"
Repository = "https://github.com/dbtinoy-/oridecon"
Documentation = "https://docs.oridecon.dev"
Issues = "https://github.com/dbtinoy-/oridecon/issues"
Changelog = "https://github.com/dbtinoy-/oridecon/blob/main/CHANGELOG.md"

[project.optional-dependencies]
audio = [
    "librosa>=0.10.0",
    "mutagen>=1.47.0",
]
video = [
    "opencv-python>=4.8.0",
]
pdf = ["pypdf2>=3.0"]
web = ["beautifulsoup4>=4.12", "aiohttp>=3.9"]
clip = ["transformers>=4.30", "torch>=2.0", "pillow>=10.0.0"]
compression = ["llmlingua>=0.2.0"]
reranking = ["flashrank>=0.2.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]
dev = ["ruff>=0.8.0", "mypy>=1.0.0"]
all = ["oridecon-ai-rag[pdf,web,clip,compression,reranking]"]

[dependency-groups]
dev = ["ruff>=0.8.0", "mypy>=1.0.0"]
test = [
    "oridecon-testing>=0.1.4",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[project.entry-points."oridecon.providers"]
rag = "oridecon.ai.rag.di.provider:RAGProvider"

[project.entry-points."oridecon.ai.subsystems"]
rag = "oridecon.ai.rag.di.provider:RAGProvider"

[project.entry-points."oridecon.ai.modules"]
rag = "oridecon.ai.rag.module:RAGModule"

[project.entry-points."oridecon.config"]
ai-rag = "oridecon.ai.rag.config:RAGConfig"

[project.entry-points."oridecon.chunking.strategies"]
fixed_size = "oridecon.ai.rag.chunking.strategies.fixed_size:FixedSizeChunker"
recursive = "oridecon.ai.rag.chunking.strategies.recursive:RecursiveChunker"
semantic = "oridecon.ai.rag.chunking.strategies.semantic:SemanticChunker"
sliding_window = "oridecon.ai.rag.chunking.strategies.sliding_window:SlidingWindowChunker"
token = "oridecon.ai.rag.chunking.strategies.token:TokenChunker"

[tool.hatch.build.targets.wheel]
packages = ["src/oridecon"]

[tool.uv.sources]
oridecon = { workspace = true }
oridecon-contracts = { workspace = true }
oridecon-testing = { workspace = true }

[tool.mypy]
python_version = "3.13"
namespace_packages = true
explicit_package_bases = true
mypy_path = ["src"]
disable_error_code = ["attr-defined", "unused-ignore", "unreachable", "no-untyped-def", "type-arg", "misc", "arg-type", "assignment", "return-value", "override", "call-arg", "no-any-return", "operator", "dict-item", "import-not-found"]
ignore_missing_imports = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
show_error_codes = true
strict = true
overrides = [
    { module = "tests.*", ignore_errors = true },
]

[tool.ruff]
extend = "../../../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["oridecon"]
force-sort-within-sections = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
addopts = [
    "--cov=oridecon.ai.rag",
    "--cov-report=term",
    "--cov-report=html",
    "--cov-fail-under=60",
]
markers = [
    "integration: mark a test as an integration test",
    "scenario: marks cross-package scenario integration tests",
]

# Copy to application.yaml and adjust to your environment. Remove sections you don't use.
# Full field reference: see the README Configuration section.
# YAML key: cli:
# Environment variable prefix: ORI_CLI__

cli:
  # Scaffolding Defaults
  default_template: "web-api"   # Template used by `lex new` (ORI_CLI__DEFAULT_TEMPLATE)
  default_database: "postgres"  # Database driver scaffold uses (ORI_CLI__DEFAULT_DATABASE)

  # Output
  color: true    # Enable coloured terminal output (ORI_CLI__COLOR)
  verbose: false # Print verbose/debug output (ORI_CLI__VERBOSE)

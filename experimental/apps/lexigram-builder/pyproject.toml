[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lexigram-builder"
version = "0.1.0"
description = "Visual graph-to-code generation framework for Lexigram"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "lexigram>=0.1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/lexigram_builder"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

# Lexigram Framework full environment configuration example.
#
# Copy to .env and adjust values for your deployment:
#   cp .env.full.example .env
#
# Generated from docs/reference/REF_ENV_VARS.md by
# dev/generators/env_example.py — do not edit by hand.
#
# All variables are optional unless noted: every config value carries a
# code default, so an unset variable falls back to framework defaults.
#
# Critical secrets MUST be set in production:
#   * any value shown as <change-me-in-production>
#   * provider API keys (OpenAI, Anthropic, Google, ...) supplied through
#     your secrets manager or LEX_*_SECRET_NAME references

# ── lexigram ──
LEX_DEBUG=false  # — (str)
LEX_LEXIGRAM__APP_NAME=lexigram-app  # Application name (str)
LEX_LEXIGRAM__DEBUG=false  # Enable debug mode. Also toggled by the LEX_DEBUG env var. (bool)
LEX_LEXIGRAM__DISCOVERY__AUTO_DISCOVER=false  # — (bool)
LEX_LEXIGRAM__DISCOVERY__DIRECTORIES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__DISABLED_MODULES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__ENABLED_MODULES=  # — (list[str])
LEX_LEXIGRAM__DISCOVERY__ENTRY_POINT_GROUP=lexigram.modules  # — (str)
LEX_LEXIGRAM__ENV=Environment.DEVELOPMENT  # Deployment environment (development, staging, production, test). (Environment)
LEX_LEXIGRAM__HEALTH__CHECK_TIMEOUT=DEFAULT_HEALTH_CHECK_TIMEOUT  # — (float)
LEX_LEXIGRAM__HEALTH__INCLUDE_DETAILS=true  # — (bool)
LEX_LEXIGRAM__LOGGING__JSON_FORMAT=false  # JSON log format (bool)
LEX_LEXIGRAM__LOGGING__LEVEL=INFO  # Global log level (str)
LEX_LEXIGRAM__LOGGING__LEVELS=  # Per-logger level overrides (dict[str, str])
LEX_LEXIGRAM__LOGGING__REDACTION__ENABLED=true  # — (bool)
LEX_LEXIGRAM__LOGGING__REDACTION__FIELD_DENYLIST=  # — (tuple[str, ...])
LEX_LEXIGRAM__LOGGING__SAMPLING__DEFAULT_RATE=1.0  # — (float)
LEX_LEXIGRAM__LOGGING__SAMPLING__ENABLED=false  # — (bool)
LEX_LEXIGRAM__LOGGING__SAMPLING__RULES=  # — (dict[str, float])
LEX_LEXIGRAM__MODULES=  # Enabled modules (list[str])
LEX_PROFILE=development  # — (str)
LEX_QUIET=false  # — (str)
LEX_SECURITY__HASHING__ALGORITHM=pbkdf2_sha256  # — (str)
LEX_SECURITY__HASHING__BLAKE2B_DIGEST_SIZE=64  # — (int)
LEX_SECURITY__HASHING__DEFAULT_HASHER=sha256  # — (str)
LEX_SECURITY__HASHING__DKLEN=32  # — (int)
LEX_SECURITY__HASHING__ITERATIONS=100000  # — (int)
LEX_SECURITY__HASHING__SALT_LENGTH=16  # — (int)
LEX_SECURITY__SANITIZATION__ALLOWED_TAGS=  # — (set[str] | None)
LEX_SECURITY__SANITIZATION__DEFAULT_SANITIZE_MODE=allow  # — (str)
LEX_SECURITY__SANITIZATION__STRIP_COMMENTS=true  # — (bool)

# ── lexigram-admin ──
LEX_ADMIN__API_PREFIX=/admin/api  # — (str)
LEX_ADMIN__AUDIT__READ_AUDIT_ENABLED=false  # Log read operations (off by default; compliance mode only). (bool)
LEX_ADMIN__AUTH__CSRF_TOKEN_LIFETIME=3600  # CSRF token expiry in seconds (int)
LEX_ADMIN__AUTH__EMAIL_OTP__ENABLED=true  # Enable email OTP factor (bool)
LEX_ADMIN__AUTH__EMAIL_OTP__RESEND_COOLDOWN_SECONDS=60  # Minimum seconds between email OTP sends (int)
LEX_ADMIN__AUTH__EMAIL_OTP__TTL_MINUTES=10  # Code validity window in minutes (int)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__ENABLED=true  # Enable email verification flow (bool)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__ENFORCEMENT=true  # Block login until the email is verified (bool)
LEX_ADMIN__AUTH__EMAIL_VERIFICATION__TOKEN_TTL_HOURS=24  # Verify link validity in hours (int)
LEX_ADMIN__AUTH__ENABLED=true  # Enable authentication (bool)
LEX_ADMIN__AUTH__ENV=development  # Deployment environment for cookie security defaults (Literal['development', 'staging', 'production'])
LEX_ADMIN__AUTH__IDLE_TIMEOUT=3600  # Session idle timeout in seconds (int)
LEX_ADMIN__AUTH__LOGIN_URL=/admin/login  # — (str)
LEX_ADMIN__AUTH__LOGOUT_URL=/admin/logout  # — (str)
LEX_ADMIN__AUTH__MFA__ENABLED=true  # Enable TOTP 2FA (bool)
LEX_ADMIN__AUTH__MFA__FACTOR=totp  # Second factor used at login: 'totp' (authenticator app) or 'email' (one-time ... (str)
LEX_ADMIN__AUTH__MFA__ISSUER=Lexigram Admin  # TOTP issuer label shown in authenticator apps (str)
LEX_ADMIN__AUTH__MFA__SKEW=1  # Allowed clock skew in 30 second steps (int)
LEX_ADMIN__AUTH__OAUTH_ENABLED=false  # — (bool)
LEX_ADMIN__AUTH__OAUTH_PROVIDERS=  # — (list[str])
LEX_ADMIN__AUTH__PASSWORD_POLICY__MAX_LENGTH=128  # — (int)
LEX_ADMIN__AUTH__PASSWORD_POLICY__MIN_LENGTH=12  # — (int)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REJECT_COMMON_PASSWORDS=true  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REJECT_CONTAINING_EMAIL=true  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_DIGIT=true  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_LOWERCASE=true  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_SPECIAL=true  # — (bool)
LEX_ADMIN__AUTH__PASSWORD_POLICY__REQUIRE_UPPERCASE=true  # — (bool)
LEX_ADMIN__AUTH__PERMISSION_CACHE_TTL=300  # — (int)
LEX_ADMIN__AUTH__PRINCIPAL_SOURCE=internal  # — (Literal['internal', 'app'])
LEX_ADMIN__AUTH__REGISTRATION__ALLOWED_EMAIL_DOMAINS=  # Restrict registration to these email domains (empty = any) (list[str])
LEX_ADMIN__AUTH__REGISTRATION__DEFAULT_ROLE=admin  # Role granted to new accounts (str)
LEX_ADMIN__AUTH__REGISTRATION__ENABLED=false  # Allow self-service registration (bool)
LEX_ADMIN__AUTH__ROLES=  # — (dict[str, Any])
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_ENABLED=true  # — (bool)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_15_MINUTES=30  # — (int)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_HOUR=60  # — (int)
LEX_ADMIN__AUTH__SECURITY__IP_RATE_LIMIT_PER_MINUTE=10  # — (int)
LEX_ADMIN__AUTH__SECURITY__LOCKOUT_THRESHOLDS=  # — (list[tuple[int, int]])
LEX_ADMIN__AUTH__SECURITY__PERMANENT_LOCKOUT_THRESHOLD=50  # — (int)
LEX_ADMIN__AUTH__SECURITY__SETUP_TOKEN=<change-me-in-production>  # Optional ADMIN_SETUP_TOKEN — when set, must be provided during first-run setup. (SecretStr | None)
LEX_ADMIN__AUTH__SECURITY__SETUP_TOKEN_OPTIN_UNSAFE=false  # Explicit escape hatch: boot without a setup token. Only for local/ephemeral e... (bool)
LEX_ADMIN__AUTH__SESSION_LIFETIME=86400  # — (int)
LEX_ADMIN__AUTH__SESSION_SECRET=<change-me-in-production>  # Session secret for signing (SecretStr)
LEX_ADMIN__AUTH__USERS=  # — (list[Any])
LEX_ADMIN__CLUSTERS__EXTRA=  # Extra clusters beyond the built-in infrastructure cluster (list[ClusterSpec])
LEX_ADMIN__COMMANDS=  # — (list[dict[str, Any]])
LEX_ADMIN__CONTRIBUTORS=  # — (dict[str, ContributorConfig])
LEX_ADMIN__CONTRIBUTOR_COLLISION_MODE=warn  # How to handle name collisions when multiple contributors register widgets, pa... (Literal['warn', 'error'])
LEX_ADMIN__DASHBOARD_LAYOUT__LAYOUT=grid  # — (Literal['grid', 'masonry'])
LEX_ADMIN__DASHBOARD_LAYOUT__MAX_WIDGETS=20  # — (int)
LEX_ADMIN__DASHBOARD_LAYOUT__WIDGET_REFRESH_DEFAULT=30  # — (int)
LEX_ADMIN__DATA__QUERY_TIMEOUT_SECONDS=5  # — (int)
LEX_ADMIN__DEBUG=false  # — (bool)
LEX_ADMIN__ENABLED=true  # — (bool)
LEX_ADMIN__EXTENSIONS=  # — (dict[str, Any])
LEX_ADMIN__FEATURES__ACTIVITY_FEED=false  # — (bool)
LEX_ADMIN__FEATURES__API_DOCS=true  # — (bool)
LEX_ADMIN__FEATURES__AUDIT_LOGGING=true  # — (bool)
LEX_ADMIN__FEATURES__AUTOSAVE=false  # — (bool)
LEX_ADMIN__FEATURES__COMMAND_PALETTE=true  # — (bool)
LEX_ADMIN__FEATURES__KEYBOARD_SHORTCUTS=true  # — (bool)
LEX_ADMIN__FEATURES__NOTIFICATIONS=true  # — (bool)
LEX_ADMIN__FEATURES__OPTIMISTIC_UPDATES=true  # — (bool)
LEX_ADMIN__FEATURES__SEARCH=true  # — (bool)
LEX_ADMIN__FEATURES__THEME_TOGGLE=true  # — (bool)
LEX_ADMIN__FEATURES__UNDO_REDO=true  # — (bool)
LEX_ADMIN__FEATURES__WEBHOOKS=false  # — (bool)
LEX_ADMIN__FORM_DEFAULTS=  # — (FormDefaults)
LEX_ADMIN__FRAMEWORK_PAGES__ENABLED=true  # — (bool)
LEX_ADMIN__FRAMEWORK_PAGES__REQUIRE_PERMISSION=admin:framework:access  # — (str)
LEX_ADMIN__HTMX_PREFIX=/admin/htmx  # — (str)
LEX_ADMIN__INTEGRATIONS__CACHE__DEFAULT_TTL_SECONDS=60  # — (int)
LEX_ADMIN__INTEGRATIONS__CACHE__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__CACHE__KEY_PREFIX=admin  # — (str)
LEX_ADMIN__INTEGRATIONS__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__FEATURES__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__MONITOR__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__CIRCUIT_FAILURE_THRESHOLD=5  # — (int)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__RESILIENCE__RETRY_MAX_ATTEMPTS=3  # — (int)
LEX_ADMIN__INTEGRATIONS__SEARCH__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__SEARCH__FALLBACK_TO_LIKE=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__STORAGE__ENABLED=true  # — (bool)
LEX_ADMIN__INTEGRATIONS__STORAGE__PRESIGNED_URL_EXPIRY=3600  # — (int)
LEX_ADMIN__INTEGRATIONS__TASKS__BULK_THRESHOLD=25  # — (int)
LEX_ADMIN__INTEGRATIONS__TASKS__ENABLED=true  # — (bool)
LEX_ADMIN__NAME=admin  # — (str)
LEX_ADMIN__NAVIGATION_GROUPS=  # — (dict[str, AdminNavigationGroup])
LEX_ADMIN__OBSERVABILITY__HIGH_CARDINALITY_LABELS_ENABLED=false  # — (bool)
LEX_ADMIN__OBSERVABILITY__METRICS_ENABLED=true  # — (bool)
LEX_ADMIN__PREFIX=/admin  # — (str)
LEX_ADMIN__RATE_LIMIT__BULK_PER_MINUTE=5  # — (int)
LEX_ADMIN__RATE_LIMIT__BURST_SIZE=10  # — (int)
LEX_ADMIN__RATE_LIMIT__CREATE_PER_MINUTE=30  # — (int)
LEX_ADMIN__RATE_LIMIT__DELETE_PER_MINUTE=20  # — (int)
LEX_ADMIN__RATE_LIMIT__ENABLED=true  # — (bool)
LEX_ADMIN__RATE_LIMIT__REQUESTS_PER_HOUR=1000  # — (int)
LEX_ADMIN__RATE_LIMIT__REQUESTS_PER_MINUTE=60  # — (int)
LEX_ADMIN__RATE_LIMIT__UPDATE_PER_MINUTE=60  # — (int)
LEX_ADMIN__RBAC__SUPER_ADMIN_ROLE=superadmin  # already special-cased by settings/widgets/impersonation. (str)
LEX_ADMIN__REQUIRE_AUTH=true  # — (bool)
LEX_ADMIN__RESOURCES=  # — (dict[str, ResourceYAMLConfig])
LEX_ADMIN__RESOURCE_DEFAULTS=  # — (ResourceDefaults)
LEX_ADMIN__STATIC_DIR=  # — (str | None)
LEX_ADMIN__STATIC_PREFIX=/admin/static  # — (str)
LEX_ADMIN__STRICT_RESOURCE_RESOLUTION=true  # When True (production default), resource/controller resolution failures durin... (bool)
LEX_ADMIN__TABLE_DEFAULTS=  # — (TableDefaults)
LEX_ADMIN__TEMPLATES_DIR=  # — (str | None)
LEX_ADMIN__TENANCY__COOKIE_NAME=admin_tenant  # — (str)
LEX_ADMIN__TENANCY__DEFAULT_TENANT_ID=  # — (str)
LEX_ADMIN__TENANCY__ENABLED=false  # — (bool)
LEX_ADMIN__TENANCY__HEADER_NAME=x-tenant-id  # — (str)
LEX_ADMIN__TENANCY__ROUTE_PREFIX_TEMPLATE=  # — (str)
LEX_ADMIN__TENANCY__TENANT_FIELD=tenant_id  # — (str)
LEX_ADMIN__TITLE=Lexigram Admin  # — (str)
LEX_ADMIN__UI__CONTENT_MAX_WIDTH=  # — (int | None)
LEX_ADMIN__UI__FAVICON_URL=  # — (str | None)
LEX_ADMIN__UI__LOGO_URL=  # — (str | None)
LEX_ADMIN__UI__PRIMARY_COLOR=#6B7280  # — (str)
LEX_ADMIN__UI__SIDEBAR_COLLAPSED_WIDTH=64  # — (int)
LEX_ADMIN__UI__SIDEBAR_WIDTH=256  # — (int)
LEX_ADMIN__UI__THEME=system  # — (Literal['light', 'dark', 'system'])

# ── lexigram-ai ──
LEX_AI__ENABLED=true  # Enable AI features (bool)
LEX_AI__GOVERNANCE=  # AI governance configuration (Any)
LEX_AI__LLM=  # LLM configuration (optional) (Any | None)
LEX_AI__NAME=ai  # Configuration name (str)
LEX_AI__OBSERVABILITY=  # AI observability configuration (tracing and metrics) (Any)
LEX_AI__RAG=  # RAG pipeline configuration (optional) (Any | None)
LEX_AI__SUBSYSTEMS=  # Dynamic configuration for third-party AI subsystems discovered via entry poin... (dict[str, dict[str, Any]])
LEX_AI__VECTOR=  # Vector store configuration (Any | None)
LEX_ENV=development  # — (str)

# ── lexigram-ai-agents ──
LEX_AI_AGENTS__DEFAULT_MAX_TOKENS=2048  # Default max tokens for LLM responses (int)
LEX_AI_AGENTS__DEFAULT_TEMPERATURE=0.7  # Default temperature for LLM calls (float)
LEX_AI_AGENTS__ENABLED=true  # Enable the AI agents subsystem (bool)
LEX_AI_AGENTS__ENABLE_METRICS=true  # Enable Prometheus metrics (bool)
LEX_AI_AGENTS__ENABLE_TRACING=true  # Enable OpenTelemetry tracing (bool)
LEX_AI_AGENTS__MAX_ITERATIONS=10  # Maximum reasoning iterations per execution (int)
LEX_AI_AGENTS__TOOL_MAX_RETRIES=3  # Number of retries for transient tool execution errors (ConnectionError, Timeo... (int)

# ── lexigram-ai-evaluation ──
LEX_AI_EVALUATION__DEFAULT_SEED=  # Default seed for reproducible experiment runs (int | None)
LEX_AI_EVALUATION__DEFAULT_THRESHOLD=0.8  # Default score threshold for passing evaluations (float)
LEX_AI_EVALUATION__EMBEDDING_MODEL=text-embedding-3-small  # Model to use for embedding-based evaluations (str)
LEX_AI_EVALUATION__ENABLED=true  # Enable the AI evaluation subsystem (bool)
LEX_AI_EVALUATION__EXPERIMENT_DIR=  # Base directory for experiment tracking and checkpoint artifacts (str | None)
LEX_AI_EVALUATION__INCLUDE_METADATA=true  # Whether to include metadata in run reports (bool)
LEX_AI_EVALUATION__MAX_RETRIES=3  # Maximum retries for failed evaluations (int)
LEX_AI_EVALUATION__MAX_SAMPLES=  # Maximum number of samples per evaluation run (int | None)
LEX_AI_EVALUATION__TIMEOUT_SECONDS=30  # Timeout for evaluation execution in seconds (int)

# ── lexigram-ai-feedback ──
LEX_AI_FEEDBACK__ASYNC_PROCESSING=true  # Process feedback handlers asynchronously in the background (bool)
LEX_AI_FEEDBACK__ENABLED=true  # Master on/off switch for all feedback collection (bool)
LEX_AI_FEEDBACK__STORE_RAW_PAYLOADS=false  # Persist raw incoming feedback payloads for auditing (bool)

# ── lexigram-ai-governance ──
LEX_AI_GOVERNANCE__ENABLED=true  # Enable AI governance (bool)
LEX_AI_GOVERNANCE__ENFORCE_BUDGET=true  # Enforce budget limits (bool)
LEX_AI_GOVERNANCE__FAIL_OPEN_ON_PERSISTENCE_ERROR=false  # Allow requests when the persistence backend is unavailable. When False (defau... (bool)
LEX_AI_GOVERNANCE__MAX_REQUEST_COST=  # Maximum cost in dollars for a single request. Requests with an estimated cost... (float | None)
LEX_AI_GOVERNANCE__MAX_TOKENS_PER_REQUEST=  # Max tokens per request (int | None)
LEX_AI_GOVERNANCE__MONTHLY_BUDGET=  # Monthly budget in dollars (float | None)
LEX_AI_GOVERNANCE__RESOURCE_UNITS=  # Resource units this governance instance tracks. Per-tenant limits are configu... (list)
LEX_AI_GOVERNANCE__RESTRICTED_MODELS=  # List of restricted models (list[str])
LEX_AI_GOVERNANCE__RPM_LIMIT=  # Requests Per Minute limit (int | None)
LEX_AI_GOVERNANCE__SOFT_LIMIT_PCT=  # Fraction of monthly_budget at which to emit a soft-limit warning (e.g. 0.8 = ... (float | None)
LEX_AI_GOVERNANCE__TPM_LIMIT=  # Tokens Per Minute limit (int | None)

# ── lexigram-ai-guard ──
LEX_AI_GUARD__ENABLED=true  # — (bool)
LEX_AI_GUARD__ENABLE_LLM_GUARDS=false  # — (bool)
LEX_AI_GUARD__GUARD_MODEL=gpt-4o-mini  # — (str)
LEX_AI_GUARD__INJECTION_ACTION=block  # — (str)
LEX_AI_GUARD__INJECTION_DETECTION=true  # — (bool)
LEX_AI_GUARD__LENGTH_ACTION=block  # — (str)
LEX_AI_GUARD__LLM_GUARD_FAIL_OPEN=false  # — (bool)
LEX_AI_GUARD__LLM_GUARD_THRESHOLD=0.7  # — (float)
LEX_AI_GUARD__MAX_INPUT_CHARS=0  # — (int)
LEX_AI_GUARD__MAX_OUTPUT_CHARS=0  # — (int)
LEX_AI_GUARD__PARALLEL_EXECUTION=false  # — (bool)
LEX_AI_GUARD__PII_ACTION=redact  # — (str)
LEX_AI_GUARD__PII_DETECTION=true  # — (bool)
LEX_AI_GUARD__PII_ENTITIES=  # — (list[str])
LEX_AI_GUARD__PII_REDACTION_OUTPUT=true  # — (bool)
LEX_AI_GUARD__RESTRICTED_TOPICS=  # — (list[str])
LEX_AI_GUARD__SENSITIVITY_LEVEL=medium  # — (str)

# ── lexigram-ai-llm ──
LEX_AI_LLM__API_BASE=  # Custom API base URL (for Azure, local, or proxied endpoints). (str | None)
LEX_AI_LLM__API_KEY=<change-me-in-production>  # API key for the chosen provider. (SecretStr | None)
LEX_AI_LLM__CACHE_TTL=3600  # Cache TTL in seconds. (int)
LEX_AI_LLM__ENABLED=true  # Enable the LLM subsystem (bool)
LEX_AI_LLM__ENABLE_CACHE=false  # Enable response caching (requires CacheBackendProtocol in container). (bool)
LEX_AI_LLM__EXTRA=  # Provider-specific extra parameters passed verbatim. (dict[str, Any])
LEX_AI_LLM__MAX_TOKENS=  # Maximum tokens in response. (int | None)
LEX_AI_LLM__MODEL=gpt-4-turbo  # Model name or identifier. (str)
LEX_AI_LLM__PIN_POLICY=ModelPinPolicy.LATEST  # Policy for enforcing the model revision pin. (ModelPinPolicy)
LEX_AI_LLM__PRICING__CACHE_TTL=86400  # Pricing cache TTL in seconds (default: 24 hours). (int)
LEX_AI_LLM__PRICING__ENABLED=true  # Register pricing manager and cost estimator. (bool)
LEX_AI_LLM__PRICING__ENABLE_FUZZY_MATCH=true  # Allow substring matching of model names to prices. (bool)
LEX_AI_LLM__PRICING__SOURCES=  # Pricing sources in priority order. (list[PricingSourceConfig])
LEX_AI_LLM__PROVIDER=ModelProvider.OPENAI  # LLM provider name. (ModelProvider)
LEX_AI_LLM__TEMPERATURE=0.7  # Sampling temperature. (float)
LEX_AI_LLM__THINKING__BUDGET_TOKENS=10000  # — (int)
LEX_AI_LLM__THINKING__EFFORT=  # — (str | None)
LEX_AI_LLM__THINKING__LEVEL=  # — (str | None)
LEX_AI_LLM__THINKING__SUPPRESS=false  # — (bool)
LEX_AI_LLM__TIMEOUT=60.0  # Request timeout in seconds. (float)

# ── lexigram-ai-mcp ──
LEX_AI_MCP__ALLOW_UNAUTHENTICATED=false  # — (bool)
LEX_AI_MCP__CLIENT_STDIO_COMMAND=  # — (list[str])
LEX_AI_MCP__CLIENT_URL=  # — (str | None)
LEX_AI_MCP__CORS_ORIGINS=  # — (list[str])
LEX_AI_MCP__ENABLED=true  # Enable the MCP server subsystem (bool)
LEX_AI_MCP__ENABLE_SSE=true  # — (bool)
LEX_AI_MCP__HOST=0.0.0.0  # — (str)
LEX_AI_MCP__MAX_REQUEST_SIZE=1024 * 1024  # — (int)
LEX_AI_MCP__PATH=/mcp  # — (str)
LEX_AI_MCP__PORT=8080  # — (int)
LEX_AI_MCP__REQUEST_TIMEOUT=30.0  # — (float)
LEX_AI_MCP__SERVER_NAME=lexigram-mcp  # — (str)
LEX_AI_MCP__SERVER_VERSION=1.0.0  # — (str)
LEX_AI_MCP__STDIO_MODE=false  # — (bool)

# ── lexigram-ai-memory ──
LEX_AI_MEMORY__CONSOLIDATION__AGE_THRESHOLD_HOURS=  # Minimum entry age (hours) before it can be consolidated (float)
LEX_AI_MEMORY__CONSOLIDATION__BATCH_SIZE=  # Maximum entries processed per consolidation pass (int)
LEX_AI_MEMORY__CONSOLIDATION__ENABLED=true  # Whether automatic background consolidation is active (bool)
LEX_AI_MEMORY__CONSOLIDATION__IMPORTANCE_PRUNE_THRESHOLD=  # Entries below this importance score are eligible for pruning (float)
LEX_AI_MEMORY__CONSOLIDATION__INTERVAL_SECONDS=  # How often to run a consolidation pass (seconds) (float)
LEX_AI_MEMORY__DEFAULT_BACKEND=  # Backend type to use ('in_memory', 'cache', 'database', 'vector') (str)
LEX_AI_MEMORY__ENABLED=true  # Enable the AI memory subsystem (bool)
LEX_AI_MEMORY__EPISODIC__DEFAULT_TOP_K=  # Default number of episodes to retrieve (int)
LEX_AI_MEMORY__EPISODIC__IMPORTANCE_WEIGHT=  # Weight applied to entry importance during scoring (float)
LEX_AI_MEMORY__EPISODIC__RECENCY_WEIGHT=  # Weight applied to temporal recency during scoring (float)
LEX_AI_MEMORY__EPISODIC__RELEVANCE_WEIGHT=  # Weight applied to semantic similarity during scoring (float)
LEX_AI_MEMORY__EPISODIC__TTL_SECONDS=  # Time-to-live for entries in seconds (0 = never expire) (int)
LEX_AI_MEMORY__SEMANTIC__MAX_FACTS_PER_ENTITY=  # Hard cap on stored facts per entity (int)
LEX_AI_MEMORY__SEMANTIC__MIN_CONFIDENCE=  # Minimum confidence score required to store a fact (float)
LEX_AI_MEMORY__TTL_SECONDS=  # Default entry TTL in seconds (0 = never expire) (int)
LEX_AI_MEMORY__WORKING__EPISODIC_FRACTION=  # Fraction of remaining budget for episodic recall (float)
LEX_AI_MEMORY__WORKING__MAX_RECENT_TURNS=  # Hard cap on recent turns regardless of budget (int)
LEX_AI_MEMORY__WORKING__RECENT_TURNS_FRACTION=  # Fraction of remaining budget for recent turns (float)
LEX_AI_MEMORY__WORKING__SEMANTIC_FRACTION=  # Fraction of remaining budget for semantic facts (float)
LEX_AI_MEMORY__WORKING__SYSTEM_PROMPT_TOKENS=  # Fixed token allocation for system prompt (int)
LEX_AI_MEMORY__WORKING__TOOL_DESCRIPTIONS_FRACTION=  # Fraction of remaining budget for tool descriptions (float)

# ── lexigram-ai-observability ──
LEX_AI_OBSERVABILITY__ENABLED=true  # Master on/off switch for all observability (bool)
LEX_AI_OBSERVABILITY__HEALTH_CHECKS_ENABLED=true  # Enable background health checking for AI components (bool)
LEX_AI_OBSERVABILITY__METRICS_ENABLED=true  # Enable metrics collection (bool)
LEX_AI_OBSERVABILITY__TRACE_MAX_ATTRIBUTE_LENGTH=0  # Cap on string attribute values written to trace spans, in characters. 0 disab... (int)
LEX_AI_OBSERVABILITY__TRACE_REDACTION_ENABLED=false  # Redact secret-shaped keys (e.g. token, password, api_key) from trace span att... (bool)
LEX_AI_OBSERVABILITY__TRACING_ENABLED=true  # Enable distributed tracing (bool)

# ── lexigram-ai-prompt ──
LEX_AI_PROMPT__DEFAULT_FORMAT=DEFAULT_RENDER_FORMAT  # — (RenderFormat)
LEX_AI_PROMPT__ENABLED=true  # Enable the AI prompt subsystem (bool)
LEX_AI_PROMPT__MAX_VARIABLE_LENGTH=0  # — (int)
LEX_AI_PROMPT__SANITIZE_INPUTS=true  # — (bool)
LEX_AI_PROMPT__STRICT_SANITIZER=true  # — (bool)

# ── lexigram-ai-rag ──
LEX_AI_RAG__CACHE_TTL=3600  # Cache TTL in seconds (default: 1 hour) (int)
LEX_AI_RAG__CHUNKING_STRATEGY=recursive  # Chunking strategy (recursive, semantic, token) (str)
LEX_AI_RAG__CHUNK_OVERLAP=50  # Overlap between consecutive chunks (int)
LEX_AI_RAG__CHUNK_SIZE=512  # Text chunk size in tokens (int)
LEX_AI_RAG__CITATION_STYLE=inline  # Citation style (inline, footnote, numbered) (str)
LEX_AI_RAG__COLLECTION_NAME=default  # Collection/index name for vector store (str)
LEX_AI_RAG__EMBEDDING_MODEL=  # Embedding model identifier. Must be set explicitly — no vendor-specific default. (str | None)
LEX_AI_RAG__EMBEDDING_PROVIDER=openai  # Embedding provider (openai, cohere, etc.) (str)
LEX_AI_RAG__ENABLED=true  # Enable the RAG pipeline (bool)
LEX_AI_RAG__ENABLE_CACHING=true  # Enable caching for RAG queries (bool)
LEX_AI_RAG__ENABLE_CITATIONS=true  # Include source citations in responses (bool)
LEX_AI_RAG__ENABLE_HALLUCINATION_DETECTION=true  # Enable hallucination detection for AI responses (bool)
LEX_AI_RAG__ENABLE_HYDE=false  # Enable HyDE (Hypothetical Document Embeddings) (bool)
LEX_AI_RAG__ENABLE_QUERY_EXPANSION=true  # Enable query expansion techniques (bool)
LEX_AI_RAG__MIN_CITATION_CONFIDENCE=0.6  # Minimum confidence for citation inclusion (float)
LEX_AI_RAG__PERSIST_DIRECTORY=  # Local directory path for vector store persistence (e.g. Chroma) (str | None)
LEX_AI_RAG__SIMILARITY_THRESHOLD=0.7  # Minimum similarity score threshold (float)
LEX_AI_RAG__SYNTHESIS_STRATEGY=hybrid  # Synthesis strategy (direct, extractive, abstractive, hybrid) (str)
LEX_AI_RAG__TENANCY__ENABLED=false  # Enable tenant-aware collection resolution in RAG pipeline (bool)
LEX_AI_RAG__TOP_K=5  # Number of documents to retrieve (int)
LEX_AI_RAG__USE_HYBRID_SEARCH=true  # Enable hybrid search (semantic + keyword) (bool)
LEX_AI_RAG__VECTOR_DIMENSION=1536  # Embedding vector dimension (1536 for OpenAI ada-002) (int)
LEX_AI_RAG__VECTOR_STORE_TYPE=pgvector  # Vector store backend (pgvector, chroma, qdrant, mock) (str)

# ── lexigram-ai-session ──
LEX_AI_SESSION__AUTO_CHECKPOINT_INTERVAL=  # Checkpoint every N turns; None to disable (int | None)
LEX_AI_SESSION__BACKEND=  # Persistence backend (in_memory, cache, database) (str)
LEX_AI_SESSION__CLEANUP_INTERVAL_S=  # How often the cleanup scheduler sweeps for expired sessions (int)
LEX_AI_SESSION__CONSOLIDATE_ON_CLOSE=  # Whether to trigger memory consolidation on session close (bool)
LEX_AI_SESSION__COOKIE_NAME=  # Cookie name for web session ID; None disables cookies (str | None)
LEX_AI_SESSION__DEFAULT_SYSTEM_PROMPT=  # System prompt injected into every new session (str | None)
LEX_AI_SESSION__DEFAULT_TURN_STRATEGY=  # Default turn-selection strategy (round_robin, priority, llm_directed) (str)
LEX_AI_SESSION__ENABLED=true  # Enable the AI session subsystem (bool)
LEX_AI_SESSION__HEADER_NAME=  # HTTP header name for session ID pass-through (str)
LEX_AI_SESSION__MAX_AGENTS_PER_GROUP=  # Maximum agents in a multi-agent group session (int)
LEX_AI_SESSION__MAX_BRANCHES_PER_SESSION=  # Maximum forked branches per session (int)
LEX_AI_SESSION__MAX_CHECKPOINTS_PER_SESSION=  # Maximum retained checkpoints per session (int)
LEX_AI_SESSION__MAX_SESSIONS_PER_USER=  # Maximum concurrent sessions per user (int)
LEX_AI_SESSION__MAX_TURNS_PER_SESSION=  # Hard cap on turns before the session is closed (int)
LEX_AI_SESSION__NAME=ai-session  # Logical name used for DI registration keys (str)
LEX_AI_SESSION__SESSION_TTL=  # Maximum age of a session in seconds (0 to disable) (int)

# ── lexigram-ai-skills ──
LEX_AI_SKILLS__ALLOWED_SCRIPT_TYPES=  # Allowed script types (py, sh, js) (list[str])
LEX_AI_SKILLS__AUTO_DISCOVER=  # Whether to auto-scan packages for skills on boot (bool)
LEX_AI_SKILLS__BUILTIN_SKILLS=  # Names of built-in skills to register (list[str])
LEX_AI_SKILLS__CACHE_BACKEND=  # Which cache backend to use (in_memory, cache) (str)
LEX_AI_SKILLS__CACHE_ENABLED=  # Whether result caching is globally enabled (bool)
LEX_AI_SKILLS__CACHE_TTL_SECONDS=  # Default TTL for cached skill results (seconds) (int)
LEX_AI_SKILLS__DEFAULT_TIMEOUT_SECONDS=  # Default execution timeout per skill (seconds) (float)
LEX_AI_SKILLS__ENABLED_DIRECTORIES=  # Which skill directories to enable (claude_code, opencode, cursor, etc.) (list[str])
LEX_AI_SKILLS__ENABLE_BUILTIN=  # Whether built-in skills are registered on boot (bool)
LEX_AI_SKILLS__ENABLE_SKILL_SOURCES=true  # Whether to scan for external skill sources on boot (bool)
LEX_AI_SKILLS__ENFORCE_PERMISSIONS=  # Whether permission checks are enforced (bool)
LEX_AI_SKILLS__LAZY_LOAD_CONTEXT=  # Whether to lazily load skill context files (bool)
LEX_AI_SKILLS__MAX_CONCURRENT_EXECUTIONS=  # Semaphore cap on concurrent skill executions (int)
LEX_AI_SKILLS__MAX_RETRIES=  # Default maximum retry attempts for skill execution (int)
LEX_AI_SKILLS__NAME=ai-skills  # Logical name used for DI registration keys (str)
LEX_AI_SKILLS__SCAN_PACKAGES=  # Fully-qualified package names to scan for skills (list[str])
LEX_AI_SKILLS__SCRIPT_TIMEOUT_SECONDS=  # Timeout for skill script execution (seconds) (int)
LEX_AI_SKILLS__SKILL_PATHS=  # Paths to scan for skills (SKILL.md folders) (list[str])

# ── lexigram-ai-workers ──
LEX_AI_WORKERS__BATCH_EMBEDDING_CONCURRENCY=3  # Concurrency level for batch embedding execution (int)
LEX_AI_WORKERS__DLQ_CHECK_INTERVAL=60  # Interval in seconds for DLQ recovery sweeps (int)
LEX_AI_WORKERS__DOCUMENT_INGESTION_CONCURRENCY=3  # Concurrency level for document parsing and chunking (int)
LEX_AI_WORKERS__ENABLED=true  # Master on/off switch for all background workers (bool)
LEX_AI_WORKERS__ENABLE_MAINTENANCE=true  # Enable vector store and cache maintenance tasks (bool)

# ── lexigram-audit ──
LEX_AUDIT__ENABLE_ADMIN=  # Whether to register the AuditAdminContributor (bool)
LEX_AUDIT__HMAC_KEY=<change-me-in-production>  # HMAC key for checksum computation (bytes | None)
LEX_AUDIT__RETENTION_POLICY=  # Retention rules (RetentionPolicy)
LEX_AUDIT__STORE_BACKEND=  # Backend type — 'sql' or 'memory' (str)
LEX_AUDIT__TABLE_NAME=  # SQL table name for the unified audit store (str)
LEX_AUDIT__VERIFICATION_BATCH_SIZE=  # Entries to verify per verification run (int)
LEX_AUDIT__VERIFICATION_SCHEDULE=  # Cron expression for scheduled verification (str)

# ── lexigram-auth ──
LEX_AUTH__ADMIN_EMAIL=  # Initial admin email (str | None)
LEX_AUTH__ADMIN_PASSWORD=<change-me-in-production>  # Initial admin password (str | None)
LEX_AUTH__ENABLED=true  # — (bool)
LEX_AUTH__LOGIN_RATE_LIMIT=5/minute  # Default rate limit (str)
LEX_AUTH__MAX_SESSIONS_PER_USER=  # Maximum number of concurrent sessions allowed per user. ``None`` (the default... (int | None)
LEX_AUTH__MIDDLEWARE__BACKEND=session  # Auth backend type (str)
LEX_AUTH__MIDDLEWARE__EXCLUDE_PATHS=  # Paths excluded from auth (list[str])
LEX_AUTH__MIDDLEWARE__EXCLUDE_PREFIXES=  # Path prefixes excluded (list[str])
LEX_AUTH__MIDDLEWARE__HEADER_NAME=Authorization  # Header name for token (str)
LEX_AUTH__MIDDLEWARE__LOGIN_RATE_LIMIT=5/minute  # Rate limit for auth endpoints (str)
LEX_AUTH__MIDDLEWARE__LOGIN_URL=  # URL to redirect for login (str | None)
LEX_AUTH__MIDDLEWARE__OPTIONAL_AUTH=false  # Whether authentication is optional (bool)
LEX_AUTH__MIDDLEWARE__PERMISSIONS_REQUIRED=  # Permissions required (list[str])
LEX_AUTH__MIDDLEWARE__ROLES_REQUIRED=  # Roles required (list[str])
LEX_AUTH__MIDDLEWARE__SCHEME=  # Token scheme (str)
LEX_AUTH__NAME=auth  # — (str)
LEX_AUTH__OAUTH2_PROVIDERS=  # OAuth2 configs (dict[str, dict[str, str]])
LEX_AUTH__PASSWORD__ARGON2_MEMORY_COST=65536  # Argon2id memory cost in KiB (OWASP floor is 19456) (int)
LEX_AUTH__PASSWORD__ARGON2_PARALLELISM=4  # Argon2id parallelism (int)
LEX_AUTH__PASSWORD__ARGON2_TIME_COST=3  # Argon2id time cost (int)
LEX_AUTH__PASSWORD__BANNED_PATTERNS=  # Substrings that must not appear in the password (case-insensitive). Use to re... (list[str])
LEX_AUTH__PASSWORD__BCRYPT_ROUNDS=12  # bcrypt cost factor for new hashes (minimum 12 in production) (int)
LEX_AUTH__PASSWORD__MAX_LENGTH=128  # Maximum password length (int)
LEX_AUTH__PASSWORD__MIN_LENGTH=12  # Minimum password length (int)
LEX_AUTH__PASSWORD__REQUIRE_DIGITS=true  # Require at least one digit (bool)
LEX_AUTH__PASSWORD__REQUIRE_LOWERCASE=false  # Require at least one lowercase letter (bool)
LEX_AUTH__PASSWORD__REQUIRE_SPECIAL=false  # Require at least one special character (non-alphanumeric) (bool)
LEX_AUTH__PASSWORD__REQUIRE_UPPERCASE=true  # Require at least one uppercase letter (bool)
LEX_AUTH__RBAC__CACHE_PERMISSIONS=true  # Cache resolved permissions (bool)
LEX_AUTH__RBAC__DEFAULT_ROLE=viewer  # Default role for new users (str)
LEX_AUTH__RBAC__ENABLED=true  # Enable RBAC enforcement (bool)
LEX_AUTH__RBAC__PERMISSION_CACHE_TTL=300  # Permission cache TTL in seconds (int)
LEX_AUTH__RBAC__SUPERUSER_BYPASS=true  # Allow superuser role to bypass all checks (bool)
LEX_AUTH__RELAY_VERIFICATION=false  # Enable binding ``RelayAuthVerifierProtocol`` for the relay gateway's inbound ... (bool)
LEX_AUTH__ROLES=  # Role definitions (dict[str, AuthRoleConfig])
LEX_AUTH__SECRET_KEY=<change-me-in-production>  # Secret key for signing (str)
LEX_AUTH__TOKEN__ACCESS_TOKEN_EXPIRE=  # Access token expiry duration (Duration)
LEX_AUTH__TOKEN__ALGORITHM=  # Algorithm (str)
LEX_AUTH__TOKEN__ID_TOKEN_EXPIRE=1h  # ID token expiry duration (Duration)
LEX_AUTH__TOKEN__KEY_ROTATION_GRACE_PERIOD=  # Duration during which tokens signed by a rotated-out key remain accepted. Pre... (Duration)
LEX_AUTH__TOKEN__REFRESH_TOKEN_EXPIRE=  # Refresh token expiry duration (Duration)
LEX_AUTH__TOKEN__REQUIRED_AUDIENCE=  # Expected ``aud`` claim for every token verified by this service. When set, to... (str | None)
LEX_AUTH__TOKEN__SECRET_KEY=<change-me-in-production>  # Secret key for signing tokens (SecretStr)
LEX_AUTH__USERS=  # Initial users (list[AuthUserConfig])

# ── lexigram-cache ──
LEX_CACHE__BACKENDS=  # Backend configs (list[CacheBackendConfig])
LEX_CACHE__DEBUG=  # Debug mode (bool)
LEX_CACHE__ENABLED=  # Whether cache is enabled (bool)
LEX_CACHE__ENV=  # Environment (development/staging/production) (str | None)
LEX_CACHE__ENVIRONMENT=  # Environment (str)
LEX_CACHE__NAME=  # Provider name (str)
LEX_CACHE__SERVICE__CIRCUIT_BREAKER_ENABLED=  # Enable circuit breaker (bool)
LEX_CACHE__SERVICE__CIRCUIT_BREAKER_THRESHOLD=  # Circuit breaker threshold (int)
LEX_CACHE__SERVICE__DEFAULT_BACKEND=  # Default backend name (str | None)
LEX_CACHE__SERVICE__DEFAULT_SERIALIZER=  # Default serializer (str)
LEX_CACHE__SERVICE__ENABLE_HEALTH_CHECKS=  # Enable health checks (bool)
LEX_CACHE__SERVICE__ENABLE_METRICS=  # Enable metrics (bool)
LEX_CACHE__SERVICE__ENABLE_PROTECTION=  # Enable stampede protection (bool)
LEX_CACHE__SERVICE__PROTECTION_LOCK_TTL=  # Protection lock TTL (int)
LEX_CACHE__SERVICE__PROTECTION_MAX_WAIT=  # Max wait for locks (float)
LEX_CACHE__SERVICE__PROTECTION_RETRY_INTERVAL=  # Lock retry interval (float)
LEX_CACHE__VERSION=  # Config version (str)

# ── lexigram-cli ──
LEX_CLI__COLOR=true  # — (bool)
LEX_CLI__DEFAULT_DATABASE=postgres  # — (str)
LEX_CLI__DEFAULT_TEMPLATE=web-api  # — (str)
LEX_CLI__VERBOSE=false  # — (bool)
LEX_CONFIG=  # — (str)

# ── lexigram-events ──
LEX_EVENTS__COMMAND_BUS__ENABLE_LOGGING=  # — (bool)
LEX_EVENTS__COMMAND_BUS__ENABLE_METRICS=  # — (bool)
LEX_EVENTS__COMMAND_BUS__ENABLE_VALIDATION=  # — (bool)
LEX_EVENTS__COMMAND_BUS__MAX_RETRIES=  # — (int)
LEX_EVENTS__COMMAND_BUS__RETRY_DELAY_SECONDS=  # — (float)
LEX_EVENTS__COMMAND_BUS__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__DEBUG=  # — (bool)
LEX_EVENTS__ENABLED=true  # — (bool)
LEX_EVENTS__ENV=  # Environment (development/staging/production) (str | None)
LEX_EVENTS__EVENT_BUS__ALLOW_NO_HANDLERS=  # — (bool)
LEX_EVENTS__EVENT_BUS__CONTINUE_ON_ERROR=  # — (bool)
LEX_EVENTS__EVENT_BUS__ENABLE_DEAD_LETTER=  # — (bool)
LEX_EVENTS__EVENT_BUS__HANDLER_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__EVENT_BUS__MAX_CONCURRENT_HANDLERS=  # — (int)
LEX_EVENTS__EVENT_BUS__MAX_HANDLER_RETRIES=  # — (int)
LEX_EVENTS__EVENT_BUS__MAX_QUEUE_PER_SUBSCRIBER=  # Maximum number of events queued per event type before backpressure is applied... (int)
LEX_EVENTS__EVENT_BUS__PARALLEL_DISPATCH=  # — (bool)
LEX_EVENTS__EVENT_BUS__RETRY_FAILED_HANDLERS=  # — (bool)
LEX_EVENTS__EVENT_STORE_BACKEND=  # — (EventStoreBackend)
LEX_EVENTS__KAFKA__AUTO_OFFSET_RESET=  # — (str)
LEX_EVENTS__KAFKA__BOOTSTRAP_SERVERS=  # Kafka bootstrap servers (str)
LEX_EVENTS__KAFKA__CONSUMER_GROUP=  # — (str)
LEX_EVENTS__KAFKA__ENABLE_AUTO_COMMIT=  # — (bool)
LEX_EVENTS__KAFKA__TOPIC_PREFIX=  # — (str)
LEX_EVENTS__LOGGING_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__LOGGING_MIDDLEWARE__INCLUDE_PAYLOAD=  # — (bool)
LEX_EVENTS__LOGGING_MIDDLEWARE__LOG_LEVEL=  # — (str)
LEX_EVENTS__LOGGING_MIDDLEWARE__MAX_PAYLOAD_LENGTH=  # — (int)
LEX_EVENTS__MEMORY__ENABLE_SNAPSHOTS=  # — (bool)
LEX_EVENTS__MEMORY__MAX_EVENTS_PER_STREAM=  # — (int)
LEX_EVENTS__METRICS_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__METRICS_MIDDLEWARE__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_EVENTS__METRICS_MIDDLEWARE__INCLUDE_HISTOGRAMS=  # — (bool)
LEX_EVENTS__METRICS_MIDDLEWARE__PREFIX=  # — (str)
LEX_EVENTS__MONGODB__CONNECTION_STRING=  # MongoDB connection string (SecretStr)
LEX_EVENTS__MONGODB__DATABASE_NAME=  # — (str)
LEX_EVENTS__MONGODB__EVENTS_COLLECTION=  # — (str)
LEX_EVENTS__MONGODB__MAX_POOL_SIZE=  # — (int)
LEX_EVENTS__MONGODB__SERVER_SELECTION_TIMEOUT=  # — (int)
LEX_EVENTS__MONGODB__SNAPSHOTS_COLLECTION=  # — (str)
LEX_EVENTS__NAME=events  # — (str)
LEX_EVENTS__POSTGRES__AUTO_CREATE_TABLES=true  # — (bool)
LEX_EVENTS__POSTGRES__COMMAND_TIMEOUT=60.0  # — (float)
LEX_EVENTS__POSTGRES__DSN=  # PostgreSQL connection string. Optional when a DatabaseProviderProtocol is inj... (SecretStr | None)
LEX_EVENTS__POSTGRES__EVENTS_TABLE=events  # — (str)
LEX_EVENTS__POSTGRES__POOL_MAX_SIZE=20  # — (int)
LEX_EVENTS__POSTGRES__POOL_MIN_SIZE=5  # — (int)
LEX_EVENTS__POSTGRES__SNAPSHOTS_TABLE=snapshots  # — (str)
LEX_EVENTS__PROJECTION__BATCH_SIZE=  # — (int)
LEX_EVENTS__PROJECTION__CHECKPOINT_INTERVAL=  # — (int)
LEX_EVENTS__PROJECTION__ENABLE_PARALLEL_PROJECTIONS=  # — (bool)
LEX_EVENTS__PROJECTION__MAX_CATCH_UP_EVENTS=  # — (int)
LEX_EVENTS__PROJECTION__REBUILD_BATCH_SIZE=  # — (int)
LEX_EVENTS__QUERY_BUS__ENABLE_LOGGING=  # — (bool)
LEX_EVENTS__QUERY_BUS__ENABLE_METRICS=  # — (bool)
LEX_EVENTS__QUERY_BUS__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__RABBITMQ__DURABLE=  # — (bool)
LEX_EVENTS__RABBITMQ__EXCHANGE_NAME=  # — (str)
LEX_EVENTS__RABBITMQ__PREFETCH_COUNT=  # — (int)
LEX_EVENTS__RABBITMQ__QUEUE_PREFIX=  # — (str)
LEX_EVENTS__RABBITMQ__URL=  # AMQP connection URL (SecretStr)
LEX_EVENTS__RETRY_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__RETRY_MIDDLEWARE__EXPONENTIAL_BASE=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__INITIAL_DELAY_SECONDS=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__MAX_DELAY_SECONDS=  # — (float)
LEX_EVENTS__RETRY_MIDDLEWARE__MAX_RETRIES=  # — (int)
LEX_EVENTS__SAGA__CLEANUP_COMPLETED_AFTER_HOURS=  # — (int)
LEX_EVENTS__SAGA__DEFAULT_TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__SAGA__ENABLE_COMPENSATION=  # — (bool)
LEX_EVENTS__SAGA__MAX_RETRIES_PER_STEP=  # — (int)
LEX_EVENTS__SAGA__PERSIST_STATE=  # — (bool)
LEX_EVENTS__SAGA__RETRY_DELAY_SECONDS=  # — (float)
LEX_EVENTS__SNAPSHOTS__ENABLED=  # — (bool)
LEX_EVENTS__SNAPSHOTS__EVENT_COUNT_THRESHOLD=  # — (int)
LEX_EVENTS__SNAPSHOTS__MAX_SNAPSHOTS_PER_AGGREGATE=  # — (int)
LEX_EVENTS__SNAPSHOTS__STRATEGY=  # — (SnapshotStrategy)
LEX_EVENTS__SNAPSHOTS__TIME_THRESHOLD_SECONDS=  # — (int)
LEX_EVENTS__SQLITE__DATABASE=./events.db  # — (str)
LEX_EVENTS__SQLITE__JOURNAL_MODE=WAL  # — (str)
LEX_EVENTS__SQLITE__PRAGMAS=  # — (dict[str, str])
LEX_EVENTS__SQLITE__WAL_MODE=true  # — (bool)
LEX_EVENTS__STREAMING__BATCH_SIZE=  # — (int)
LEX_EVENTS__STREAMING__BUFFER_SIZE=  # — (int)
LEX_EVENTS__STREAMING__ENABLE_WEBSOCKET=  # — (bool)
LEX_EVENTS__STREAMING__MAX_SUBSCRIBERS=  # — (int)
LEX_EVENTS__STREAMING__POLL_INTERVAL_MS=  # — (int)
LEX_EVENTS__STREAMING__WEBSOCKET_PING_INTERVAL=  # — (int)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__ISOLATION_LEVEL=  # — (str)
LEX_EVENTS__TRANSACTION_MIDDLEWARE__TIMEOUT_SECONDS=  # — (float)
LEX_EVENTS__VALIDATION_MIDDLEWARE__ENABLED=  # — (bool)
LEX_EVENTS__VALIDATION_MIDDLEWARE__STRICT_MODE=  # — (bool)
LEX_EVENTS__VERSION_SKEW_ALERTS_ENABLED=true  # — (bool)

# ── lexigram-features ──
LEX_FEATURES__CACHE_TTL=DEFAULT_CACHE_TTL  # Seconds to cache flag evaluations (0 = disabled). (int)
LEX_FEATURES__DEFAULT_ENABLED=DEFAULT_ENABLED  # Default value when a flag is not found in the provider. (bool)
LEX_FEATURES__ENABLED=true  # Enable the feature flags subsystem (bool)
LEX_FEATURES__FLAG_ENV_PREFIX=FLAG_ENV_PREFIX  # Env var prefix used by EnvProvider when reading flag values. (str)
LEX_FEATURES__INITIAL_FLAGS=  # Seed flags for the in-memory provider (name -> enabled). (dict[str, bool])

# ── lexigram-graph ──
LEX_GRAPH__BACKEND=  # Graph store backend to use (str)
LEX_GRAPH__BULK_BATCH_SIZE=  # Batch size for bulk operations (int)
LEX_GRAPH__DEFAULT_QUERY_LIMIT=  # Default limit for query results (int)
LEX_GRAPH__DEFAULT_TRAVERSAL_MAX_DEPTH=  # Default maximum depth for traversals (int)
LEX_GRAPH__ENABLED=true  # Enable the graph store subsystem (bool)
LEX_GRAPH__MAX_RETRIES=  # Maximum number of retries for operations (int)
LEX_GRAPH__MEMORY__MAX_EDGES=  # Maximum number of edges in memory (int)
LEX_GRAPH__MEMORY__MAX_NODES=  # Maximum number of nodes in memory (int)
LEX_GRAPH__NEO4J__CONNECTION_TIMEOUT=  # Connection timeout in seconds (float)
LEX_GRAPH__NEO4J__DATABASE=  # Target database name (str)
LEX_GRAPH__NEO4J__ENCRYPTED=false  # Whether to use SSL/TLS encryption (bool)
LEX_GRAPH__NEO4J__FETCH_SIZE=  # Default fetch size for results (int)
LEX_GRAPH__NEO4J__MAX_CONNECTION_POOL_SIZE=  # Maximum number of connections in the pool (int)
LEX_GRAPH__NEO4J__MAX_TRANSACTION_RETRY_TIME=30.0  # Maximum time for transaction retries (float)
LEX_GRAPH__NEO4J__PASSWORD=<change-me-in-production>  # Neo4j password (SecretStr)
LEX_GRAPH__NEO4J__TRUST=TRUST_SYSTEM_CA_SIGNED_CERTIFICATES  # Trust strategy for SSL (str)
LEX_GRAPH__NEO4J__URI=bolt://localhost:7687  # Neo4j BOLT URI (str)
LEX_GRAPH__NEO4J__USERNAME=neo4j  # Neo4j username (str)
LEX_GRAPH__RETRY_DELAY=  # Delay between retries in seconds (float)
LEX_GRAPH__TENANCY__ENABLED=false  # Enable tenant-aware graph resolution (bool)
LEX_GRAPH__TENANCY__STRATEGY=node_property  # Which tenancy strategy to use. One of ``"node_property"`` or ``"graph_per_ten... (str)
LEX_GRAPH__TENANCY__TEMPLATE=  # Collection name template for ``GRAPH_PER_TENANT`` strategy. Supports ``{logic... (str)

# ── lexigram-graphql ──
LEX_GRAPHQL__ALIAS_LIMIT__ENABLED=true  # — (bool)
LEX_GRAPHQL__ALIAS_LIMIT__MAX_ALIASES=  # — (int)
LEX_GRAPHQL__BATCH__ENABLED=false  # — (bool)
LEX_GRAPHQL__BATCH__MAX_BATCH_SIZE=10  # — (int)
LEX_GRAPHQL__CACHE__DEFAULT_MAX_AGE=  # — (Duration | int)
LEX_GRAPHQL__CACHE__DEFAULT_SCOPE=CacheScope.PUBLIC  # — (CacheScope)
LEX_GRAPHQL__CACHE__ENABLED=true  # — (bool)
LEX_GRAPHQL__CACHE__VARY_HEADERS=  # — (list[str])
LEX_GRAPHQL__COMPLEXITY__DEFAULT_FIELD_COST=1.0  # — (float)
LEX_GRAPHQL__COMPLEXITY__DEFAULT_LIST_COST=10.0  # — (float)
LEX_GRAPHQL__COMPLEXITY__ENABLED=true  # — (bool)
LEX_GRAPHQL__COMPLEXITY__MAX_COMPLEXITY=  # — (int)
LEX_GRAPHQL__DATALOADER__BATCH_DELAY_MS=2.0  # Delay in milliseconds before executing a DataLoaderProtocol batch. A small no... (float)
LEX_GRAPHQL__DATALOADER__BATCH_ENABLED=true  # — (bool)
LEX_GRAPHQL__DATALOADER__CACHE_ENABLED=true  # — (bool)
LEX_GRAPHQL__DATALOADER__ENABLED=true  # — (bool)
LEX_GRAPHQL__DATALOADER__MAX_BATCH_SIZE=100  # — (int)
LEX_GRAPHQL__DEBUG=false  # — (bool)
LEX_GRAPHQL__DEPTH_LIMIT__ENABLED=true  # — (bool)
LEX_GRAPHQL__DEPTH_LIMIT__IGNORE_INTROSPECTION=true  # — (bool)
LEX_GRAPHQL__DEPTH_LIMIT__MAX_DEPTH=  # — (int)
LEX_GRAPHQL__ENABLED=true  # — (bool)
LEX_GRAPHQL__ENABLE_IDENTITY_RESOLUTION=false  # — (bool)
LEX_GRAPHQL__ENV=  # Environment (development/staging/production) (str | None)
LEX_GRAPHQL__ERRORS__DEBUG_MODE=false  # — (bool)
LEX_GRAPHQL__ERRORS__INCLUDE_STACKTRACE=false  # — (bool)
LEX_GRAPHQL__ERRORS__LOG_ERRORS=true  # — (bool)
LEX_GRAPHQL__ERRORS__MASK_ERRORS=true  # — (bool)
LEX_GRAPHQL__INTROSPECTION__ALLOWED_ENVIRONMENTS=  # — (set[str])
LEX_GRAPHQL__INTROSPECTION__ENABLED=true  # — (bool)
LEX_GRAPHQL__METRICS__ENABLED=false  # — (bool)
LEX_GRAPHQL__METRICS__HISTOGRAM_BUCKETS=  # — (list[float])
LEX_GRAPHQL__METRICS__INCLUDE_LABELS=  # — (list[str])
LEX_GRAPHQL__METRICS__NAMESPACE=lexigram_graphql  # — (str)
LEX_GRAPHQL__NAME=graphql  # — (str)
LEX_GRAPHQL__PATH=  # — (str)
LEX_GRAPHQL__PERSISTED_QUERIES__ENABLED=true  # — (bool)
LEX_GRAPHQL__PERSISTED_QUERIES__STORE_TYPE=memory  # — (str)
LEX_GRAPHQL__PERSISTED_QUERIES__TTL_SECONDS=86400  # — (Duration | int)
LEX_GRAPHQL__PLAYGROUND__ENABLED=true  # — (bool)
LEX_GRAPHQL__PLAYGROUND__PATH=  # — (str)
LEX_GRAPHQL__PLAYGROUND__TITLE=Lexigram GraphQL Playground  # — (str)
LEX_GRAPHQL__RATE_LIMIT__BURST_LIMIT=10  # — (int)
LEX_GRAPHQL__RATE_LIMIT__ENABLED=true  # — (bool)
LEX_GRAPHQL__RATE_LIMIT__REQUESTS_PER_MINUTE=60  # — (int)
LEX_GRAPHQL__SCHEMA_BASELINE_PATH=  # Path to a GraphQL SDL (.graphql) file containing the baseline schema. When se... (str | None)
LEX_GRAPHQL__SUBSCRIPTIONS__CONNECTION_TIMEOUT=60  # — (Duration | int)
LEX_GRAPHQL__SUBSCRIPTIONS__ENABLED=true  # — (bool)
LEX_GRAPHQL__SUBSCRIPTIONS__KEEPALIVE_INTERVAL=  # — (Duration | int)
LEX_GRAPHQL__SUBSCRIPTIONS__PATH=  # — (str)
LEX_GRAPHQL__SUBSCRIPTIONS__PROTOCOL=SubscriptionProtocol.GRAPHQL_TRANSPORT_WS  # — (SubscriptionProtocol)
LEX_GRAPHQL__TRACING__ENABLED=false  # — (bool)
LEX_GRAPHQL__TRACING__SAMPLE_RATE=1.0  # — (float)
LEX_GRAPHQL__TRACING__SERVICE_NAME=lexigram-graphql  # — (str)
LEX_GRAPHQL__TRACING__TRACE_DATALOADERS=true  # — (bool)
LEX_GRAPHQL__TRACING__TRACE_RESOLVERS=true  # — (bool)

# ── lexigram-http ──
LEX_HTTP__COOKIE_JAR=true  # — (bool)
LEX_HTTP__ENFORCE_URL_SAFETY=true  # — (bool)
LEX_HTTP__MAX_REDIRECTS=5  # — (int)
LEX_HTTP__POOL__FORCE_CLOSE=false  # — (bool)
LEX_HTTP__POOL__MAX_CONNECTIONS=DEFAULT_MAX_CONNECTIONS  # — (int)
LEX_HTTP__POOL__MAX_CONNECTIONS_PER_HOST=DEFAULT_MAX_CONNECTIONS_PER_HOST  # — (int)
LEX_HTTP__POOL__MAX_KEEPALIVE_CONNECTIONS=DEFAULT_MAX_KEEPALIVE_CONNECTIONS  # — (int)
LEX_HTTP__POOL__TIMEOUT=DEFAULT_TIMEOUT  # — (float)
LEX_HTTP__POOL__TTL_DNS_CACHE=DEFAULT_TTL_DNS_CACHE  # — (int)
LEX_HTTP__PROXY=  # — (str | None)
LEX_HTTP__TRUST_ENV=true  # — (bool)

# ── lexigram-monitor ──
LEX_MONITOR__BACKEND_TYPE=  # Monitoring backend type (BackendType)
LEX_MONITOR__DEBUG=  # Enable debug mode (bool)
LEX_MONITOR__ENABLED=  # Enable monitoring (bool)
LEX_MONITOR__ENV=  # Environment (development/staging/production) (str | None)
LEX_MONITOR__ENVIRONMENT=  # Deployment environment (Environment)
LEX_MONITOR__ERROR_TRACKING__DSN=  # Sentry DSN; error tracking is a no-op when unset (SecretStr | None)
LEX_MONITOR__ERROR_TRACKING__ENVIRONMENT=  # Environment tag for captured events (Any)
LEX_MONITOR__ERROR_TRACKING__SEND_DEFAULT_PII=  # Send default PII fields to the error tracker (bool)
LEX_MONITOR__ERROR_TRACKING__TRACES_SAMPLE_RATE=  # Traces sample rate (0.0 to 1.0) (float)
LEX_MONITOR__HEALTH__CHECKS=  # List of health check names to run (list[str])
LEX_MONITOR__HEALTH__ENABLED=  # Enable health checks (bool)
LEX_MONITOR__HEALTH__INCLUDE_DETAILS=  # Include detailed health info in response (bool)
LEX_MONITOR__HEALTH__INTERVAL=  # Health check interval in seconds (int)
LEX_MONITOR__HEALTH__PATH=  # Health endpoint path (str)
LEX_MONITOR__HEALTH__TIMEOUT=  # Health check timeout in seconds (float)
LEX_MONITOR__LOGGING__ENABLED=  # Enable structured logging (bool)
LEX_MONITOR__LOGGING__FORMAT=  # Log format (json, text) (str)
LEX_MONITOR__LOGGING__INCLUDE_TRACE_CONTEXT=  # Include trace context in logs (bool)
LEX_MONITOR__LOGGING__LEVEL=  # Default log level (str)
LEX_MONITOR__LOGGING__REDACT_FIELDS=  # Fields to redact from logs (list[str])
LEX_MONITOR__METRICS__COLLECTION_INTERVAL=  # Metrics collection interval in seconds (float)
LEX_MONITOR__METRICS__DEFAULT_LABELS=  # Default labels for all metrics (dict[str, str])
LEX_MONITOR__METRICS__ENABLED=  # Enable metrics collection (bool)
LEX_MONITOR__METRICS__HISTOGRAM_BUCKETS=  # Default histogram bucket boundaries (list[float])
LEX_MONITOR__METRICS__PREFIX=  # MetricProtocol name prefix (str)
LEX_MONITOR__NAME=  # Provider name (str)
LEX_MONITOR__OPENTELEMETRY__BATCH_SIZE=  # Export batch size (int)
LEX_MONITOR__OPENTELEMETRY__COMPRESSION=  # Compression type (none, gzip) (str)
LEX_MONITOR__OPENTELEMETRY__ENDPOINT=  # OTLP endpoint URL (str | None)
LEX_MONITOR__OPENTELEMETRY__EXPORT_INTERVAL=  # Export interval seconds (float)
LEX_MONITOR__OPENTELEMETRY__HEADERS=  # OTLP request headers (dict[str, str])
LEX_MONITOR__OPENTELEMETRY__INSECURE=  # Use insecure connection (bool)
LEX_MONITOR__OPENTELEMETRY__METRICS_EXPORTERS=  # List of metrics exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__OPENTELEMETRY__TIMEOUT=  # Export timeout seconds (float)
LEX_MONITOR__OPENTELEMETRY__TRACING_EXPORTERS=  # List of tracing exporters to build. (list[OTelExporterConfig])
LEX_MONITOR__PROMETHEUS__ENABLE_DEFAULT_METRICS=  # Enable default process metrics (bool)
LEX_MONITOR__PROMETHEUS__METRICS_TABLE=  # Table name for metrics samples (str)
LEX_MONITOR__PROMETHEUS__PATH=  # Metrics endpoint path (str)
LEX_MONITOR__PROMETHEUS__PORT=  # Metrics server port (int)
LEX_MONITOR__PROMETHEUS__PUSHGATEWAY_URL=  # Pushgateway URL for push-based metrics (str | None)
LEX_MONITOR__PROMETHEUS__PUSH_INTERVAL=  # Push interval for Pushgateway (float)
LEX_MONITOR__PROMETHEUS__STORE_IN_DB=  # Persist metrics observations to DB (bool)
LEX_MONITOR__SLO__ALERT_CHANNELS=  # Alert channel names for SLO violation dispatch (list[str])
LEX_MONITOR__SLO__ENABLED=  # Enable periodic SLO evaluation worker (bool)
LEX_MONITOR__SLO__EVALUATION_INTERVAL=  # SLO evaluation interval in seconds (float)
LEX_MONITOR__SLO__SUPPRESSION_WINDOW_SECONDS=  # Alert suppression window in seconds (int)
LEX_MONITOR__TRACING__ENABLED=  # Enable tracing (bool)
LEX_MONITOR__TRACING__MAX_ATTRIBUTES=  # Max attributes per span (int)
LEX_MONITOR__TRACING__MAX_EVENTS=  # Max events per span (int)
LEX_MONITOR__TRACING__MAX_LINKS=  # Max links per span (int)
LEX_MONITOR__TRACING__MAX_SPANS=  # Max number of spans to keep in memory (int)
LEX_MONITOR__TRACING__MAX_TRACES_PER_SECOND=  # Max traces to sample per second (int)
LEX_MONITOR__TRACING__PROPAGATION_FORMATS=  # Propagation format list (list[str])
LEX_MONITOR__TRACING__SAMPLER_TYPE=  # Tracing sampler type (SamplerType)
LEX_MONITOR__TRACING__SAMPLE_RATE=  # Sample rate (0.0 to 1.0) (float)
LEX_MONITOR__TRACING__SERVICE_NAME=  # Service name for traces (str)

# ── lexigram-multimedia ──
LEX_MULTIMEDIA__BEAT__BACKEND=librosa  # — (Literal['librosa', 'madmom'])
LEX_MULTIMEDIA__BEAT__LIBROSA_SAMPLE_RATE=22050  # — (int)
LEX_MULTIMEDIA__BEAT__MADMOM_BASE_URL=http://localhost:5600  # — (str)
LEX_MULTIMEDIA__BEAT__MAX_ANALYZE_SAMPLES=60000000  # — (int)
LEX_MULTIMEDIA__BEAT__MAX_ASSET_BYTES=25 * 1024 * 1024  # — (int)
LEX_MULTIMEDIA__BEAT__TIMEOUT=30.0  # — (float)
LEX_MULTIMEDIA__CACHE_RESULTS=false  # — (bool)
LEX_MULTIMEDIA__IMAGE__BACKEND=local-http  # — (Literal['local-http', 'stability', 'openai', 'comfyui'])
LEX_MULTIMEDIA__IMAGE__COMFYUI_BASE_URL=http://localhost:8188  # — (str)
LEX_MULTIMEDIA__IMAGE__COMFYUI_CFG_SCALE=7.0  # — (float)
LEX_MULTIMEDIA__IMAGE__COMFYUI_CHECKPOINT=sd_xl_base_1.0.safetensors  # — (str)
LEX_MULTIMEDIA__IMAGE__COMFYUI_POLL_INTERVAL=1.0  # — (float)
LEX_MULTIMEDIA__IMAGE__COMFYUI_STEPS=20  # — (int)
LEX_MULTIMEDIA__IMAGE__COMFYUI_WORKFLOW_PATH=  # — (str | None)
LEX_MULTIMEDIA__IMAGE__LOCAL_HTTP_BASE_URL=http://localhost:5005  # — (str)
LEX_MULTIMEDIA__IMAGE__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__IMAGE__OPENAI_BASE_URL=https://api.openai.com  # — (str)
LEX_MULTIMEDIA__IMAGE__OPENAI_MODEL=dall-e-3  # — (str)
LEX_MULTIMEDIA__IMAGE__STABILITY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__IMAGE__TIMEOUT=60.0  # — (float)
LEX_MULTIMEDIA__INTERPOLATE__BACKEND=rife  # — (Literal['rife'])
LEX_MULTIMEDIA__INTERPOLATE__RIFE_BASE_URL=http://localhost:5500  # — (str)
LEX_MULTIMEDIA__INTERPOLATE__TIMEOUT=15.0  # — (float)
LEX_MULTIMEDIA__MUSIC__ACE_STEP_BASE_URL=http://localhost:5300  # — (str)
LEX_MULTIMEDIA__MUSIC__BACKEND=local-http  # — (Literal['local-http', 'stability-audio', 'ace-step', 'stable-audio-open'])
LEX_MULTIMEDIA__MUSIC__LOCAL_HTTP_BASE_URL=http://localhost:5003  # — (str)
LEX_MULTIMEDIA__MUSIC__STABILITY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__MUSIC__STABLE_AUDIO_OPEN_BASE_URL=http://localhost:5301  # — (str)
LEX_MULTIMEDIA__MUSIC__TIMEOUT=60.0  # — (float)
LEX_MULTIMEDIA__STORAGE_PATH_PREFIX=multimedia/  # — (str)
LEX_MULTIMEDIA__TTS__BACKEND=local-http  # — (Literal['local-http', 'elevenlabs', 'openai', 'chatterbox', 'kokoro', 'f5-tts', 'piper'])
LEX_MULTIMEDIA__TTS__CHATTERBOX_BASE_URL=http://localhost:5100  # — (str)
LEX_MULTIMEDIA__TTS__CHATTERBOX_CFG_WEIGHT=0.5  # — (float)
LEX_MULTIMEDIA__TTS__CHATTERBOX_EXAGGERATION=0.5  # — (float)
LEX_MULTIMEDIA__TTS__CHATTERBOX_TEMPERATURE=0.85  # — (float)
LEX_MULTIMEDIA__TTS__ELEVENLABS_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__TTS__ELEVENLABS_VOICE_ID=  # — (str | None)
LEX_MULTIMEDIA__TTS__F5_TTS_BASE_URL=http://localhost:5102  # — (str)
LEX_MULTIMEDIA__TTS__KOKORO_BASE_URL=http://localhost:5101  # — (str)
LEX_MULTIMEDIA__TTS__KOKORO_DEFAULT_VOICE=af_heart  # — (str)
LEX_MULTIMEDIA__TTS__LOCAL_HTTP_BASE_URL=http://localhost:5002  # — (str)
LEX_MULTIMEDIA__TTS__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__TTS__OPENAI_BASE_URL=https://api.openai.com  # — (str)
LEX_MULTIMEDIA__TTS__OPENAI_MODEL=tts-1  # — (str)
LEX_MULTIMEDIA__TTS__OPENAI_VOICE=alloy  # — (str)
LEX_MULTIMEDIA__TTS__PIPER_BASE_URL=http://localhost:5103  # — (str)
LEX_MULTIMEDIA__TTS__PIPER_DEFAULT_VOICE=en_US-lessac-medium  # — (str)
LEX_MULTIMEDIA__TTS__TIMEOUT=60.0  # — (float)
LEX_MULTIMEDIA__UPSCALE__BACKEND=real-esrgan  # — (Literal['real-esrgan', 'hat'])
LEX_MULTIMEDIA__UPSCALE__HAT_BASE_URL=http://localhost:5401  # — (str)
LEX_MULTIMEDIA__UPSCALE__REAL_ESRGAN_BASE_URL=http://localhost:5400  # — (str)
LEX_MULTIMEDIA__UPSCALE__TIMEOUT=30.0  # — (float)
LEX_MULTIMEDIA__VIDEO__BACKEND=local-http  # — (Literal['local-http', 'runway', 'openai', 'wan22', 'cogvideox', 'svd', 'comfyui'])
LEX_MULTIMEDIA__VIDEO__COGVIDEOX_BASE_URL=http://localhost:5201  # — (str)
LEX_MULTIMEDIA__VIDEO__COMFYUI_BASE_URL=http://localhost:8188  # — (str)
LEX_MULTIMEDIA__VIDEO__COMFYUI_CHECKPOINT=svd_xt_1_1.safetensors  # — (str)
LEX_MULTIMEDIA__VIDEO__COMFYUI_FPS=6  # — (int)
LEX_MULTIMEDIA__VIDEO__COMFYUI_MOTION_BUCKET_ID=127  # — (int)
LEX_MULTIMEDIA__VIDEO__COMFYUI_POLL_INTERVAL=1.0  # — (float)
LEX_MULTIMEDIA__VIDEO__COMFYUI_WORKFLOW_PATH=  # — (str | None)
LEX_MULTIMEDIA__VIDEO__LOCAL_HTTP_BASE_URL=http://localhost:5004  # — (str)
LEX_MULTIMEDIA__VIDEO__OPENAI_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__VIDEO__OPENAI_BASE_URL=https://api.openai.com  # — (str)
LEX_MULTIMEDIA__VIDEO__OPENAI_MODEL=sora-2  # — (str)
LEX_MULTIMEDIA__VIDEO__PROCESSING__FFMPEG_BINARY=ffmpeg  # — (str)
LEX_MULTIMEDIA__VIDEO__PROCESSING__MAX_ASSET_BYTES=25 * 1024 * 1024  # — (int)
LEX_MULTIMEDIA__VIDEO__PROCESSING__MAX_CONCURRENT_JOBS=2  # — (int)
LEX_MULTIMEDIA__VIDEO__PROCESSING__TEMP_DIR=  # — (str | None)
LEX_MULTIMEDIA__VIDEO__PROCESSING__TIMEOUT=300.0  # — (float)
LEX_MULTIMEDIA__VIDEO__RUNWAY_API_KEY_SECRET_NAME=<change-me-in-production>  # — (str)
LEX_MULTIMEDIA__VIDEO__SVD_BASE_URL=http://localhost:5202  # — (str)
LEX_MULTIMEDIA__VIDEO__TIMEOUT=  # — (float | None)
LEX_MULTIMEDIA__VIDEO__WAN22_BASE_URL=http://localhost:5200  # — (str)

# ── lexigram-nosql ──
LEX_NOSQL__BACKENDS=  # Named NoSQL backends for multi-store support. When non-empty, the provider re... (list[NamedNoSQLConfig])
LEX_NOSQL__DRIVER=mongodb  # NoSQL driver name (str)
LEX_NOSQL__ENABLED=true  # Enable NoSQL support (bool)
LEX_NOSQL__FIRESTORE__CREDENTIALS_JSON=  # Path to a service account JSON key file, or the raw JSON string. When ``None`... (str | None)
LEX_NOSQL__FIRESTORE__DATABASE_ID=  # Firestore database ID (use '(default)' for the default database) (str)
LEX_NOSQL__FIRESTORE__PROJECT_ID=  # Google Cloud project ID (str)
LEX_NOSQL__MONGODB__AUTH_SOURCE=admin  # Authentication database (str)
LEX_NOSQL__MONGODB__CONNECT_TIMEOUT_MS=10000  # Connection timeout (ms) (int)
LEX_NOSQL__MONGODB__DATABASE=lexigram  # Database name (str)
LEX_NOSQL__MONGODB__MAX_POOL_SIZE=100  # Maximum connection pool size (int)
LEX_NOSQL__MONGODB__MIN_POOL_SIZE=10  # Minimum connection pool size (int)
LEX_NOSQL__MONGODB__READ_PREFERENCE=primaryPreferred  # Read preference mode (str)
LEX_NOSQL__MONGODB__RETRY_READS=true  # Enable read retries (bool)
LEX_NOSQL__MONGODB__RETRY_WRITES=true  # Enable write retries (bool)
LEX_NOSQL__MONGODB__SERVER_SELECTION_TIMEOUT_MS=5000  # Server selection timeout (ms) (int)
LEX_NOSQL__MONGODB__SOCKET_TIMEOUT_MS=30000  # Socket timeout (ms) (int)
LEX_NOSQL__MONGODB__URI=mongodb://localhost:27017  # MongoDB connection URI (str)
LEX_NOSQL__MONGODB__WRITE_CONCERN_W=majority  # Write concern level (str | int)

# ── lexigram-notification ──
LEX_NOTIFICATION__PUSH_BACKENDS=  # Named push notification backends for multi-backend support. When non-empty, t... (list[NamedPushConfig])
LEX_NOTIFICATION__SMS_BACKENDS=  # Named SMS backends for multi-backend support. When non-empty, the provider re... (list[NamedSMSConfig])

# ── lexigram-queue ──
LEX_QUEUE__BACKENDS=  # Named queue backends for multi-backend support. When non-empty, the provider ... (list[NamedQueueConfig])

# ── lexigram-resilience ──
LEX_RESILIENCE__BULKHEAD__MAX_CONCURRENT=10  # Max concurrent requests (int)
LEX_RESILIENCE__BULKHEAD__NAME=  # Bulkhead name (str)
LEX_RESILIENCE__BULKHEAD__QUEUE_SIZE=100  # Max queue size (int)
LEX_RESILIENCE__BULKHEAD__TIMEOUT=30.0  # Execution timeout (float)
LEX_RESILIENCE__CIRCUIT_BREAKER=  # — (CircuitBreakerConfig)
LEX_RESILIENCE__RETRY__ABORT_IF=  # — (Callable[[Any], bool] | None)
LEX_RESILIENCE__RETRY__ABORT_ON=  # — (tuple[type[Exception], ...])
LEX_RESILIENCE__RETRY__BACKOFF_FACTOR=2.0  # — (float)
LEX_RESILIENCE__RETRY__BASE_DELAY=1.0  # — (float)
LEX_RESILIENCE__RETRY__IDEMPOTENT_METHODS_ONLY=true  # — (bool)
LEX_RESILIENCE__RETRY__JITTER=true  # — (bool | float)
LEX_RESILIENCE__RETRY__MAX_ATTEMPTS=3  # — (int)
LEX_RESILIENCE__RETRY__MAX_DELAY=60.0  # — (float)
LEX_RESILIENCE__RETRY__ON_RETRY=  # — (Callable[[int, Exception | None], None] | None)
LEX_RESILIENCE__RETRY__RETRY_IF=  # — (Callable[[Exception], bool] | None)
LEX_RESILIENCE__RETRY__RETRY_ON=  # — (tuple[type[Exception], ...])
LEX_RESILIENCE__RETRY__RETRY_ON_RESULT=  # — (Callable[[Any], bool] | None)
LEX_RESILIENCE__RETRY__RETRY_SYNC=false  # — (bool)
LEX_RESILIENCE__TIMEOUT__TIMEOUT=30.0  # — (float)
LEX_RESILIENCE__TIMEOUT__TIMEOUT_MESSAGE=Operation timed out  # — (str)

# ── lexigram-search ──
LEX_SEARCH__BACKENDS=  # Named search backends for multi-backend support. When non-empty, the provider... (list[NamedSearchConfig])
LEX_SEARCH__BACKEND_TYPE=  # Search backend type (BackendType)
LEX_SEARCH__DATABASE=  # Named database to use for DB-backed backends (postgres/mysql). References a n... (str | None)
LEX_SEARCH__ELASTICSEARCH__API_KEY=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__ELASTICSEARCH__HOSTS=  # Elasticsearch hosts (list[str])
LEX_SEARCH__ELASTICSEARCH__INDEX_PREFIX=lexigram_search_  # — (str)
LEX_SEARCH__ELASTICSEARCH__NUMBER_OF_REPLICAS=0  # — (int)
LEX_SEARCH__ELASTICSEARCH__NUMBER_OF_SHARDS=1  # — (int)
LEX_SEARCH__ELASTICSEARCH__PASSWORD=<change-me-in-production>  # — (SecretStr | None)
LEX_SEARCH__ELASTICSEARCH__USERNAME=  # — (str | None)
LEX_SEARCH__ELASTICSEARCH__USE_SSL=false  # — (bool)
LEX_SEARCH__ELASTICSEARCH__VERIFY_CERTS=true  # — (bool)
LEX_SEARCH__ENABLED=true  # Enable the search subsystem (bool)
LEX_SEARCH__MEILISEARCH__API_KEY=<change-me-in-production>  # MeiliSearch API key (SecretStr | None)
LEX_SEARCH__MEILISEARCH__DISPLAYED_ATTRIBUTES=  # Fields to return in results (list[str])
LEX_SEARCH__MEILISEARCH__FILTERABLE_ATTRIBUTES=  # Attributes that can be filtered (list[str])
LEX_SEARCH__MEILISEARCH__MAX_CONNECTIONS=10  # Maximum number of connections (int)
LEX_SEARCH__MEILISEARCH__MIN_WORD_SIZE_FOR_TYPOS=  # Minimum word size for typo tolerance (dict[str, int])
LEX_SEARCH__MEILISEARCH__RANKING_RULES=  # Ranking rules in order (list[str])
LEX_SEARCH__MEILISEARCH__SEARCHABLE_ATTRIBUTES=  # Fields to search in (list[str])
LEX_SEARCH__MEILISEARCH__SORTABLE_ATTRIBUTES=  # Attributes that can be sorted (list[str])
LEX_SEARCH__MEILISEARCH__TIMEOUT=30  # Request timeout in seconds (int)
LEX_SEARCH__MEILISEARCH__TYPO_TOLERANCE_ENABLED=true  # Enable typo tolerance (bool)
LEX_SEARCH__MEILISEARCH__URL=http://localhost:7700  # MeiliSearch server URL (str)
LEX_SEARCH__MONGO__CONNECTION_STRING=  # — (SecretStr)
LEX_SEARCH__MONGO__DATABASE_NAME=search  # — (str)
LEX_SEARCH__MONGO__USE_ATLAS_SEARCH=false  # — (bool)
LEX_SEARCH__MYSQL__CONNECTION_STRING=  # — (SecretStr)
LEX_SEARCH__MYSQL__FULLTEXT_MODE=natural_language  # — (str)
LEX_SEARCH__MYSQL__MIN_WORD_LENGTH=3  # — (int)
LEX_SEARCH__OPENSEARCH__HOSTS=  # OpenSearch hosts (list[str])
LEX_SEARCH__OPENSEARCH__INDEX_PREFIX=lexigram_search_  # — (str)
LEX_SEARCH__OPENSEARCH__PASSWORD=<change-me-in-production>  # — (str | None)
LEX_SEARCH__OPENSEARCH__TIMEOUT=30  # — (int)
LEX_SEARCH__OPENSEARCH__USERNAME=  # — (str | None)
LEX_SEARCH__OPENSEARCH__USE_SSL=false  # — (bool)
LEX_SEARCH__OPENSEARCH__VERIFY_SSL=true  # — (bool)
LEX_SEARCH__OPERATIONS__BULK_CHUNK_SIZE=  # Bulk request chunk size (int)
LEX_SEARCH__OPERATIONS__MAX_RETRIES=  # Max retry attempts (int)
LEX_SEARCH__OPERATIONS__REQUEST_TIMEOUT=  # Request timeout seconds (float)
LEX_SEARCH__OPERATIONS__RETRY_BACKOFF=  # Retry backoff multiplier (float)
LEX_SEARCH__POSTGRES__AUTO_CREATE_TABLES=true  # — (bool)
LEX_SEARCH__POSTGRES__CONNECTION_STRING=  # PostgreSQL connection string (SecretStr)
LEX_SEARCH__POSTGRES__ENABLE_TRIGRAM=true  # Enable pg_trgm fuzzy matching (bool)
LEX_SEARCH__POSTGRES__TEXT_SEARCH_CONFIG=english  # PostgreSQL text search config (str)
LEX_SEARCH__QUERY__DEFAULT_LIMIT=  # — (int)
LEX_SEARCH__QUERY__ENABLE_AGGREGATIONS=false  # — (bool)
LEX_SEARCH__QUERY__ENABLE_FACETING=true  # — (bool)
LEX_SEARCH__QUERY__ENABLE_HIGHLIGHTING=true  # — (bool)
LEX_SEARCH__QUERY__FUZZY_THRESHOLD=0.8  # — (float)
LEX_SEARCH__QUERY__MAX_LIMIT=  # — (int)
LEX_SEARCH__QUERY__STRATEGY=fuzzy  # — (str)
LEX_SEARCH__SQLITE__AUTO_CREATE_TABLES=true  # — (bool)
LEX_SEARCH__SQLITE__DB_PATH=:memory:  # — (str)
LEX_SEARCH__SQLITE__TOKENIZER=porter unicode61  # — (str)
LEX_SEARCH__TIMEOUT=  # Default request timeout seconds (float)
LEX_SEARCH__TYPESENSE__API_KEY=<change-me-in-production>  # Typesense API key (SecretStr | None)
LEX_SEARCH__TYPESENSE__CONNECTION_TIMEOUT=30  # Connection timeout (int)
LEX_SEARCH__TYPESENSE__HEALTH_CHECK_INTERVAL=60  # Health check interval (int)
LEX_SEARCH__TYPESENSE__NODES=  # Typesense node connections (list[dict[str, str]])

# ── lexigram-secrets ──
LEX_SECRETS__AUDIT_ACTOR_ID=<change-me-in-production>  # Actor identifier for audit log entries (str)
LEX_SECRETS__BACKEND_OPTIONS=<change-me-in-production>  # Keyword arguments for backend constructor (dict)
LEX_SECRETS__BACKEND_TYPE=<change-me-in-production>  # Backend store type (memory, vault, ...) (str)
LEX_SECRETS__ENABLED=<change-me-in-production>  # Whether secrets subsystem is enabled (bool)
LEX_SECRETS__MAX_AGE_SECONDS=<change-me-in-production>  # Seconds before automatic rotation (float)
LEX_SECRETS__NAME=<change-me-in-production>  # Configuration name (str)
LEX_SECRETS__TENANT_ID=<change-me-in-production>  # Optional tenant namespace (str | None)
LEX_SECRETS__WARNING_BEFORE_SECONDS=<change-me-in-production>  # Seconds before expiry to warn (float)

# ── lexigram-sql ──
LEX_SQL__AUDIT_HMAC_KEY=change-me-in-production  # HMAC key for audit checksum signing. Plain text or base64. (str | None)
LEX_SQL__BACKENDS=  # Multi-database backends list. When non-empty, drives multi-DB mode. The entry... (list[NamedDatabaseConfig])
LEX_SQL__BACKEND__URL=postgresql://lexigram:lexigram@localhost:5432/lexigram  # Database connection URL (may contain credentials) (SecretStr)
LEX_SQL__ENABLED=true  # — (bool)
LEX_SQL__MIGRATIONS__LOCK_TIMEOUT=30s  # — (Duration)
LEX_SQL__NAME=database  # — (str)
LEX_SQL__OPERATIONS__ECHO=false  # — (bool)
LEX_SQL__OPERATIONS__STATEMENT_TIMEOUT=60s  # — (Duration)
LEX_SQL__OUTBOX__BATCH_MAX_AGE=30s  # — (Duration)
LEX_SQL__OUTBOX__ENABLED=true  # — (bool)
LEX_SQL__OUTBOX__POLL_INTERVAL=5s  # — (Duration)
LEX_SQL__POOL__ACQUIRE_TIMEOUT=30s  # — (Duration)
LEX_SQL__POOL__IDLE_TIMEOUT=5m  # — (Duration)
LEX_SQL__POOL__MAX_LIFETIME=1h  # — (Duration)
LEX_SQL__POOL__MAX_OVERFLOW=5  # — (int)
LEX_SQL__POOL__MAX_SIZE=  # — (int)
LEX_SQL__POOL__MIN_SIZE=  # — (int)
LEX_SQL__POOL__RECYCLE=3600  # — (int)
LEX_SQL__POOL__TIMEOUT=  # — (float)

# ── lexigram-storage ──
LEX_STORAGE__BACKENDS=  # Named storage backends for multi-store support. When non-empty, the provider ... (list[NamedStorageConfig])
LEX_STORAGE__DEFAULT_DRIVER=  # Default storage driver to use (Literal['local', 's3', 'gcs', 'azure', 'memory', 'r2'])
LEX_STORAGE__DRIVERS=  # Driver-specific configurations (dict[str, StorageLocalConfig | StorageS3Config | StorageGCSConfig | StorageAzureConfig | StorageMemoryConfig | StorageR2Config])
LEX_STORAGE__ENABLED=true  # — (bool)
LEX_STORAGE__ENV=  # Environment (development/staging/production) (str | None)
LEX_STORAGE__HEALTH_CHECK_TIMEOUT=5.0  # Timeout in seconds for the startup health check in StorageProvider.boot() (float)
LEX_STORAGE__NAME=storage  # — (str)
LEX_STORAGE__SERVICE__ALLOWED_MIME_TYPES=  # Allowed MIME types for upload validation. Defaults to a safe set of common im... (list[str])
LEX_STORAGE__SERVICE__MAX_FILE_SIZE_MB=  # Maximum file size in MB (int)

# ── lexigram-tasks ──
LEX_TASKS__BACKENDS=  # Named task queue backends for multi-queue support. When non-empty, the provid... (list[NamedTaskConfig])
LEX_TASKS__BACKEND__AMQP_URL=  # AMQP connection URL (may contain credentials). (SecretStr)
LEX_TASKS__BACKEND__POSTGRES_DSN=  # Postgres DSN (required when type="postgres"; may contain credentials). (SecretStr | None)
LEX_TASKS__BACKEND__QUEUE_NAME=  # Name of the task queue (str)
LEX_TASKS__BACKEND__REDIS_URL=redis://localhost:6379/0  # Redis connection URL (may contain credentials). (SecretStr)
LEX_TASKS__BACKEND__TYPE=  # Queue backend type (str)
LEX_TASKS__ENABLED=true  # Whether tasks module is enabled (bool)
LEX_TASKS__ENV=  # Environment (development/staging/production) (str | None)
LEX_TASKS__EXTRA=  # — (dict[str, Any])
LEX_TASKS__NAME=tasks  # Configuration name (str)
LEX_TASKS__RATE_LIMIT__BURST=  # Maximum burst size (int | None)
LEX_TASKS__RATE_LIMIT__ENABLED=false  # Whether rate limiting is enabled (bool)
LEX_TASKS__RATE_LIMIT__PER=1.0  # Time period in seconds (float)
LEX_TASKS__RATE_LIMIT__RATE=100  # Number of tasks allowed per time period (int)
LEX_TASKS__RETRY__ABORT_IF=  # — (Callable[[Any], bool] | None)
LEX_TASKS__RETRY__ABORT_ON=  # — (tuple[type[Exception], ...])
LEX_TASKS__RETRY__BACKOFF_FACTOR=2.0  # — (float)
LEX_TASKS__RETRY__BASE_DELAY=1.0  # — (float)
LEX_TASKS__RETRY__IDEMPOTENT_METHODS_ONLY=true  # — (bool)
LEX_TASKS__RETRY__JITTER=true  # — (bool | float)
LEX_TASKS__RETRY__MAX_ATTEMPTS=3  # — (int)
LEX_TASKS__RETRY__MAX_DELAY=60.0  # — (float)
LEX_TASKS__RETRY__ON_RETRY=  # — (Callable[[int, Exception | None], None] | None)
LEX_TASKS__RETRY__RETRY_IF=  # — (Callable[[Exception], bool] | None)
LEX_TASKS__RETRY__RETRY_ON=  # — (tuple[type[Exception], ...])
LEX_TASKS__RETRY__RETRY_ON_RESULT=  # — (Callable[[Any], bool] | None)
LEX_TASKS__RETRY__RETRY_SYNC=false  # — (bool)
LEX_TASKS__SCHEDULER__CHECK_INTERVAL=  # Interval between schedule checks (seconds) (float)
LEX_TASKS__SCHEDULER__ENABLED=true  # Whether scheduling is enabled (bool)
LEX_TASKS__SCHEDULER__TIMEZONE=  # Timezone for cron expressions (str)
LEX_TASKS__TIMEOUT__DEFAULT_TIMEOUT=  # Default timeout (float)
LEX_TASKS__TIMEOUT__ENFORCE_TIMEOUT=true  # Enforce timeouts (bool)
LEX_TASKS__TIMEOUT__MAX_TIMEOUT=  # Maximum allowed timeout (float)
LEX_TASKS__WORKER__DEFAULT_TIMEOUT=  # Default timeout for tasks without an explicit timeout (seconds) (float)
LEX_TASKS__WORKER__ENFORCE_TIMEOUT=true  # Whether to enforce timeouts on all tasks (bool)
LEX_TASKS__WORKER__MAX_CONCURRENT_TASKS=  # Maximum concurrent tasks per worker (int)
LEX_TASKS__WORKER__MAX_TIMEOUT=  # Maximum allowed timeout for any task (seconds) (float)
LEX_TASKS__WORKER__POLL_INTERVAL=  # Interval between queue polls (seconds) (float)
LEX_TASKS__WORKER__SHUTDOWN_TIMEOUT=  # Timeout for graceful shutdown (seconds) (float)
LEX_TASKS__WORKER__WORKER_COUNT=  # Number of worker instances (int)

# ── lexigram-tenancy ──
LEX_TENANCY__INTEGRATION__CACHE_KEY_PREFIX=true  # — (bool)
LEX_TENANCY__INTEGRATION__SQL_CONTEXT_BRIDGE=true  # — (bool)
LEX_TENANCY__LIFECYCLE__AUTO_PROVISION_ISOLATION=true  # — (bool)
LEX_TENANCY__LIFECYCLE__ISOLATION_STRATEGY=row_level  # — (str)
LEX_TENANCY__OVERRIDES__CACHE_TTL=DEFAULT_CONFIG_CACHE_TTL  # — (int)
LEX_TENANCY__RESOLUTION__HEADER_NAME=DEFAULT_HEADER_NAME  # — (str)
LEX_TENANCY__RESOLUTION__JWT_CLAIM_KEY=DEFAULT_JWT_CLAIM_KEY  # — (str)
LEX_TENANCY__RESOLUTION__PATH_PATTERN=DEFAULT_PATH_PATTERN  # — (str | None)
LEX_TENANCY__RESOLUTION__RESOLVERS=  # — (list[str])
LEX_TENANCY__RESOLUTION__STRICT_MEMBERSHIP=true  # — (bool)
LEX_TENANCY__RESOLUTION__SUBDOMAIN_PATTERN=  # — (str | None)
LEX_TENANCY__RESOLUTION__TRUSTED_RESOLVERS=  # — (list[str])
LEX_TENANCY__RESOLUTION__VALIDATOR_CACHE_TTL=DEFAULT_VALIDATOR_CACHE_TTL  # — (int)

# ── lexigram-testing ──
LEX_TEST_ELASTICSEARCH_URL=  # — (str)
LEX_TEST_KAFKA_BOOTSTRAP=  # — (str)
LEX_TEST_MINIO_ACCESS_KEY=  # — (str)
LEX_TEST_MINIO_ENDPOINT=  # — (str)
LEX_TEST_MINIO_SECRET_KEY=<change-me-in-production>  # — (str)
LEX_TEST_MONGODB_DSN=  # — (str)
LEX_TEST_NEO4J_AUTH=  # — (str)
LEX_TEST_NEO4J_URL=  # — (str)
LEX_TEST_POSTGRES_DSN=  # — (str)
LEX_TEST_POSTGRES_DSN_RAW=  # — (str)
LEX_TEST_QDRANT_URL=  # — (str)
LEX_TEST_REDIS_URL=redis://localhost:6379/0  # — (str)

# ── lexigram-ui ──
LEX_UI__AUTO_ESCAPE=true  # HTML-escape user strings by default. (bool)
LEX_UI__DEBUG_COMPONENTS=false  # Render data-component debug attributes. (bool)
LEX_UI__DEFAULT_THEME=default  # Default CSS theme name. (str)
LEX_UI__ENABLE_REALTIME=false  # Enable realtime update features. (bool)
LEX_UI__ENABLE_SSE=false  # Enable Server-Sent Events support. (bool)
LEX_UI__HTMX_VERSION=2.0.4  # HTMX CDN version. (str)
LEX_UI__THEME=light  # Active UI theme. (str)

# ── lexigram-vector ──
LEX_VECTOR__BACKEND=  # Vector store backend to use (str)
LEX_VECTOR__BACKENDS=  # Named vector store backends for multi-store support. When non-empty, the prov... (list[NamedVectorConfig])
LEX_VECTOR__CACHE_TTL=86400  # Cache TTL in seconds (default: 24 hours) (int)
LEX_VECTOR__COLLECTION_NAME=default  # Default collection name for AI-layer operations (str)
LEX_VECTOR__DEFAULT_DIMENSION=1536  # Default vector dimension (int)
LEX_VECTOR__DEFAULT_DISTANCE_METRIC=DistanceMetric.COSINE  # Default distance metric for new collections (DistanceMetric)
LEX_VECTOR__DEFAULT_INDEX_TYPE=IndexType.HNSW  # Default index type for new collections (IndexType)
LEX_VECTOR__EMBEDDING_MODEL=text-embedding-3-small  # Embedding model name for AI-layer embedding generation (str)
LEX_VECTOR__ENABLED=true  # Enable the vector store subsystem (bool)
LEX_VECTOR__ENABLE_CACHE=false  # Enable embedding caching (requires a CacheBackend binding) (bool)
LEX_VECTOR__MAX_RETRIES=  # Maximum number of retries for operations (int)
LEX_VECTOR__MEMORY__MAX_COLLECTIONS=100  # Maximum number of collections in memory (int)
LEX_VECTOR__MEMORY__MAX_VECTORS_PER_COLLECTION=100000  # Maximum number of vectors per collection (int)
LEX_VECTOR__PGVECTOR__CREATE_EXTENSION=true  # Whether to create pgvector extension if missing (bool)
LEX_VECTOR__PGVECTOR__DATABASE=primary  # Name of the database backend from db.backends to use for pgvector. Matches a ... (str)
LEX_VECTOR__PGVECTOR__DEFAULT_EF_SEARCH=  # Default ef_search for HNSW index (int)
LEX_VECTOR__PGVECTOR__DEFAULT_LISTS=  # Default number of lists for IVFFlat index (int)
LEX_VECTOR__PGVECTOR__DEFAULT_PROBES=  # Default number of probes for IVFFlat index (int)
LEX_VECTOR__PGVECTOR__SCHEMA=public  # Database schema for vector tables (str)
LEX_VECTOR__PGVECTOR__TABLE_PREFIX=vec_  # Prefix for vector storage tables (str)
LEX_VECTOR__PINECONE__API_KEY=<change-me-in-production>  # Pinecone API key (SecretStr)
LEX_VECTOR__PINECONE__ENVIRONMENT=  # Pinecone environment (e.g. 'us-west1-gcp') (str)
LEX_VECTOR__PINECONE__INDEX_NAME=  # Name of the Pinecone index (str)
LEX_VECTOR__PINECONE__NAMESPACE=  # Default namespace for the index (str)
LEX_VECTOR__PINECONE__POOL_THREADS=4  # Number of threads for the connection pool (int)
LEX_VECTOR__PINECONE__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__QDRANT__API_KEY=<change-me-in-production>  # Qdrant API key (SecretStr | None)
LEX_VECTOR__QDRANT__GRPC_PORT=6334  # gRPC port for Qdrant (int)
LEX_VECTOR__QDRANT__PREFER_GRPC=true  # Whether to prefer gRPC over HTTP (bool)
LEX_VECTOR__QDRANT__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__QDRANT__URL=http://localhost:6333  # Qdrant server URL (str)
LEX_VECTOR__RETRY_DELAY=  # Delay between retries in seconds (float)
LEX_VECTOR__TENANCY__ENABLED=false  # Enable tenant-aware collection name resolution (bool)
LEX_VECTOR__TENANCY__RESOLVER_KIND=templated  # Which ``TenantCollectionResolver`` to use. One of ``"templated"`` or ``"pinec... (str)
LEX_VECTOR__UPSERT_BATCH_SIZE=  # Number of vectors per upsert batch (int)
LEX_VECTOR__WEAVIATE__API_KEY=<change-me-in-production>  # Weaviate API key for authenticated clusters (SecretStr | None)
LEX_VECTOR__WEAVIATE__GRPC_PORT=50051  # gRPC port for the Weaviate cluster (int)
LEX_VECTOR__WEAVIATE__TIMEOUT=  # Request timeout in seconds (float)
LEX_VECTOR__WEAVIATE__URL=http://localhost:8080  # Weaviate cluster URL (HTTP) (str)

# ── lexigram-web ──
LEX_WEB__API_DOCS__ENABLED=true  # Enable API documentation endpoints (/docs, /redoc) and auto-configure CSP for... (bool)
LEX_WEB__API_DOCS__PROVIDER=both  # Documentation provider: 'swagger', 'redoc', or 'both' (str)
LEX_WEB__AUTH_EXCLUDE_PATHS=  # Paths to exclude from authentication (list[str])
LEX_WEB__COMPRESSION_ENABLED=true  # — (bool)
LEX_WEB__CORS=  # — (CORSConfig)
LEX_WEB__DEBUG_ROUTES=false  # Enable debug routes (bool)
LEX_WEB__DEBUG_ROUTES_TOKEN=<change-me-in-production>  # Token required to access debug routes (sent as X-Debug-Token header). (SecretStr | None)
LEX_WEB__ENABLED=true  # — (bool)
LEX_WEB__ENABLE_AUTH=false  # Enable built-in authentication middleware. Requires authenticators to be regi... (bool)
LEX_WEB__ENABLE_DEBUG_ROUTES_ENV_GATE=false  # Require explicit opt-in for debug route registration. (bool)
LEX_WEB__ENABLE_IDENTITY_RESOLUTION=false  # Automatically resolve OAuth external IDs to internal UUIDs in authenticated r... (bool)
LEX_WEB__ENV=  # Environment (development/staging/production) (str | None)
LEX_WEB__MAX_BODY_SIZE=10 * 1024 * 1024  # Maximum allowed request body size in bytes. Requests with a Content-Length he... (int | None)
LEX_WEB__NAME=web  # — (str)
LEX_WEB__OPENAPI_TITLE=API  # OpenAPI Title (str)
LEX_WEB__OPENAPI_URL=  # — (str | None)
LEX_WEB__OPENAPI_VERSION=1.0.0  # OpenAPI Version (str)
LEX_WEB__RATE_LIMIT__DEFAULT_LIMIT=  # Max requests per window (int)
LEX_WEB__RATE_LIMIT__DEFAULT_WINDOW=  # Window size in seconds (int)
LEX_WEB__RATE_LIMIT__ENABLED=true  # Enable rate limiting. When true, RateLimitMiddleware enforces the matched per... (bool)
LEX_WEB__RATE_LIMIT__RULES=  # Per-path rate limit rules; longest-prefix match wins (dict[str, RateLimitRuleConfig])
LEX_WEB__RATE_LIMIT__STORAGE_BACKEND=memory  # Storage backend (memory/redis) (str)
LEX_WEB__RATE_LIMIT__WHITELIST_IPS=  # Exempt IP addresses (list[str])
LEX_WEB__REDOC_JS_URL=  # — (str | None)
LEX_WEB__REDOC_URL=/redoc  # — (str | None)
LEX_WEB__ROLE_GUARD__RULES=  # Role guard rules in declaration order (list[RoleGuardRuleConfig])
LEX_WEB__SECURITY__ALLOWED_HOSTS=  # Hostnames permitted to reach the application. Empty by default; must be confi... (list[str])
LEX_WEB__SECURITY__CORS__ALLOWED_ORIGINS=  # Allowed origins (use ['*'] to allow all) (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_CREDENTIALS=<change-me-in-production>  # — (bool)
LEX_WEB__SECURITY__CORS__ALLOW_HEADERS=  # — (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_METHODS=  # — (list[str])
LEX_WEB__SECURITY__CORS__ALLOW_ORIGIN_REGEX=  # Regex pattern for allowed origins (matched when not in allowed_origins) (str | None)
LEX_WEB__SECURITY__CORS__DEBUG_PERMISSIVE=false  # When True and debug mode is active, allow any origin via wildcard (explicit o... (bool)
LEX_WEB__SECURITY__CORS__ENABLED=true  # Enable CORS (bool)
LEX_WEB__SECURITY__CORS__EXPOSE_HEADERS=  # — (list[str])
LEX_WEB__SECURITY__CORS__MAX_AGE=600  # — (int)
LEX_WEB__SECURITY__CROSS_ORIGIN__EMBEDDER_POLICY=require-corp  # Cross-Origin-Embedder-Policy header value (str)
LEX_WEB__SECURITY__CROSS_ORIGIN__ENABLED=false  # Emit cross-origin isolation headers (bool)
LEX_WEB__SECURITY__CROSS_ORIGIN__OPENER_POLICY=same-origin  # Cross-Origin-Opener-Policy header value (str)
LEX_WEB__SECURITY__CROSS_ORIGIN__RESOURCE_POLICY=same-origin  # Cross-Origin-Resource-Policy header value (str)
LEX_WEB__SECURITY__CSP__ENABLED=true  # Emit the Content-Security-Policy header (bool)
LEX_WEB__SECURITY__CSRF__COOKIE_DOMAIN=  # — (str | None)
LEX_WEB__SECURITY__CSRF__COOKIE_HTTPONLY=true  # — (bool)
LEX_WEB__SECURITY__CSRF__COOKIE_NAME=csrf_token  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_PATH=/  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_SAMESITE=Lax  # — (str)
LEX_WEB__SECURITY__CSRF__COOKIE_SECURE=true  # — (bool)
LEX_WEB__SECURITY__CSRF__ENABLED=false  # — (bool)
LEX_WEB__SECURITY__CSRF__EXCLUDED_PATHS=  # URL path prefixes exempt from CSRF validation for cookie-less requests; cooki... (list[str])
LEX_WEB__SECURITY__CSRF__EXCLUDE_AUTH_SCHEMES=  # Authorization header schemes that bypass CSRF validation (explicit opt-in). (list[str])
LEX_WEB__SECURITY__CSRF__EXCLUDE_CONTENT_TYPES=  # Content-Type values that bypass CSRF validation (explicit opt-in — JSON reque... (list[str])
LEX_WEB__SECURITY__CSRF__HEADER_NAME=X-CSRF-Token  # — (str)
LEX_WEB__SECURITY__CSRF__SECRET_KEY=change-me-in-production  # HMAC secret used to sign and verify CSRF tokens (populated via LEX_WEB__SECUR... (SecretStr | None)
LEX_WEB__SECURITY__CSRF__TOKEN_LENGTH=32  # — (int)
LEX_WEB__SECURITY__CSRF__TOKEN_TTL=3600  # TTL in seconds for synchronizer-mode tokens stored in cache. (int)
LEX_WEB__SECURITY__CUSTOM_HEADERS=  # Additional HTTP response headers emitted verbatim (dict[str, str])
LEX_WEB__SECURITY__ENABLED=true  # Enable the security subsystem (bool)
LEX_WEB__SECURITY__ENABLE_CORS=true  # — (bool)
LEX_WEB__SECURITY__ENABLE_CSRF=true  # — (bool)
LEX_WEB__SECURITY__HEADERS__CONTENT_TYPE_NOSNIFF=true  # — (bool)
LEX_WEB__SECURITY__HEADERS__CSP=  # — (str | None)
LEX_WEB__SECURITY__HEADERS__FRAME_OPTIONS=DENY  # — (str)
LEX_WEB__SECURITY__HEADERS__HSTS_INCLUDE_SUBDOMAINS=true  # — (bool)
LEX_WEB__SECURITY__HEADERS__HSTS_MAX_AGE=31536000  # — (int)
LEX_WEB__SECURITY__HEADERS__PERMISSIONS_POLICY=  # — (str | None)
LEX_WEB__SECURITY__HEADERS__REFERRER_POLICY=strict-origin-when-cross-origin  # — (str)
LEX_WEB__SECURITY__HEADERS__XSS_PROTECTION=1; mode=block  # — (str)
LEX_WEB__SECURITY__HSTS__ENABLED=false  # Emit the Strict-Transport-Security header (bool)
LEX_WEB__SECURITY__HSTS__INCLUDE_SUBDOMAINS=true  # Apply HSTS to all subdomains (bool)
LEX_WEB__SECURITY__HSTS__MAX_AGE=31536000  # HSTS max-age in seconds (default 1 year) (int)
LEX_WEB__SECURITY__HSTS__PRELOAD=false  # Include site in HSTS preload list (bool)
LEX_WEB__SECURITY__PERMISSIONS_POLICY=  # Permissions-Policy directive map (dict[str, str])
LEX_WEB__SECURITY__REFERRER_POLICY=strict-origin-when-cross-origin  # Referrer-Policy header value (str)
LEX_WEB__SERVER__DEBUG=false  # Enable debug mode (bool)
LEX_WEB__SERVER__HOST=  # Bind host (str)
LEX_WEB__SERVER__PORT=  # Bind port (int)
LEX_WEB__SERVER__RELOAD=  # Enable auto-reload (bool)
LEX_WEB__SERVER__WORKERS=  # Number of workers (int)
LEX_WEB__STATIC__DIRECTORY=static  # Directory to serve (str)
LEX_WEB__STATIC__ENABLED=false  # Enable static file serving (bool)
LEX_WEB__STATIC__HTML=false  # Serve HTML files (SPA mode) (bool)
LEX_WEB__STATIC__PREFIX=/static  # URL prefix for static files (str)
LEX_WEB__SWAGGER_CSS_URL=  # — (str | None)
LEX_WEB__SWAGGER_JS_URL=  # — (str | None)
LEX_WEB__SWAGGER_UI_URL=  # — (str | None)
LEX_WEB__TEMPLATE_DIRECTORY=templates  # Directory for Jinja2 templates (str)

# ── lexigram-webhook ──
LEX_WEBHOOK__ALLOW_PRIVATE_URLS=  # Allow registering and delivering webhooks to private, loopback, or link-local... (bool)
LEX_WEBHOOK__DELIVERY_LOG_RETENTION_DAYS=  # Days to retain delivery log (0 = indefinite) (int)
LEX_WEBHOOK__DELIVERY_TIMEOUT_SECONDS=  # HTTP request timeout per delivery attempt (float)
LEX_WEBHOOK__DISABLE_AFTER_CONSECUTIVE_FAILURES=  # Auto-disable threshold (int)
LEX_WEBHOOK__ENABLE_ADMIN=  # Whether to register the admin contributor (bool)
LEX_WEBHOOK__EVENT_ID_HEADER=  # HTTP header for the event ID (str)
LEX_WEBHOOK__EVENT_TYPE_HEADER=  # HTTP header for the event type (str)
LEX_WEBHOOK__FAILURE_WINDOW_HOURS=  # Window for counting consecutive failures (int)
LEX_WEBHOOK__RETRY_BACKOFF_FACTOR=  # Exponential backoff multiplier (float)
LEX_WEBHOOK__RETRY_BASE_DELAY=  # Initial retry delay in seconds (float)
LEX_WEBHOOK__RETRY_MAX_ATTEMPTS=  # Maximum delivery attempts before dead-letter (int)
LEX_WEBHOOK__RETRY_MAX_DELAY=  # Maximum retry delay ceiling in seconds (float)
LEX_WEBHOOK__SECRET_LENGTH=<change-me-in-production>  # Secret length in bytes (hex-encoded output is 2x) (int)
LEX_WEBHOOK__SECRET_ROTATION_GRACE_HOURS=<change-me-in-production>  # Hours both old and new secrets are accepted (int)
LEX_WEBHOOK__SIGNATURE_ALGORITHM=  # HMAC algorithm name (str)
LEX_WEBHOOK__SIGNATURE_HEADER=  # HTTP header for the HMAC signature (str)
LEX_WEBHOOK__STORE_BACKEND=  # Persistence backend ('sql' requires lexigram-webhook[sql]) (str)
LEX_WEBHOOK__TIMESTAMP_HEADER=  # HTTP header for the delivery timestamp (str)

# ---------------------------------------------------------------------------
# Variables referenced directly in source (os.getenv / os.environ) — optional;
# unset values fall back to code defaults. Keep in sync with source usage.
#
# Required by other demos:
#   LEXIGRAM_EXPERIMENT_SEED (llm-experiment), REALTIME_PORT (realtime-monitor)
# Referenced by core framework source / doctor CLIs:
#   ANTHROPIC_API_KEY, APNS_KEY_PATH, APP_ENV, AUDIT_HMAC_KEY, AUTH_JWT_SECRET,
#   BROKER_URL, DATABASE_URL, F5_TTS_REFERENCE_ROOT, FCM_SERVER_KEY, JWT_SECRET,
#   OAUTH_CLIENT_SECRET, OPENAI_API_KEY, OTEL_EXPORTER_OTLP_ENDPOINT,
#   RABBITMQ_URL, REDIS_URL, SMTP_HOST, VECTOR_BACKEND, VECTOR_STORE_BACKEND
# Test/tooling only:
#   ADMIN_BASE, ADMIN_SETUP_TOKEN, AUTH_SECRET, ENVIRONMENT, LEXI_SECRET,
#   LOG_LEVEL, PLAYWRIGHT_SNAPSHOT, TEST_POSTGRES_DSN
# ---------------------------------------------------------------------------
ADMIN_BASE=http://127.0.0.1:9003  # lexigram-admin e2e test base URL
ADMIN_SETUP_TOKEN=changeme  # lexigram-admin boot token (integration/CI)
ANTHROPIC_API_KEY=sk-ant-changeme  # Anthropic provider key (AI doctor)
APNS_KEY_PATH=  # Apple push notification key path
APP_ENV=development  # generic app environment read
AUDIT_HMAC_KEY=changeme  # audit doctor / signing key
AUTH_JWT_SECRET=changeme  # lexigram-auth JWT secret
AUTH_SECRET=changeme  # lexigram-cli environment validation
LEX_CONFIG_ALLOW_UNKNOWN=false  # bypass strict unknown-key errors (true/false)
OAUTH_CLIENT_SECRET=change-me-oauth-client-secret  # app startup secret hook
BROKER_URL=amqp://guest:guest@localhost:5672//  # queue doctor broker URL
DATABASE_URL=postgresql://lexigram:lexigram@localhost:5432/lexigram  # SQL doctor / DB URL
ENVIRONMENT=development  # legacy app-environment compatibility reads
F5_TTS_REFERENCE_ROOT=  # multimedia-tts reference audio root
FCM_SERVER_KEY=changeme  # Firebase Cloud Messaging key
JWT_SECRET=changeme  # app startup secret hook (JWT signing)
LEXIGRAM_EXPERIMENT_SEED=  # demos/llm-experiment seed
LEXI_SECRET=changeme  # secrets store test fixture
LOG_LEVEL=info  # SQL logging level read
OPENAI_API_KEY=sk-changeme  # OpenAI provider key (AI doctor)
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4318  # OpenTelemetry collector endpoint
PLAYWRIGHT_SNAPSHOT=  # set to 1 to update admin e2e snapshots
RABBITMQ_URL=amqp://guest:guest@localhost:5672//  # queue doctor RabbitMQ URL
REALTIME_PORT=7071  # demos/realtime-monitor port
REDIS_URL=redis://localhost:6379/0  # queue doctor Redis URL
SENTRY_DSN=  # Sentry DSN; error tracking falls back to this when LEX_MONITOR__ERROR_TRACKING__DSN is unset
SMTP_HOST=localhost:25  # notification doctor SMTP host
TEST_POSTGRES_DSN=postgresql://lexigram:lexigram@localhost:5432/lexigram_test  # events/tasks postgres integration tests
VECTOR_BACKEND=memory  # vector doctor backend name
VECTOR_STORE_BACKEND=memory  # vector doctor backend name

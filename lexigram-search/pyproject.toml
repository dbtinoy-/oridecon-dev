[build-system]
requires = [
    "hatchling",
]
build-backend = "hatchling.build"

[project]
name = "lexigram-search"
version = "0.1.3005"
description = "Full-text search and indexing for Lexigram Framework - Elasticsearch, Meilisearch, and Algolia"
readme = "README.md"
requires-python = ">=3.11"
keywords = [
    "search",
    "elasticsearch",
    "meilisearch",
    "algolia",
    "full-text",
    "indexing",
    "async",
    "framework",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Internet :: WWW/HTTP :: Indexing/Search",
    "Framework :: AsyncIO",
    "Typing :: Typed",
]
dependencies = [
    "lexigram>=0.1.1",
    "lexigram-contracts>=0.1.0",
    "typing-extensions>=4.0.0",
    "jinja2>=3.1.0",
]
authors = [
    { name = "Lexigram Framework Team", email = "team@lexigram.dev" },
]
maintainers = [
    { name = "Lexigram Framework Team", email = "team@lexigram.dev" },
]

[project.license]
text = "MIT"

[project.optional-dependencies]
elasticsearch = [
    "elasticsearch>=8.0.0",
]
meilisearch = [
    "meilisearch>=0.28.0",
]
algolia = [
    "algoliasearch>=3.0.0",
]
# Database-native search backends
postgres = [
    "asyncpg>=0.29.0",
]
mysql = [
    "aiomysql>=0.1.0",
]
sqlite = [
    "aiosqlite>=0.19.0",
]
mongodb = [
    "motor>=3.3.0",
]
# Bundles
database = [
    "asyncpg>=0.29.0",
    "aiomysql>=0.1.0",
    "aiosqlite>=0.19.0",
]
search-all = [
    "elasticsearch>=8.0.0",
    "meilisearch>=0.28.0",
    "algoliasearch>=3.0.0",
    "asyncpg>=0.29.0",
    "aiomysql>=0.1.0",
    "aiosqlite>=0.19.0",
    "motor>=3.3.0",
]
test = [
    "lexigram-testing>=0.1.1",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]
dev = [
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
all = [
    "elasticsearch>=8.0.0",
    "meilisearch>=0.28.0",
    "algoliasearch>=3.0.0",
    "lexigram-testing>=0.1.1",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[project.urls]
Homepage = "https://lexigram.dev"
Repository = "https://github.com/dbtinoy-/lexigram"
Documentation = "https://docs.lexigram.dev"
Issues = "https://github.com/dbtinoy-/lexigram/issues"
Changelog = "https://github.com/dbtinoy-/lexigram/blob/main/CHANGELOG.md"

[project.entry-points."lexigram.modules"]
SearchModule = "lexigram.search.module:SearchModule"

[project.entry-points."lexigram.config"]
search = "lexigram.search.config:SearchConfig"

[project.entry-points."lexigram.cli.contributors"]
search = "lexigram.search.cli.contributor:SearchCliContributor"

[dependency-groups]
dev = [
    "black>=23.0.0",
    "ruff>=0.1.0",
    "mypy>=1.0.0",
]
test = [
    "lexigram-testing>=0.1.1",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0.0",
    "pytest-mock>=3.10.0",
]

[tool.ruff]
extend = "../pyproject.toml"
line-length = 88
target-version = "py311"

[tool.ruff.lint]
extend-ignore = [
    "E722",  # bare except — search backend fallback patterns
    "PLE0604",  # `*` used in `__all__` without `__all__` defined
    "RET504",  # unnecessary assignment before return — ORM building patterns
    "SIM118",  # use key in dict instead of key in dict.keys()
    "TC004",  # TYPE_CHECKING re-import — messaging protocol forwarding
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "TID252"]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "all"

[tool.ruff.lint.isort]
known-first-party = ["lexigram"]
force-sort-within-sections = true

[tool.mypy]
python_version = "3.11"
mypy_path = ["src"]
namespace_packages = true
explicit_package_bases = true
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
warn_unreachable = true
strict_equality = true
show_error_codes = true
overrides = [
    { module = "tests.*", ignore_errors = true },
    { module = "docs.*", ignore_errors = true },
]

[tool.uv.sources.lexigram]
workspace = true

[tool.uv.sources.lexigram-testing]
workspace = true

[tool.uv.sources.lexigram-contracts]
workspace = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
addopts = ["--cov=lexigram.search", "--cov-report=term", "--cov-fail-under=40"]
testpaths = ["tests"]
markers = [
    "integration: mark a test as an integration test",
    "scenario: marks cross-package scenario integration tests",
    "requires_elasticsearch: marks tests that require a live Elasticsearch connection",
]

[tool.hatch.build.targets.wheel]
packages = [
    "src/lexigram",
]
